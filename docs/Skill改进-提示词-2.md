# Skill 改进提示词 · 第二轮（⑫ 视角重构 + 必含内容强化 + 第一册优先 + 初学者友好）

> 用途：把用户对「教材应该长什么样」的 6 条要求，落成对 `replicate-learning` Skill 的具体改动。
> 与第一份的关系：`docs/Skill改进-提示词.md` 管**工具缺陷**（D1/D4/D5/D3）；本份管**教材规则与判据**。两份可分别执行。
> 直接整段复制下方「提示词正文」给目标模型。

---

## 提示词正文（从这里开始复制）

你是一名技术文档规范与静态检查工具的设计者。任务：**按用户的 6 条要求改造 `replicate-learning` Skill 的教材规则与闸门**。这是"收紧要求、补上缺失维度"的改造，**不是**重构整个 Skill。

### 零、环境与铁律（先看这段，避免白干）

- Windows 11 + PowerShell；控制台代码页 **GBK(936)**。
- **所有 python 命令前先设** `$env:PYTHONIOENCODING='utf-8'`，否则闸门脚本在管道下会抛 `UnicodeEncodeError`（已知缺陷 D1，本轮不要求你修，但你必须绕开它）。
- **带圈数字陷阱（必须修，见 §三-R7）**：`⑨ ⑩ ⑮ ⑯`（U+2468/2469/246E/246F）在本环境的落盘路径可能丢失，而 `gate_lecture.py` 用 `^## ⑫\s`、`^## ⑯\s` **定位段落**——标题丢码会导致**整段不被检查**。你本轮要新增多处检查，**必须先让"找不到段"变成可见的 FAIL**，否则你的改动无法被验证。
- **禁止修改任何被学习项目**：`D:\develop\workspace\skilltest-2026-09\agentic-rag-cold`、`...\ragent-official-cold`、`D:\develop\workspace\agentic-rag`、`D:\ragent-official` 一律**只读**（只用于跑回归与误报率实测）。
- 只允许修改：`<skill>` 目录内的 `SKILL.md`、`spec/*`、`references/*`、`scripts/*`、`docs/*`；以及最后同步安装位。

### 一、对象与必读

- Skill 权威目录：`D:\develop\workspace\replicate-learningV2\skills\replicate-learning`
- 安装位（最后要同步）：`C:\Users\13610\.agents\skills\replicate-learning`
- 备份已在：`D:\develop\workspace\skilltest-2026-09\_backup-skill-before-kimi`（出问题可对照，**不要删**）
- 开工前必读（按序）：`SKILL.md` 全文（重点 §1.1 路由、§1.3 最小交付、§2 读者定位、§3 证据边界、§4 七阶段、§5 切分、§6.1/§6.1.1/§6.2/§6.2.1/§6.3/§6.4/§6.5/§6.6、§7 输出目录）→ `spec/00-质量契约.json`（判据 SSOT）→ `references/批次讲解全文模板.md`（§6.4 可执行版）→ `spec/操作手册-闸门与工具.md` → `spec/血证档案.md`（改判据前必读）→ `scripts/gate_lecture.py`（判据实现）

### 二、基线（不改任何东西之前先做）

```powershell
cd D:\develop\workspace\replicate-learningV2\skills\replicate-learning
$env:PYTHONIOENCODING='utf-8'
python scripts/skill_selfcheck.py     # 期望 47 项 0 失败 PASS
python scripts/v2_selfcheck.py        # 期望 PASS (6 contract entries)
python scripts/gate_all.py --root "D:\develop\workspace\agentic-rag\NOTES\教学讲解" --src "D:\develop\workspace\agentic-rag" --out before.json
# ↑ 97 份存量讲解（只读）→ 这是你的"回归集"与"误报率测量集"，务必留下 before.json
python scripts/gate_all.py --root "D:\develop\workspace\skilltest-2026-09\ragent-official-cold\NOTES\教学讲解" --src "D:\develop\workspace\skilltest-2026-09\ragent-official-cold" --out before-official.json
```

### 三、改动清单（R1~R7，对应 6 条用户要求；每条都写清"改哪个文件、改成什么、闸门怎么查、怎么验"）

#### R1（用户要求 1）默认"就地写教材"要说清楚
- **要求**：教材默认写进**被学习项目自己的** `NOTES/教学讲解/`（Skill §7 已是如此），但必须补一条**前置提醒**：若目标仓库的 `.gitignore` **没有**忽略 `NOTES/`，则教材会进入该仓库的 git；此时应提示用户二选一（加忽略规则 / 改用隔离副本）。同时说明"隔离副本"只在需要隔离答案卷或保持原仓库零改动时才用。
- **改**：`SKILL.md` §7 + `references/V2执行协议.md` §1（启动时确定三个路径那段）。
- **验**：文档里能搜到"gitignore 未忽略 NOTES 时如何处置"的明确指引。

#### R2（用户要求 2 + 4）"只对着讲解看"必须成立：教材自足 + 初学者友好
- **要求（写进硬标准）**：
  1. **教材自足**：读者**不需要**在源码里额外加注释；教材代码块是源码**逐字复制** + 行尾 `// :L<真实行号>  ←教材：<说明>`，源码文件本身**不得被修改**。（现有规则已如此，但要写明"这是刻意的：读者对着教材看即可，行号可回查源码"。）
  2. **每段代码必须有"白话开场"**：在代码块**之前**用一句人话说清"这段代码在干什么、为什么需要它"；不允许直接甩代码。
  3. **逐行要点表必须覆盖全部关键行**（现有 §6.1.1 判据不变，但要点表要能解释**数据从哪来、到哪去、失败会怎样**，不能只翻译变量名）。
  4. **每个陌生 API 第一次出现**必须给"调用前手里有什么 / 调用后拿到什么 / 框架内部做了什么"。
- **改**：`SKILL.md` §2、§6.1、§6.1.1；`references/批次讲解全文模板.md` ⑥ 节说明；同步到 `spec/00-质量契约.json` 的 doc/tpl 锚点。
- **闸门**：密度类判据（D1/D2/D3）**保持不变**；新增"代码块前必须有白话开场句"的可选报告项（**先做报告项，不要直接 FAIL**，理由见 §四-2）。

#### R3（用户要求 3）"第一册优先"要成为**被支持的正式模式**
- **要求**：Skill 现在把"三本书同步推进"写成 V2 硬约束（`SKILL.md` §23-30 的硬约束 2、`references/V2执行协议.md` §2、`references/三本书与工程日志.md`）。用户需要的是：**允许按册设定优先级**——当用户明确说"先要第一册"时：
  1. 第一册（全阶段全批次）为唯一主线；
  2. 第二册/第三册**暂停**，但**必须在状态文件与能力账本里显式标注"用户指令暂停 + 预计何时补"**，不得静默省略；
  3. 每次收尾仍须报告"缺口清单"（哪些机制还没有实验证据、哪些架构问题还没挑战），保证暂停可见、可追。
- **改**：`SKILL.md` §23-30 硬约束 2、§1.1 路由（新增"先第一册/只要第一册"分支）、§7 输出目录说明；`spec/V2质量契约.json`（若硬约束被写进契约则同步）；`references/三本书与工程日志.md`；`references/V2执行协议.md` §2。
- **不得**：直接删掉"三本书"概念（它是 V2 的核心资产），而是改成"**默认同步 / 允许用户指定单册优先，且暂停必须留痕**"。
- **验**：在 SKILL 里能找到一个明确的"单册优先模式"小节，说明触发词、暂停留痕要求、缺口报告要求。

#### R4（用户要求 4）"讲解里的代码都要详细讲"要更硬
- **要求**：⑥ 节五层结构（问题→代码→逐行要点表→边界与副作用→【怎么用】/【上下游】/【怎么接】）保持不变；新增两条必含项：
  1. **每个文件/每个职责段都必须有"这段代码怎么被构造、怎么被调用"**（构造方式：谁 new 它 / 谁注入它 / 生命周期是什么）；
  2. **必须指出该类用到的实现手法**（设计模式 / 关键数据结构 / 算法），并说明"为什么用这个手法而不是更朴素的写法"。
- **改**：`SKILL.md` §6.4 ⑥ 必含项；`references/批次讲解全文模板.md` ⑥；`spec/00-质量契约.json` 相应条目（doc/tpl 锚点）。
- **闸门**：新增**报告项**（统计"⑥ 件里有多少件写了构造方式/手法"），**不要**直接 FAIL（避免把 97 份存量全判红）。

#### R5（用户要求 5 · 本轮最重要的改造）⑫「Vibecoding 视角」把"回顾已实现"改成"**从零构建**"

- **用户原话**：⑫ 那一节应该聚焦**真实企业级开发场景**，并且**默认没有该批的内容**，然后**根据项目已有的东西推导出流程**；而不是现在这样（默认该批已经实现、再回述"当时怎么做"）。
- **现状问题（要改掉的）**：`SKILL.md` §6.2 目前要求"**回顾性重构声明**"——⑫ 被写成对**已经写完的这一批代码**的回述（"这批代码是怎么复刻出来的"）。用户认为这**不合逻辑**：读者要学的是"我手上有一个需求，怎么在这个项目里把它做出来"。
- **改造后必须满足（写进 §6.2 与模板，且可机械检查）**：
  1. **默认设定改为"该能力尚未存在"**：⑫ 全节一律以"**你现在接到这个需求，要在本项目里把它从零做出来**"为起点；**禁止**出现"本批已经实现/本批的代码/回看这一批"这类以"已有该批成果"为前提的表述（自指禁令继续生效）。
  2. **必须从"项目里已有的东西"出发**：明确列出**可复用的既有资产**（真实类名/接口/配置/表/工具，带路径与行号），说明"为什么这个需求应该长在这些既有资产上"，以及"哪些既有约定必须遵守（否则会破坏什么）"。
  3. **必须落到真实企业级场景**：需求工单里要有**角色、业务价值、硬约束（性能/一致性/成本/合规/可观测）、失败代价**；至少一条"生产环境才会暴露"的约束（如降级、限流、幂等、审计、灰度）。禁止玩具需求。
  4. **流程必须完整可复现**：需求 → 既有资产勘察（读哪些文件、为什么）→ 方案比较（≥3 方案 + 落选理由）→ 增量实现（≥6 步、每步可独立编译/验证）→ 可直接复制的提示词（八段骨架）→ AI 产出后审查（≥6 项）→ 验证反馈循环（≥3 轮四段）→ 最终沉淀（≥5 条规则）。**每条都要有"现场会怎样"（AI 默认会怎么走偏、你凭什么一眼看出不对）**。
  5. **诚实标注保留**：如果这一节是你**逆向重建**的，必须显式标注"这是按最终代码逆向重建的叙事，不是当时的真实对话"——但**叙事视角仍必须是"从零构建"**（不得用声明当挡箭牌降低详细度）。
- **改**：`SKILL.md` §6.2 整节重写（含 §6.2.1 八条、§6.2.2 回链、§6.2.3 标签）、`references/批次讲解全文模板.md` ⑫ 整节、`spec/00-质量契约.json`（S12/A4/A6 等与 ⑫ 相关的条目）、`spec/血证档案.md`（新增一条血证，记录本轮要求的由来）。
- **闸门（关键）**：为 ⑫ 新增**可机械检查项**：
  - **FAIL 档**（判定项）：⑫ 段落中不得出现"回顾语"集合（如 `本批已实现`、`本批的代码`、`回看这一批`、`当时我们`、`已经写完` 等；**词表要按实际扫描结果给全，并给误报率**）；
  - **报告档**（不计 FAIL）：统计 ⑫ 是否写了"既有资产清单（≥3 条带路径）"、"企业级约束（≥1 条）"、"方案数"、"实现步数"、"审查项数"、"迭代轮次"、"沉淀条数"。
- **验**：拿 1~2 份存量批次（只读副本）改造前后的 ⑫ 段落做 A/B：改造前报"回顾语 N 处"，把该段按新模板重写一版后再跑 → 0 处且报告档维度齐备。

#### R6（用户要求 6）必须补齐"架构图 / 调用链 / 底层 / 实现方式与使用过程"
- **要求**：把这四样从"通常会有"提升为**每批必含且可机械检查**：
  1. **项目架构图**：① 节必须有**盒状/分层图**，能看出"本批对象在整体中的位置 + 数据流向 + 与外部依赖的边界"；图中出现的每个组件名都要是**真实存在的类/模块/中间件**。
  2. **详细调用链**：⑦.1 必须给出**编号调用链，每一跳带真实类名 + 行号**，并标出**跨模块 / 跨线程 / 跨进程边界**（如 T=请求线程、A=回调线程）。
  3. **深入底层**：⑧ 必须至少有一张卡达到 **L0-L3**（核心类尽量 L4），且 **L2 必须给可运行的手写等价实现**（显式标注"源码中没有这个文件"）、L3 必须给具体失败场景、L4 无实证时必须写"未实测"。
  4. **该类的实现方式与使用过程**：⑥ 件必须同时讲清"**内部怎么实现**"与"**外部怎么用**"（【怎么用】调用现场 + 最小可复制调用 + 传入/返回形态；【上下游】上游谁喂、下游谁吃、失败时各自看到什么；接口/抽象件还要【怎么接】最小实现 + 注册路径 + 扩展步骤）。
- **改**：`SKILL.md` §6.4 的 ①⑦⑧⑥ 必含项；`references/批次讲解全文模板.md` 对应节；`spec/00-质量契约.json` 新增/收紧条目；`scripts/gate_lecture.py` 增加检查。
- **闸门**：
  - ① 新增 FAIL 档：该节必须存在**图**（用 `text` 围栏且含 ≥5 条连接线/箭头），且图中至少 3 个标识符能在仓库中找到；
  - ⑦.1 新增 FAIL 档：必须有 ≥5 跳编号链且每跳带行号（`:L` 或 `:NNN`）；
  - ⑧ 新增 FAIL 档：必须出现 `L0`~`L3` 四个层级标签，且含"省略了什么生产边界"或等价表述；
  - ⑥ 的【怎么用】/【上下游】已有判据保持不变。
  - **每一条新 FAIL 都必须先给误报率**（见 §四-2）。

#### R7（工具缺陷，必须同批修，否则你新增的检查无法验证）
- **让"找不到段"变成可见 FAIL**：`gate_lecture.py` 的 ⓪ 组增加硬检查——17 个一级节标题中任一未按 §6.4 规范形态命中（含带圈数字丢失、多余空格），**报 FAIL 并打印实际标题原文**（现状是静默跳过）。同时给 `new_batch.py` 加"生成后自断言 17 个节标题正则全命中"。

### 四、判据变更规程（违反即视为未完成）

1. **不得放宽任何既有判据**（`spec/00-质量契约.json` 的 S/F/R/D/N/A/U/P/V 全部条目）。本次是**收紧 + 补维度**，不是放宽。
2. **新判据必须先测误报率再收紧**（Skill 自己的血证教训 H20/H22/H23）：
   - 先在 97 份存量讲解上**只报告不判定**，人工翻查误报；
   - 误报率≈0 才能升为 FAIL；否则只能做**报告项**，并写进存量工单；
   - 变更登记里必须写：**实测误报数 / 抽查了哪几份 / 误报原因**。
3. **必须走 SKILL §6.6 全流程**：递增 `GATE_VERSION` → 变更登记表加行（版本/日期/改了什么/影响面/存量工单）→ **回归集判定不变或逐条解释到"判定层"** → 公告与工单（写进 §6.6 与操作手册）。
4. **回归证据**：改前 `before.json` / 改后 `--baseline before.json` **逐字段比对**；任何字段变化都要逐条解释（口径：**登记里的数字必须复核到"判定"那一层**，不能拿探针中间量外推——血证 H24）。
5. **自检必须全绿**：`skill_selfcheck.py`（47 项）与 `v2_selfcheck.py`；若你新增了工具或判据，要同步扩自检项并让它跑通。
6. **同步四处副本**：源仓库（commit 由用户决定）／`C:\Users\13610\.agents\skills\replicate-learning`（**必须**，排除 `__pycache__`，同步后用安装位复跑两条自检）／workbuddy 拷贝／宿主项目内脚本副本——不存在的就如实说明。

### 五、交付物（缺一即未完成）

1. **改动补丁**：按文件列出改了什么（diff 或"改前/改后"片段）。
2. **变更登记**：§6.6 表格新增行 + `spec/00-质量契约.json` 的新增/修改条目 + 新增血证条目。
3. **误报率实测报告**：新 FAIL 项在 97 份存量讲解上的实测结果（命中数、抽查样例、误报数）。
4. **回归证据**：`before.json` 与 `--baseline` 比对输出、`skill_selfcheck` / `v2_selfcheck` 输出（含退出码）。
5. **改动说明（中文）**：每条 R1~R7 → 根因 / 改法 / 影响面（哪些存量批次会变红/变绿）/ 存量工单 / 回滚方式。
6. **验证清单**：把 §三 每条的"验"逐条跑一遍，附命令与真实输出。

### 六、禁止事项（写了会被退回）

-  重建/重写整个 Skill 或闸门算法；只做 §三 列出的改动。
- ❌ 为了让存量批次不变红而把新判据全部降级成"报告项"（该 FAIL 的必须 FAIL，只是要先验误报率）。
-  用"标免检小节"的方式让某个批次过闸门。
- ❌ 未经误报率实测就收紧判据。
- ❌ 修改任何被学习项目（只读）。
- ❌ 删除或弱化"证据等级 / 未实测标注 / 不全绿不提交"这些既有纪律。
- ❌ 不跑自检、不同步安装位就声称完成。
- ❌ 把"我建议"写成"已验证"。

### 七、建议顺序

1. 读必读文件 → 建基线（§二，含两份 before.json）。
2. **R7 先做**（让"找不到段"报 FAIL + new_batch 自断言）——否则后面的改动无法验证。
3. **R5**（ 重构，最重要）：SKILL §6.2 重写 → 模板同步 → 契约与血证 → 闸门新检查（先报告、验误报、再 FAIL）。
4. **R3**（单册优先模式）：SKILL 硬约束 2 + 路由 + V2 协议 + 三本书文档。
5. **R6**（架构图/调用链/底层/实现方式）：契约 + 模板 + 闸门（逐项验误报率）。
6. **R2 / R4**（初学者友好与代码详解强化）：文档 + 报告项。
7. **R1**（就地写与 gitignore 提醒）。
8. 跑全套回归 → 同步安装位 → 交 §五 交付物。

## 提示词正文（复制到此结束）

---

## 附：这几条要求对应的原始现场（供你自己核对）

| 要求 | 现场证据 |
|---|---|
| 现状 ⑫ 被写成"回顾已实现" | `SKILL.md` §6.2 现含"回顾性重构声明：本节的开发过程是回顾性重构…"；`references/批次讲解全文模板.md`  同款声明；实际生产出的批次（`logs\T9-独立盲评与最终验收报告.md` 与靶子 `NOTES\...\批次1-*.md` 的 ⑫）均以"本批"回顾口吻书写 |
| 三本书同步是硬约束 | `SKILL.md` 硬约束 2；`references/三本书与工程日志.md` |
| 带圈数字丢码会静默跳过 | `logs\T9-独立盲评与最终验收报告.md` §2.4（实测 11/11 → 10/11 的现场同源问题）、`gate_lecture.py:818/:984` |
| 回归/误报率纪律的出处 | `spec/血证档案.md` H20 / H22 / H23 / H24；`SKILL.md` §6.6 |