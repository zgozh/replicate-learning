# replicate-learning 改进方案

## 从「讲透一个项目」升级为「长出一个工程师」

> 版本：v1 · 2026-09-07
> 对象：`zgozh/replicate-learning`（本地源仓库 `D:\develop\workspace\replicate-learning`）
> 目标读者定位：**只懂一门语言基础语法的初学者**
> 核心诉求：提高学习**深度 / 效率 / 广度**；深入底层与**开发工程能力**；从整体到部分、分阶段到分批次；业务—架构—代码**完全吃透**。

---

> ## ★ 2026-09-07 修订纪要（按用户新要求，skill 已落地，本文档正文留作历史评审依据）
>
> **用户新要求**：vibecoding 时代，学习目标收敛为「懂业务—架构—代码实现」，**去掉所有强制读者动手/提交的环节**（不手写、不复述卡关、不交作业）；快速高效；**以落盘的教学讲解为唯一学习载体**（讲解必须保存到 `NOTES/教学讲解/` 对应位置），讲解本身要覆盖原方案全部深度/广度/工程要求。
>
> **由此做的调整（正式内容以 `skills/replicate-learning/` 源文件为准）**：
>
> | 原方案设计 | 落地调整 |
> |---|---|
> | Gate 0 盲设计 / 预测对照（读者先猜） | **去掉门禁** → 转译为讲解文本里的「直觉对照」小节（直接讲"新手本能会怎么写、为什么翻车、答案卷为何如此"） |
> | L2 手写（读者写 20~40 行） | 改为 **L2 手写示范**——AI 给出够真等价实现作为讲解材料，明确"阅读即可，不必自己写" |
> | 脱手复述/三层出题验收（读者答题、回炉卡关） | 改为**核对问答 + 标准答案（纯阅读）**：答案已给、无需提交、不卡进度 |
> | 工程练习卡（单测/调试/故障注入由读者做） | 转译为讲解内**工程视角小节**：测试视角 / 故障情景（配 AI 真跑的 L4 证据）/ 演进视角 / 横向对照，以读代练 |
> | 新增阶段 -1 系统全景 | **保留并落地** → 开局四件套含 `NOTES/00-系统全景地图.md`（五件产物），每件讲解②挂回定位 |
> | 概念词典去重 | **保留并落地** → `NOTES/概念词典.md`，先修只讲新词、已收录引用 |
> | 零件类紧凑讲解 | **保留并落地** → 新增 `references/零件类讲解模板.md` |
> | 能力台阶 L0~L4（决定读者练习形态） | **删去读者侧台阶** → 保留读者目标叙述（看得懂/讲得清/判断得准），穿透升级 L0~L4 由 AI 执行并写进讲解 |
> | 批次文件八块→十块 | 落地为批次模板的九块阅读结构（本批地图/先修/定位/清单/逐件详解/核对问答/构建证据/溯源 diff/三立即） |
> | 横向对照 / 换件重写 / 阶段闭卷考 | 横向对照并入工程视角按值得配给；换件重写与阶段闭卷考降级为可选深潜（README 口径未强制） |
>
> **已同步**：`skills/replicate-learning/` 四处副本逐字一致（源仓库 / `~/.workbuddy/skills` / `~/.agents/skills` / `D:\Agent_Learnings\...\dsh-toolkit\skills`）；GitHub 仓库 README 已更新口径。

---

## 目录

- [零、先说结论](#零先说结论)
- [一、现状诊断：它已经很强，但站错了位置](#一现状诊断它已经很强但站错了位置)
- [二、改进总纲：加四条轴](#二改进总纲加四条轴)
- [三、深度改进：从整体到部分的真正落地](#三深度改进从整体到部分的真正落地)
- [四、工程能力改进：补上六维（本方案的重头）](#四工程能力改进补上六维本方案的重头)
- [五、效率改进：降本不降深](#五效率改进降本不降深)
- [六、广度改进：不被一个项目绑死](#六广度改进不被一个项目绑死)
- [七、分阶段与分批次的**具体做法**](#七分阶段与分批次的的具体做法)
- [八、业务—架构—代码三层如何「完全吃透」](#八业务架构代码三层如何完全吃透)
- [九、文件级改动清单](#九文件级改动清单)
- [十、落地路线：分三步，别一次改完](#十落地路线分三步别一次改完)
- [十一、效果验证：技能版 TDD 用例](#十一效果验证技能版-tdd-用例)
- [十二、向后兼容：你已跑到阶段 4，产出不能废](#十二向后兼容你已跑到阶段-4产出不能废)

---

## 零、先说结论

这份 skill 目前是**一个高质量讲解生成器**。

它的所有机制——六段硬格式、满深度不分级、L1/L2/L3 底层穿透、逐类开工 Gate、讲解全文落盘、二十多条真实事故复盘——全部在优化同一件事：**让 AI 讲得更透、更稳、不退化**。

这套东西是真资产，**一行都不要删**。

但它有一个结构性上限：**知识流向是单向的（AI → 人）**。学习者在这套流程里的动作只有五个：确认目录、确认清单、听讲解、复述、手写 20 行。这是一个**消费者**的动作集，不是一个**工程师**的动作集。

工程师的动作集是：设计、写码、调试、测试、压测、排错、重构、权衡。

所以本方案的核心命题只有一句：

> **保留全部讲解质量机制，在上面叠加四条新轴，把学习者的角色从「听众」换成「动手的人」。**

四条轴分别是：**系统整体轴**（补）、**能力台阶轴**（新）、**工程维度轴**（新）、**时间轴**（新）。

---

## 一、现状诊断：它已经很强，但站错了位置

### 1.1 已经很强的部分（明确不动）

| 机制 | 价值 | 处置 |
|---|---|---|
| 六段硬格式（场景/位置/四问/逐行/调用链/用法） | 防「要点带过」的骨架 | 保留 |
| 满深度不分级 | 防 DTO/配置被一笔带过 | 保留 |
| L1/L2/L3 底层穿透 | **防 API 调用工程师，这是全篇最有价值的一节** | 保留并**加实证**（见 §4.2） |
| 逐类开工 Gate 1 穿透证据表 / Gate 2 六段骨架 | 把「事后检查」改成「动笔门禁」，根治质量漂移 | 保留并加 Gate 0（见 §3.3） |
| 讲解全文落盘 + 阶段/批次两级命名 | 对抗聊天窗口压缩 | 保留 |
| 每批构建验证 + 一批一 commit | 证据先于断言 | 保留并扩展到测试（见 §4.3） |
| 二十多条真实事故复盘清单 | 踩过的坑写进规则，非常成熟 | 保留 |

### 1.2 六个结构性缺口

| # | 缺口 | 具体表现 | 后果 |
|---|---|---|---|
| **1** | **「直接复刻」跳过了最强学习信号** | SKILL.md 明确写「不强制使用者手写第一版」「直接复刻」，流程是：读答案卷 → 复刻 → 讲透 | 认知科学里，**先自己尝试（哪怕失败）再对答案** 的记忆留存远高于直接看答案（测试效应 / 预测误差）。现在的流程主动放弃了这一层，学的是「看懂」，不是「想到」 |
| **2** | **工程能力几乎为零** | 全文翻遍，工程相关只有：每批构建验证、commit 规范、diff 对照。**测试、调试、并发、性能、可观测、故障排查、重构、演进阅读——一个都没有** | 学完能读懂能复述，但**不会测试、不会排错、不敢改**，这恰恰是「开发工程能力」的主体 |
| **3** | **底层穿透停在纸面推演** | L3 要求「推演具体故障」（如「业务 01.250 回滚把审计一起回滚」），但那是**想出来的**，不是**真跑出来的** | 对初学者，「想象的故障」和「亲眼看到的堆栈」是两种认知强度。前者记不住，后者记一辈子 |
| **4** | **「从整体到部分」没真正落地** | 有阶段/批次/类的分解，但**缺系统级整体认知这个强制前置**。实际执行是一上来就钻进 `system` 模块的 Sa-Token 三件套 | 学的人不知道这模块在整个系统里长什么样、请求怎么进来、数据怎么落库 → 盲人摸象，学完 30 批拼不出全景 |
| **5** | **无遗忘对抗机制** | 只有落盘，没有复习调度、没有跨批次串联、没有全局概念词典 | 学完就忘，且已讲过的概念在后续批次重复展开，浪费 token |
| **6** | **广度未扩展** | 只学一个项目的一种实现，无横向对照、无换框架重写 | 学会 MyBatis-Plus 的写法，换 JPA 仍归零——**这仍是 API 调用工程师，只是换了个更深的 API** |

### 1.3 一个必须点破的矛盾

skill 里同时写着两句话：

- 「直接复刻，不强制手写第一版」——为了**快速**；
- 「手写 20~40 行 No-Framework 版，证明你不是 API 调用工程师」——为了**深度**。

这两句在**同一批内**是冲突的：前一句让你抄，后一句考你写。**抄完再考**，考的其实是「背」。

解法不是二选一，而是**换顺序**：**先手写（哪怕很烂）→ 再看答案卷 → 再复刻 → 再讲透**。（见 §3.3 预测式复刻）

---

## 二、改进总纲：加四条轴

| 轴 | 现状 | 改进后 |
|---|---|---|
| **认知轴**（业务→架构→代码） | 已有，但只在**类级**执行，系统级缺失 | 补**系统级**一层：全景 → 模块 → 类 |
| **能力台阶轴**（新增） | 无。默认所有人、所有批次目标是「读懂」 | 每批标注目标台阶 L0~L4，决定**主练习形态** |
| **工程维度轴**（新增） | 只有「构建验证」 | 六维：演进阅读 / 测试 / 调试 / 并发性能 / 可观测与故障注入 / 重构设计。每批挂 1~2 维 |
| **时间轴**（新增） | 单程：讲→存 | 闭环：预测 → 对照 → 复刻 → 讲解 → 练习 → **复习** |

### 核心流程公式变化

```
旧：读答案卷 → 直接复刻 → 逐类讲透 → 落盘 → 下一批
                  ↑ 跳过
新：系统全景 → 盲设计(先自己想) → 对照答案卷(产生预测误差)
     → 复刻 → 穿透实证(L0源码/L4故障真跑) → 讲解(重心跟着"没想到"走)
     → 工程练习(六维挂1~2) → 复习(间隔) → 下一批
```

---

## 三、深度改进：从整体到部分的真正落地

### 3.1 新增「阶段 -1 · 系统全景」（强制，不可裁剪）

现在大型项目才做《全链路拆解文档》，且可裁剪。改为：**任何项目，无论大小，开工必做**，产出《系统全景地图》，五件东西：

| # | 产物 | 要求 | 为什么 |
|---|---|---|---|
| 1 | **运行时部署图** | 进程、端口、中间件、外部依赖的物理视角。一张 ASCII 图 + 每个框「它挂了会怎样」 | 初学者第一道墙是「这玩意儿到底跑在哪」 |
| 2 | **一条请求打穿全栈（Trace 实操）** | **不是画出来的，是真跑一遍抓出来的**：起服务 → 发一个请求 → 抓完整调用栈 → 贴真实日志/链路 | 「从整体到部分」的第一课就是**看一遍真实的完整流转** |
| 3 | **数据模型全景** | ER 图 + **表↔模块读写矩阵**（哪张表被谁读、被谁写）+ 每张表「为什么这么设计」 | 数据是系统的骨架，比代码稳定得多，先看数据再看代码 |
| 4 | **模块依赖方向图** | 每模块一句话职责 + **「删掉它，哪个业务问题会回来」** | 建立「每个模块都有存在理由」的直觉 |
| 5 | **业务约束 → 架构决策推导链** | 把非功能需求（QPS / 数据量 / 一致性 / 延迟 / 成本）逐条推到技术选型：为什么选 MySQL 不选 PG？为什么要 Redis？为什么要 MQ？**每条给备选 + 不选的理由** | **这是架构能力的核心**，现状完全没有。不会推导 = 永远只会照抄选型 |

> **给初学者的关键**：这五件东西做**一次**，后面几十批都靠它定位。没有它，每批都是孤立知识点；有了它，每批都能挂回全景。

**落盘**：`NOTES/00-系统全景地图.md`（固定路径，续传时第一个读它）。

### 3.2 能力台阶：决定每批怎么练（新增，关键）

现在的验收只有「脱手复述」（= L0）和一点手写（= L2 的边缘）。补成五级台阶，**每批开工前明确标注本批目标台阶**：

| 台阶 | 定义 | 主练习形态 | 验收方式 | 现状 |
|---|---|---|---|---|
| **L0 读懂** | 合上讲解能讲清每个类 | 脱手复述（题数=类数，附标准答案） | 复述对答案 | ✅ 已有 |
| **L1 能改** | 需求变更能改对 | **变异练习**：AI 提一个真实需求变更（"加一个按角色过滤"），学习者改码 | 改完编译 + 测试过 + diff 合理 | ❌ 缺 |
| **L2 能写** | 合上一切能自己写出来 | **盲写**：只给接口契约 + 需求，自己写实现，写完才对答案卷 | 与答案卷 diff，自己分析差异 | ⚠️ 只有 20 行片段 |
| **L3 能设计** | 给需求能自己出设计 | **盲设计**：给业务需求，自己出类图 / 表结构 / 接口签名 / 分支伪代码 | 与答案卷对照，写《差异分析》 | ❌ 缺 |
| **L4 能排错** | 出故障能定位 | **故障注入**：AI 埋一个 bug 或停掉一个中间件，学习者定位并修复 | 定位到具体行 + 说清为什么 | ❌ 缺 |

**台阶不是难度档，是练习类型。** 建议配比：一个项目里 L0 占 20%、L1 占 30%、L2 占 25%、L3 占 15%、L4 占 10%。前期以 L0/L1 为主建立手感，中后期加大 L2/L3/L4。

### 3.3 预测式复刻：把「先自己想」插到最前面（**单点收益最大**）

在现有 Gate 1（穿透证据表）、Gate 2（六段骨架）之前，加 **Gate 0 · 盲设计**：

```
Gate 0 · 盲设计（不看答案卷，15~30 分钟）
  ├─ 输入：本批业务需求（一句话）+ 已学上下文 + 项目全景地图
  ├─ 输出（学习者自己写，不抄）：
  │    ① 我会建哪几个类 / 表 / 配置，各自干嘛
  │    ② 关键方法签名
  │    ③ 主要分支伪代码（if/else、异常、兜底）
  │    ④ 我认为的难点在哪
  └─ AI 存档 → 然后才允许打开答案卷
```

对照后产出《预测 vs 答案对照表》：

| 我的设计 | 答案卷做法 | 判定 | 缺口原因（这是讲解重心） |
|---|---|---|---|
| 用 `ThreadLocal` 存用户 | 用 `UserContext` + TTL | 想到了 | — |
| 没考虑 | 登出时 `clear()` | **没想到** | 线程池复用导致串号 → 重点讲 |
| 在 Service 层查角色 | 在 Interceptor 层注入 | **想错了** | 职责放错层 → 重点讲 |

**三重收益**：

1. **学习信号最强**——「没想到 / 想错了」的地方，认知冲击力是平铺直叙的几倍；
2. **讲解重心可倾斜**——深度预算花在刀刃上（已懂的地方快讲），**这是最大的效率杠杆**；
3. **天然产生 L3 素材**——「想错了」的原因就是真实的取舍分析。

> ⚠️ 注意：这不降低满深度要求。全量六段照讲，**只是「没想到/想错了」处加倍展开，已懂处收短**。深度不打折，篇幅重分配。

---

## 四、工程能力改进：补上六维（本方案的重头）

> 用户原话：「应该要深入底层和开发工程能力……不然就只是个 API 调用工程师」。
> 现状：六维一个都没系统覆盖。以下逐个补，**每维一个可落盘模板**放进 `references/`。

### 4.1 维度一 · 演进阅读（读 git 历史，看代码怎么长出来）

**练什么**：`git log -p <file>` / `git blame` / release notes / upgrade SQL，**反推这个类是为什么变成现在这样的**。

**为什么重要**：看最终态只能学到「是什么」，看演进能学到「**为什么**」——哪个 bug 催生了哪个分支，哪个需求催生了哪个抽象。这是最便宜的设计经验获取方式，且**完全不需要天赋，只需要纪律**。

**练习件 · 演进卡**：

| 项 | 内容 |
|---|---|
| 类/文件 | `AuthServiceImpl.java` |
| 关键提交 | `a1b2c3 feat(auth): 增加登出清 Token` |
| 这个提交解决了什么 | 登出后 Token 仍可用 → 越权 |
| 改之前的写法 / 改之后 | ... |
| 我从中提炼的决策 | 敏感操作必须服务端失效，不能只靠前端删 Cookie |
| 可迁移到 | 任何会话/凭证类功能 |

### 4.2 维度二 · 测试（现状：`批次.txt` 里自发做了单测，但 skill 里**完全没要求**）

**这是最不该缺的一维**——学一个成熟项目却不学它怎么测，等于只学了一半。

**练什么**：

- 这个类的哪些分支值得测（正常 / 边界 / 异常 / 并发）；
- mock 什么、不 mock 什么（为什么 `StpUtil` 静态难测、怎么绕）；
- 断言什么（不只是「没抛异常」，是**行为正确**）；
- 集成测试怎么搭（Testcontainers / 内存 DB）。

**要求**：每批**至少一个类写真实单测**，且**测试也是复刻对象**——读答案卷的测试（如果有），讲它为什么这么测。

**练习件 · 单测卡**：

| 类 | 测什么分支 | 怎么造输入 | 断言什么 | 踩到的坑（如 `excludePathPatterns(String...)` 重载歧义） |
|---|---|---|---|---|

> 注：`批次.txt` 里已经出现过真实的 mock 重载歧义踩坑（`anyString()` 匹配不到 `String...` → 返回 null → NPE）。这种坑**应该被固化成教学素材**，而不是修完就过。

### 4.3 维度三 · 调试（会不会调试，是新手和工程师的分水岭）

**练什么**：

- 断点类型（行断点 / 条件断点 / 异常断点 / 方法断点）；
- 表达式求值、修改变量继续跑；
- 日志级别与定位（怎么加临时日志、怎么读堆栈）；
- 抓包（HTTP / RPC）；线上态工具（JVM 项目：Arthas `watch`/`trace`/`tt`）。

**练习形式（L4）**：AI **故意埋一个 bug** 或**关掉一个中间件**，学习者用调试手段定位到具体行。

**练习件 · 调试卡**：

| 现象（真实报错） | 我的第一个假设 | 怎么验证（断点/命令） | 结论 | 根因 | 修复 |
|---|---|---|---|---|---|

### 4.4 维度四 · 并发与性能

**练什么**：

- 这个类的**线程模型**：这段代码在哪个线程跑？有几个线程？（Tomcat 线程池 / 异步 / MQ 消费者）
- 共享状态在哪、谁会并发改、锁粒度；
- 连接池配置（数量怎么定、为什么）；
- 压测：怎么打、看什么指标、怎么定位瓶颈。

**要求**：凡涉及共享状态 / 异步 / 池化的类，**必须画线程时序图**，并回答「两个请求同时进来会怎样」。

**练习件 · 并发卡**：

| 共享资源 | 谁会并发访问 | 现方案怎么保护 | 去掉保护会怎样（真跑） | 瓶颈点 | 压测结果 |
|---|---|---|---|---|---|

### 4.5 维度五 · 可观测 + 故障注入（把 L3 从「推演」升级为「实证」）

**现状问题**：L3 要求「推演具体故障」，但那是想出来的。**对初学者，想出来的记不住。**

**改进：L3 推演 + L4 实证，两者都要。**

**L4 实证的做法**（真做一遍，贴真实证据）：

| 实验 | 怎么破坏 | 预期推演（L3） | 真跑观察到的（L4） | 差异与收获 |
|---|---|---|---|---|
| ThreadLocal 串号 | 删掉 `finally { clear() }` | 下个请求读到上个用户 | **真实日志/返回体截图** | 为什么本地不复现（线程复用概率） |
| 审计丢失 | 把 `@Transactional` 加错层 | 回滚把审计一起撤 | **DB 真实行数对比** | — |
| 缓存击穿 | 停掉 Redis | 全打 DB | **DB 连接数打满的真实报错** | — |

**练习件 · 故障卡**：上表就是模板，关键是**必须贴真实证据**（日志、堆栈、数据行），不许写「会导致问题」。

> **底层穿透升级为 L0–L4 五层**：
> - **L0 源码实证**（新）：必须打开真实源码（IDE 跳转 / 反编译 jar / GitHub），**贴出真实代码片段 + 文件名 + 行号**。现状 Gate 1 要求「先查证再写」，但没要求贴证据片段 —— 补上。
> - L1 机制层（保留）
> - L2 手写层（保留）
> - L3 取舍层·推演（保留）
> - **L4 故障实证**（新）：真破坏、真跑、贴真证据。

### 4.6 维度六 · 重构与设计演进（学完之后，能不能改进它）

**练什么**：复刻完之后，回头审视——

- 这个类有什么坏味道（过长、职责过多、魔法值、分支嵌套深）；
- 如果需求变成 X，现在的设计哪里会痛；
- 答案卷有没有可以改进的地方（**答案卷不是圣旨**）。

**为什么重要**：这是从「会复刻」到「会设计」的桥。**只复刻不批判 = 永远在别人后面。**

**练习件 · 重构提案卡**：

| 现状 | 问题（具体后果） | 我的改法 | 代价/风险 | 答案卷为什么没这么改（推测） |
|---|---|---|---|---|

### 4.7 六维怎么挂到批次上

不是每批都做六维（那会爆炸）。规则：

- **每批挂 1~2 维**，由本批内容性质决定：
  - 涉及共享状态/异步 → 挂④并发
  - 涉及中间件 → 挂⑤故障注入
  - 有可测的分支逻辑 → 挂②测试
  - 答桉卷该类 git 历史有料 → 挂①演进
  - 复刻完回头看 → 挂⑥重构
- **每阶段至少覆盖 4 维**；一个项目下来六维各做 ≥2 次。

---

## 五、效率改进：降本不降深

现状「所有文件一律满深度散文」成本极高，几十批下来上下文爆炸。以下改动**深度不降，篇幅可降 40%+**：

### 5.1 零件类换文体（最大杠杆）

现状：DTO / 枚举 / 配置 / 常量也用和机制类一样的散文六段。

**改**：零件类用**结构化填空模板**，八项固定，每项 2~5 行，**信息密度更高、更好复习、篇幅减半**：

```markdown
### 类 3/8：UserRole（枚举）
- **为什么存在**：……（删掉它会怎样）
- **每个值的语义**：ADMIN=…… / USER=……
- **数据从哪来**：DB 哪个字段 → 谁转换 → 谁塞进来
- **被谁读走**：谁判断它、判断后做什么（指针到具体类+方法）
- **为什么用枚举不用 String/int**：方案对比 + 取舍
- **会咬人的地方**：同事把 "bailian" 拼成 "BaiLian" → equals 失配 → 静默走别家（**具体到人和故障**）
- **L1 机制**：枚举在 JVM 里是什么（拆到底）
- **No-Framework 手写版**：不用枚举怎么写（15 行内）
```

**深度一点没降**（八项覆盖现有六段的全部信息点），但**去掉了散文的注水**。

**配套**：黄金样例拆成两个 —— 现有 `黄金样例.md` 明确为**机制类**基准，新增 `黄金样例-零件类.md` 作为零件类基准。

### 5.2 全局概念词典（去重，第二个杠杆）

现状：先修概念是**批级**的，同一概念在多批重复展开。

**改**：建 `NOTES/概念词典.md`（全局、跨阶段累积），格式：

| 概念 | 大白话 + 生活化类比 | 首次讲解出处 | 备注 |
|---|---|---|---|
| AOP | 给方法门口挂道门帘，进出前后自动登记 | 阶段2批次1 §先修 | — |

后续批次遇到已收录概念：**只写「见概念词典 §AOP」一句话**，不重复展开。

**收益**：讲解篇幅下降，且**词典本身是复习神器**（三个月后翻一遍就想起大半）。

### 5.3 讲解重心随预测表倾斜

见 §3.3。已懂的快讲，「没想到/想错了」的加厚。**总深度不变，分布优化。**

### 5.4 上下文节流

- 每批开工只读：① 概念词典 ② 系统全景地图 ③ 上一批落盘文件 ④ 本批答案卷源码。**不全量读历史批次**。
- 阶段切换时开新会话（现状已有会话分配策略，明确为**强制**而非建议）。

---

## 六、广度改进：不被一个项目绑死

### 6.1 横向对照卡（每阶段收官 1 张）

学到某个机制时，问：**别的成熟项目怎么做同一件事？**

| 机制 | 本项目做法 | 项目 B 做法 | 差异原因 | 我更认可哪个、为什么 |
|---|---|---|---|---|

**获取方式**：AI 直接检索同类开源项目对比（成本很低，一次 10 分钟）。

**收益**：把「MyBatis-Plus 的写法」升级为「ORM 逻辑删除的通用解法”——**这才是可迁移的**。

### 6.2 换件重写（每 2~3 阶段一次，终极防 API 调用工程师检验）

挑一批核心依赖，**换成另一种技术栈重写同一批，跑通同样的测试**：

| 原实现 | 重写为 | 检验什么 |
|---|---|---|
| MyBatis-Plus 逻辑删除 | 手写 JDBC / JPA | 是否真的懂逻辑删除，还是只懂 `@TableLogic` |
| Sa-Token | 手写 JWT + 拦截器 | 是否真的懂会话，还是只懂 `StpUtil.login()` |
| Spring `@Transactional` | 手写事务模板 | 是否真的懂传播行为 |

**这比 L2 的 20~40 行片段强得多**——20 行只能证明"我知道原理"，重写一批能证明"我能用原理造出来"。

### 6.3 一机制多场景

学到的模式，要求**说出 3 个它还能用在哪**（如「业务唯一 key + 三态 + TTL 做幂等」还能用于：防重复下单、防重复推送、防重复扣款）。写进《模式卡》（现状「经验沉淀」节已有雏形，提为强制）。

---

## 七、分阶段与分批次的**具体做法**

### 7.1 阶段划分法（改进版）

现状按**模块**切（framework / system / rag…）。改为主维度**系统分层**，并在每个阶段标注**能力目标 + 工程维度**：

| 阶段 | 主题 | 能力目标 | 挂靠工程维度 | 预估批次 | 阶段验收（闭卷） |
|---|---|---|---|---|---|
| **-1** | 系统全景 | L0 读懂全局 | ①演进 | 2~3 | 能画出全景图并讲清「删掉 X 会怎样」 |
| **0** | 跑通 + Trace | L0 | ③调试 | 1~2 | 能抓出一条完整请求链路并逐跳解释 |
| 1 | 数据层（表/DO/Mapper） | L1 | ②测试 | 3~4 | 自己加一张表并跑通 CRUD |
| 2 | 地基 framework | L1→L2 | ②⑥ | 4~5 | 独立改一处横切逻辑 |
| 3 | 业务域 A | L2 | ④⑤ | 4~6 | 盲写一个模块的实现 |
| … | … | **台阶递增** | **轮转覆盖六维** | … | … |
| N | 收官 | L3 + L4 | 全部 | 2 | 换件重写 + 横向答辩 |

**关键变化**：
- 阶段不再只是「切哪块代码」，而是**「练到什么能力」**；
- 能力台阶**单调递增**（前期读懂，中期能改能写，后期能设计能排错）；
- 工程维度**轮转覆盖**，不重复不遗漏。

### 7.2 批次划分法（改进版）

现状：按「相关类聚合」分，3~9 件/批。**保留这个主原则**，加两条切分约束：

1. **一批一个能力台阶**：不要把「需要盲设计的机制类」和「只需读懂的 DTO」混在一批（练习形态会打架）；
2. **一批挂 1~2 个工程维度**：本批内容天然关联哪维就挂哪维。

批次大小判据（**保留现状**）：每个件还能不能走满深度、不压成大纲句。

### 7.3 一批的标准流程（新版 9 步，对比现状 6 步）

```
 0. 回顾（5 分钟）：上一批 + 3 批前各抽 2 题        【新】
 1. Gate 0 盲设计：不看答案卷，自己出设计            【新】★ 最大收益
 2. Gate 1 穿透证据表：扫外部调用点，查证真实机制     【现有，加 L0 源码实证片段】
 3. Gate 2 六段骨架：先写子标题 + 每段一句要点        【现有】
 4. 整批写码（逐行三问注释）+ 预测对照表              【对照表是新】
 5. 逐类六段讲解（重心随对照表倾斜）                  【现有】
 6. 工程练习：本批挂靠的 1~2 维（测试/调试/并发/故障…）【新】
 7. 构建验证 + 测试通过 + diff + commit              【现有，测试提为强制】
 8. 落盘（十块齐全）+ 更新概念词典 + 勾地图           【现有八块 → 十块】
```

### 7.4 批次文件：八块 → 十块

在现有八块（先修概念 / 业务闭环定位 / 类清单表 / 全局调用链图 / 逐类六段 / 验收 / 构建证据 / 溯源 diff）基础上**新增两块**，插在中间：

| 新增块 | 位置 | 内容 |
|---|---|---|
| **④·五 预测 vs 答案对照表** | 放在类清单表之后 | 我想到了 / 没想到 / 想错了三列 + 缺口原因 |
| **⑥·五 工程练习卡** | 放在验收之后 | 本批挂靠维度的练习件（单测卡/调试卡/故障卡/并发卡/演进卡/重构卡） |

> **向后兼容**：已完成的批次**不返工**，从启用点起的新批次用十块。见 §12。

---

## 八、业务—架构—代码三层如何「完全吃透」

现状已建立「业务 → 架构 → 代码」三层主线（SKILL.md「认知主线」节），**方向是对的，但三层的内容厚度严重不均**：业务层最薄（只有一个具名场景），代码层最厚（六段）。

### 8.1 业务层：从「一个场景」扩到「四件东西」

| 补充项 | 内容 | 为什么 |
|---|---|---|
| **业务规则表** | 这个域的硬规则清单（"一个用户只能有一个 ADMIN 角色""订单 30 分钟未支付自动关闭"）。**每条规则标明它在代码哪一处落地** | 业务规则是需求的真身，代码只是它的投影 |
| **非功能需求/业务约束** | QPS、数据量、一致性要求、延迟上限、成本约束 | **这是架构决策的输入**，缺了它架构层就是空中楼阁 |
| **失败模式清单** | 这个业务动作可能怎么失败、失败了业务上怎么补偿 | 工程师和新手的区别：新手只想正常路径 |
| **角色与权限地图** | 谁能做这个动作、谁不能 | 大量代码（拦截器、鉴权、审计）都是为它服务的 |

### 8.2 架构层：从「上下游」扩到「决策推导链」

现状只讲「它放哪一层、上下游是谁、依赖方向」。补三件：

| 补充项 | 内容 |
|---|---|
| **约束 → 方案 → 备选 → 选择理由** | 每段架构决策写成四段式：「因为约束 X，所以需要 Y；可选方案有 A/B/C；选 A；代价是 Z」 |
| **删改推演** | 「如果把这个类移到另一层会怎样」「如果依赖方向反过来会怎样」——**具体的后果**，不是形容词 |
| **架构坏味道识别** | 循环依赖、层穿透（Controller 直接调 Mapper）、上帝类、散弹式修改。每阶段让学习者**在答案卷里找出 1~2 处**（答案卷不完美，找出来才是真懂） |

### 8.3 代码层：现状最强，补三项

| 补充项 | 内容 |
|---|---|
| **L0 源码实证** | 机制讲解必须贴真实源码片段 + 文件 + 行号（见 §4.5） |
| **L4 故障实证** | 真破坏、真跑、贴真证据（见 §4.5） |
| **测试视角** | 每个类讲完补一句「这个类该怎么测、测哪几个分支」（见 §4.2） |

### 8.4 三层的串联考核（阶段闭卷考）

每阶段结束，出**跨批次串联题**，要求学习者**合上所有笔记**作答：

> 例：「从用户在前端点『提问』开始，讲清：请求怎么进来的 → 经过了哪几层 → 查了哪些表 → 调了哪个模型 → 在哪一步可能失败 → 失败会怎样 → 你怎么定位这类故障 → 如果 QPS 涨 10 倍哪里先崩、怎么改」

答不上来的那一层 = 该层没内化，回炉。**这比逐批复述更能暴露「懂了局部、拼不起整体」。**

---

## 九、文件级改动清单

| 文件 | 现状 | 改动 | 优先级 |
|---|---|---|---|
| `SKILL.md` | 入口，六段+穿透+Gate1/2+落盘 | ①加「面向谁」里补**能力台阶**定位；②**加 Gate 0 盲设计**（§3.3）；③穿透升级 **L0–L4**（§4.5）；④标准循环 6 步 → **9 步**（§7.3）；⑤批次文件八块 → **十块** | **P0** |
| `SKILL.md`「常见翻车」 | 20+ 条 | 补 6 条：跳过盲设计直接抄 / 只推演不实证 / 不写测试 / 学完不复习 / 只学一个项目不对照 / 把答案卷当圣旨不批判 | P1 |
| `docs/复刻式学习方法-通用.md` | 全量方法论 | ①新增「系统全景」章节（§3.1）；②新增「工程能力六维」章节（§4）；③新增「能力台阶」章节（§3.2）；④「先修业务与架构」补业务规则表/约束/失败模式/权限地图（§8.1） | **P0** |
| `examples/黄金样例.md` | 机制类深度基准 | 明确标注为**机制类**基准，补充 L0 源码实证与 L4 故障实证示范 | P1 |
| `examples/黄金样例-零件类.md` | **不存在** | **新建**：DTO/枚举/配置/常量的结构化八项基准（§5.1） | P1 |
| `examples/黄金样例-工程练习.md` | **不存在** | **新建**：单测卡/调试卡/故障卡/并发卡/演进卡/重构卡各一份示范 | P1 |
| `references/概念词典模板.md` | **不存在** | **新建**（§5.2） | **P0** |
| `references/系统全景地图模板.md` | **不存在** | **新建**：五件产物的骨架（§3.1） | **P0** |
| `references/预测对照表模板.md` | **不存在** | **新建**（§3.3） | **P0** |
| `references/工程练习卡模板.md` | **不存在** | **新建**：六维六卡的空白模板 | P1 |
| `references/批次讲解全文模板.md` | 八块 | 扩为**十块**（加预测对照表、工程练习卡） | **P0** |
| `references/阶段规划表模板.md` | **不存在** | **新建**：阶段×能力台阶×工程维度×验收 的规划表（§7.1） | P1 |
| `references/横向对照卡模板.md` | **不存在** | **新建**（§6.1） | P2 |
| `references/阶段闭卷考模板.md` | **不存在** | **新建**（§8.4） | P2 |
| `references/类清单表模板.md` | 三列 | 加两列：**能力台阶**、**挂靠工程维度** | P1 |
| `references/AGENTS.md-复刻增量模板.md` | 增量 A/B/C | 增量 B 学习资料指针加两行：系统全景地图、概念词典 | P1 |
| `references/通用工程约定.md` | 验证/commit/续接/协作 | ①验证节加**测试必须**；②加「复习调度」条（间隔回顾） | P1 |
| `README.md` | 现状描述 | 同步新口径：系统全景、能力台阶、工程六维、L0–L4 | P2 |

---

## 十、落地路线：分三步，别一次改完

> **原则（沿用 README 里的「技能版 TDD」方法论）**：先写会翻车的场景 → 改到通过 → 收口反模式。
> 且：改动必须**向后兼容**（见 §12），不能让你已经跑完的 4 个阶段作废。

### v1 · 立刻做（改动小、收益最大，半天完成）

1. **Gate 0 盲设计 + 预测对照表**（§3.3）——单点收益最大
2. **概念词典**（§5.2）——效率杠杆最大
3. **测试提为强制 + 单测卡**（§4.2）——补最不该缺的一维

> 这三个改动**不推翻任何现有结构**，只是加动作，可以下一批就启用。

### v2 · 本阶段结束后做

4. **系统全景地图**（§3.1）——补「整体到部分」
5. **能力台阶 L0–L4**（§3.2）——决定练习形态
6. **工程六维其余五维 + 六个卡模板**（§4.1/4.3/4.4/4.5/4.6）
7. **L0 源码实证 + L4 故障实证**（§4.5）——把穿透从纸面拉到实证
8. **零件类换文体 + 零件类黄金样例**（§5.1）

### v3 · 中后期做

9. 横向对照卡 + 换件重写（§6）
10. 阶段闭卷考 + 串联题（§8.4）
11. 阶段规划表（§7.1）

---

## 十一、效果验证：技能版 TDD 用例

用改进前/后的 skill 跑下面四个场景。**改进前都应翻车，改进后应通过**：

| # | RED 场景（没有改进就会翻车） | 改进后通过标准 |
|---|---|---|
| **1** | 学完 20 批后，让学习者**独立实现一个新的业务模块**（不看答案卷） | 能跑通 + 测试过 + 说清每个设计决策的备选与取舍（**L2/L3**） |
| **2** | 停掉 Redis / 删掉一行 `clear()`，让学习者**定位问题** | 10 分钟内定位到具体行，并说清为什么（**L4 + 调试卡**） |
| **3** | 要求用**另一种技术**重写某批核心依赖（MyBatis-Plus → JDBC） | 重写版跑通同样测试（**换件重写**） |
| **4** | 三个月后随机抽问阶段 1 的一个概念 | 能答出，且能指出出处（**概念词典 + 间隔复习**） |

再加一个**元用例**（检验 skill 自身）：

| # | 场景 | 通过标准 |
|---|---|---|
| **5** | 让改进后的 skill 连续跑 5 批，检查质量是否漂移 | 5 批的「预测对照表」和「工程练习卡」都非空、都贴了真实证据（**门禁生效**） |

---

## 十二、向后兼容：你已跑到阶段 4，产出不能废

**硬约束（改 skill 时必须遵守）**：

1. **目录与命名不变**：`NOTES/教学讲解/阶段N-<主题>（技术）/批次M-<主题>（核心类）.md` 保持原样，已落盘的文件一个都不动；
2. **八块 → 十块是扩展不是替换**：新增块插在中间，已有八块的位置与内容不变；
3. **已完成的批次不返工**：从启用点起的新批次用新流程。若想补，只对 ★ 类补一张**工程练习卡**（成本可控）；
4. **概念词典可回填**：把已完成批次的先修概念抽出来建词典，是一次性收益；
5. **系统全景地图可随时补**：现在补也不晚，补完反而能让前面 4 个阶段的知识**挂回全景**——建议**优先补这个**，性价比最高。

---

## 附：一句话总结

> 现在这份 skill 解决的是「**讲得透不透**」。
> 改进后要解决的是「**你做不做得出**」。
>
> 做法不是推倒重来，而是：
> **加一个全景前置（看得见整体）、加一道盲设计门禁（先想后抄）、加六个工程维度（会测会调会排错）、把穿透从推演升级为实证（真破坏真跑）、用概念词典和零件类模板省下的成本去换复习和横向对照。**
