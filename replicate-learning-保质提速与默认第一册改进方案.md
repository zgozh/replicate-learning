# replicate-learning 保质提速与默认第一册改进方案

> 日期：2026-09-20
> 性质：基于现有 skill、脚本及真实教材的改进设计；2026-09-20 已实施默认第一册、短入口、章节组装、源文件清单和缓存等首轮改造。后续需用真实生成任务量测耗时与质量。
> 目标仓库：`D:\develop\workspace\replicate-learningV2`
> 调研版本：`afcf8f7fb780e1aa1a66899ff25a8b5c9ed5c3e2`；`gate_lecture.py` 的判据版本为 `2.30`。
> 教材样本：`D:\ragent-official\NOTES\教学讲解\第一册-项目源码与工程实现`。
> 本文其余建议需按实际代码与实测结果判断实施状态。

## 1. 结论与建议

可以显著减少生成过程中的重复劳动，但不能在没有完整运行数据时承诺“任何批次都从一小时变成十分钟”。现有教材本身很长、讲得很细，必要的源码调查和解释写作确实需要时间。应当优先减少重复读规则、重复生成、机械返工、无关册的管理任务，而不是缩成类职责摘要。

建议分两步推进：

1. **第一步：保持现有 17 节和教学深度，重做执行方式。** 默认只生成第一册；规则按任务加载；一次准备完整的本批证据；源码与固定结构由脚本生成；从开批就使用最终判据；修复只触及失败部分；状态与索引统一生成。
2. **第二步：用对照实验批准内容契约的简化。** 取消为凑数而规定的固定篇幅、固定失败轮数等要求，以真实机制覆盖、证据和读者任务检验替代。第一册的源码细讲、调用与扩展、故障分析、Vibecoding 教学继续保留。

优先级最高的五项：

| 优先级 | 改动 | 为什么先做 |
|---|---|---|
| P0 | 默认 `books=[1]`；二、三册未请求即不属于本次范围 | 满足本次明确要求，消除“暂停欠账”与反复询问 |
| P0 | 精简执行入口，合并相互冲突的规则 | 当前入口约 6.9 万字符，多处规则会让模型反复阅读、寻找口径 |
| P0 | 固定契约版本，写作前生成正确骨架和检查清单 | 最近批次反复经历首跑失败、局部修补、升档再修 |
| P1 | 复用源码注入，建立持久分片和注释计划，局部修复后重组 | 保留完整源码和讲解，减少重新输出整篇 |
| P1 | 机器检查与语义审读分工，统一结果和状态投影 | 减少为格式返工，把审读时间投入真实错误与因果解释 |

**不建议作为默认方案：**删去 ⑫、只讲核心类、把完整源码换成少数片段、拿缓存摘要替代首次源码精读、只要程序退出码为 0 就宣称通过、无条件让许多子 Agent 各写一章。

## 2. 调研依据与结论边界

### 2.1 实际检查的内容

- 仓库 `skills/replicate-learning/` 的入口、V2 协议、旧版及 V2 质量契约、阶段流程、三册说明、批次模板、样例索引、事故档案与工具手册。
- `gate_lecture.py`、`gate_all.py`、`inject_source.py`、`callsite.py`、`sync_gate_result.py`、`v2_selfcheck.py` 等相关实现。
- 第一册全部“批次*.md”的长度与章节分布；重点细读批次3、12、38、44的部分章节，并读取批次1、42、43等的结构数据。
- ragent 的续传状态与能力账本。状态文件记录了最近批次的修复轮数，但未提供能够拆解一小时耗时的完整模型调用轨迹。
- Codex 当前安装副本与权威仓库：排除 `__pycache__` 后各 55 个文件，内容比较无差异。本方案针对的就是当前安装版本，未把另一个旧版误当作分析对象。

上述为抽样内容审读与全目录结构统计，不是对所有教材逐句完成事实核验。

### 2.2 教材规模实测

统计时目录中有 44 份“批次*.md”，编号为1—18及20—45；未计 `.bak`。总计约 506 万字符，中位数 2,094.5 行、108,869.5 字符，最短1,478行、最长2,934行。目录仍可能继续生成，后续实施应固定输入快照再做比较。

| 批次 | 总行数 | 总字符数 | ⑥逐件讲解字符数 | ⑫字符数 |
|---|---:|---:|---:|---:|
| 1 启动装配 | 1,826 | 107,123 | 35,144 | 15,344 |
| 3 统一响应与异常 | 1,855 | 95,413 | 30,861 | 14,169 |
| 12 路由与首包探测 | 2,873 | 165,623 | 75,820 | 16,991 |
| 38 关键词族 | 1,851 | 104,940 | 52,124 | 12,047 |
| 42 Markdown解析 | 2,224 | 115,184 | 67,100 | 11,425 |
| 43 CSV与Excel解析 | 2,225 | 118,148 | 62,542 | 13,234 |
| 44 图片解析与模型 | 1,663 | 87,765 | 36,807 | 12,465 |

口径：UTF-8解码后的文本字符数，不是汉字数，也不是模型 token 数；章节统计忽略代码围栏内的 Markdown 标题。样本约25%—57%的字符位于围栏内，但围栏还包含图、提示词和教学示例，**不能把这个比例直接算作可以省掉的源码输出比例**。

这说明一个批次已经相当于一篇较长的教材。即使工具零延迟，模型需要新写的大量解释仍构成时间下限。

### 2.3 一次门禁实测

只读执行了批次43的 `gate_lecture.py`，参数包含 `--src D:\ragent-official`，使用 `python -B`，不写 `--json`，不改教材。

- 有效测量：**4.184秒，退出码0，v2.30总判定PASS，Git快照正常读取**。
- 沙箱中的预先尝试曾出现“快照读取失败，退回只比当前树”；排除权限限制后才得到上面的有效结果。
- 这是当前环境的一次单批测量，不是冷/热缓存基准，不代表全部检查、构建或端到端生成耗时。

因此当前有证据支持的判断是：**至少对批次43而言，单次门禁运行本身不是一小时的主要来源。** 真正需要测量的是门禁前后的长文生成与返工，以及是否每轮附带全库审计和构建。

### 2.4 事实、推断和目标分开

| 类型 | 本次可以说什么 |
|---|---|
| 已观察事实 | 入口文档很长；每节有多次样例对照要求；最近批次记载多轮返工；单批门禁本次约4.18秒 |
| 有依据的推断 | 重复阅读、长文重写与格式修补很可能占用大量时间；需要调用轨迹验证占比 |
| 用户体验事实 | 用户报告Flash模型一个批次常需1小时以上 |
| 尚未知 | 精确的输入/输出token、模型等待时间、实际完整生成时长分布、每类操作耗时比例 |
| 优化目标 | 相同源码范围、相同质量条件下，把中位端到端耗时先降到旧版的60%以内，再挑战40%以内 |

不使用文件创建时间与修改时间之差冒充一次生成的耗时，也不把单次门禁PASS解释为所有语义事实正确。

## 3. 现有质量为什么好，应该保住什么

样本的价值不只是“内容多”，而是把理解代码所需的几个环节连接起来：

1. **先解释问题，再看代码。** 批次3从“throw怎样变成JSON”组织异常族，而不是按类名列定义。
2. **代码有真实位置与教学解释。** 批次38既展示ES写入实现，也解释并发建索引、部分失败、404处理这些具体后果。
3. **解释调用与接入。** 构造方式、上下游、最小调用、扩展方式让读者能把孤立的类放回系统。
4. **讲正常路径，也讲时间顺序和失败。** 批次12围绕缓冲、首包判定、取消、名额归还展开，保留了流式并发场景真正难的部分。
5. **区分执行证据和设计。** 批次44明确指出没有专属测试，替身测试仍是设计态，VLM与存储真实调用未实测。
6. **教读者如何指挥和审查AI。** ⑫的项目约束、提示词、审查与偏差信号是第一册的重要工程教学内容，不应因为默认关闭二、三册而一并删除。

必须保留的底线：

- 纳入范围的文件有清晰覆盖记录；核心类、所有关键方法与异常路径不漏讲。
- 真实源码由工具读取和注入；来源、版本、路径、行号可以核验。
- 项目源码、教学合成片段、设计建议、真实实测严格区分。
- 对关键控制流、状态变化、资源释放、副作用解释“为什么”，而不只是复述语句。
- 初学者在当前批次能理解主线，不必不断跳到旧批次才能看懂。
- 围栏、表格、标题、链接可正常阅读；源码内容检查与结构检查都实际执行。
- 尚未运行的测试不写成通过；编译通过不写成单测通过；合成演示不写成真实生产结果。

## 4. 当前耗时来源与规则问题

### 4.1 第一册优先仍背着二、三册的管理任务

`SKILL.md:26、134、870` 与 `references/V2执行协议.md:25` 将二、三册定义为默认义务，只做第一册属于暂停，要写预计补齐时间、能力欠账、每次缺口报告，并在继续时询问是否恢复。

但 ragent 的状态文件本来已经是“第一册优先”。因此改变默认值主要消除**管理开销和范围歧义**，不能把三册内容的全部成本算成这个项目即将节省的时间。

### 4.2 入口过长，规则重复且存在冲突

当前 `SKILL.md` 实测约938行、69,052字符；完整模板约793行、21,628字符，质量契约约492行、15,878字符。

具体冲突包括：

- `SKILL.md:8` 要求不全文重读，但`:65—75`列出大量必读项，`:535—541`又要求写任何一节前进行模板、黄金节选、分维度黄金批对照。
- `SKILL.md:533`把历史批次称为可选校准，而后面的“开批三读”又使多套历史样例成为每节必读。
- 主文档阶段E/F代表“底层穿透/Vibecoding”，V2协议的E/F代表“第二册验证/第三册挑战”，G也分别指归档与工程交付。关闭第二册不应被误读成关闭第一册的底层穿透。
- 多处同时宣称自己是唯一权威：§6.4、质量JSON、模板、最高水位样例与脚本。模型必须花时间判断冲突时听谁的。

这些并不意味着模型每次一定读了全部文件；它们是**会诱发重复读取和错误选用规则的具体条件**。

### 4.3 后置检查和版本升级制造返工

ragent续传记录记载：批次39首跑7类FAIL后四轮修正，批次42四轮修复，批次43一轮修复，批次44两轮修复后升档又补深潜结构。

`SKILL.md:561`的流程还包括“盖戳后严格档复跑”。终检有必要，但如果前半程用宽口径、末尾提升声明才暴露缺项，模型就会在成稿后重写。

正确方向是：**开批固定最终适用版本，首次检查就是最终口径；检查可前移，终检继续保留。**

### 4.4 深度指标混入固定产量要求

例如：⑫不少于180行、至少3个方案、至少6步实现、至少3轮真实失败迭代、固定数量的反例或调用链跳数。`SKILL.md:414—415、441—477`还给出更高的轮数和篇幅指导。

其中最需要改的是“至少3轮真实失败”：仓库没有这些失败记录时，不能一边禁止编造事实，一边要求必须补足三轮。它既耗时，也诱导将设想写成历史。

不能简单全删这些规则。它们很多来自“越写越薄”的真实事故。应当保留要防的缺陷，换成**机制覆盖、不同失败类别、可执行验收、证据充分性**检查，并通过盲评证明替代标准有效。

### 4.5 工具已有提速基础，但没有完全收拢

- `inject_source.py`已经能按计划注入源码和教学注释，不需要重新开发一套注入器。
- `new_batch.py`已有17节骨架；应该扩展而非绕过它手工拼模板。
- `SKILL.md:545—561`要求 `verify_blocks.py`、`scan_detail.py`、`acceptance.py`及可选的`pipeline_batch.py`，但前两个及流水线脚本不在本次检查的skill `scripts/`中。规则引用了宿主沉淀，通用安装包不能假设每个项目都有。
- `sync_gate_result.py:56—75`再次启动门禁，并用正则解析展示文本。先人工跑、再sync、再升级口径重跑，存在重复执行与文本解析维护成本。
- `gate_all.py:302`明确说明存量问题不影响退出码；不能拿它的退出码0代替逐批内容通过。

### 4.6 脚本缓存可优化，但优先级低于减少LLM返工

- `gate_lecture.py:484—498`建全Java索引；`:518—520`逐行检查时调用`:554—560`的`src_code_set()`，后者重新读文件并归一化。
- `load_snapshot():579—610`通过`git archive`加载整份快照；`gate_all.py:79—80`每篇再次设置快照，相同SHA也可能重复加载。
- 快照文件缓存按相对路径取值，`gate_all.reset_snap()`仅无SHA分支清缓存；**可能发生跨SHA复用旧内容**。这是静态检查发现的风险，尚未用双提交夹具复现，应先写针对性回归再修，不能把推断写成已证实事故。

这些值得改，但把4秒门禁优化到1秒，本身不足以让一小时明显缩短。价值更大的是一轮集中反馈、减少模型等待与反复编辑，并避免缓存错误。

## 5. 逐项裁决：哪些必要，哪些可以简化

| 操作 | 裁决 | 新执行方式 | 质量保护 |
|---|---|---|---|
| 首次项目考古、完整文件清单 | 必要 | 首次建索引；先定位全量清单，再按本批精读 | 不用“没读”冒充“已讲” |
| 每批重新全仓考古 | 通常不必要 | 核对源码变更，失效相关索引 | 新增、删除、改名、未提交文件都纳入 |
| 本批源码及关键依赖精读 | 必要 | 批量读取、完整方法边界、带来源 | 不能只给模型类名和摘要 |
| 每节三套样例重新读 | 可简化 | 开批一次取匹配语言与结构的最小样例包，失败时按规则补读 | 版本固定，避免“上一批越来越薄” |
| 逐行复制源码和手写行号 | 不必要 | 脚本注入完整源码 | 保真与反向覆盖继续终检 |
| 中文教学注释、逐行要点与回放 | 必要 | 内容写一次，按分工展示；注释解释动作，要点解释因果，回放解释时间顺序 | 禁止三个位置逐字重复同一句 |
| 每个DTO硬凑复杂设计争论 | 可简化 | 仍解释所有字段、构造、使用、约束；没有算法就明确没有 | 不把“简单”当漏讲理由 |
| 第一册机制原理与必要实验 | 必要且按风险选取 | 能以源码证明的先静态核验；关键争议可跑小实验 | 关闭二册不等于关闭验证 |
| 每批默认完整第二、三册 | 删除默认义务 | 用户明确开启哪册才生成哪册 | 既有内容保留，未请求不算欠账 |
| 每批八步Issue交付日志 | 仅工程任务必要 | 纯教材生成只写批次证据与状态；实际修改代码才开Issue日志 | 保留关键命令与失败证据 |
| 每批运行无变化的全模块构建 | 可简化 | 首次基线、相关源码/依赖/环境变化或关键主张需要时运行 | 旧结果标时间与范围，不能伪称本次实跑 |
| 全部七类事实/结构门禁 | 必要 | 单批终检保留，前置检查合并输出 | 全绿必须带执行范围和计数 |
| 每次小修都全库审计 | 可简化 | 受影响批次检查；共享工具/模板改变与阶段收尾才全库审计 | 增量结果与定期全量结果比对 |
| 门禁失败后整篇重新生成 | 不必要 | 按规则ID修局部正文、注释表或结构，重新组装 | 终检绑定最终产物hash |
| ⑫完整工程教学 | 保留 | 保留需求、勘察、取舍、实施、提示词、审查、反馈、沉淀 | 不把第一册变成只读源码教程 |
| ⑫固定篇幅、三轮“真实失败” | 修改契约后取消 | 写出真实存在的迭代；其他写成明确的预设故障与验证方法 | 不伪造日志，不为凑数制造故障 |
| 每批重写所有地图和能力账本 | 可简化 | 一份结构化增量，自动生成摘要与视图 | 人工核对新增结论和覆盖主归属 |
| 每批清理全库`.bak`、修旧批版式 | 不应附带执行 | 临时产物进入工作目录；旧批治理单独立任务 | 历史教材不被自动改写 |
| 修改skill后全量回归 | 必要 | 作为skill发布流程，不混入每次教材生成 | 保留事故回归与多语言样本 |

## 6. 默认第一册：明确的行为协议

### 6.1 新默认

建议入口直接规定：

> 默认仅生成《第一册：项目源码与工程实现》。第二册与第三册是用户显式请求才启用的独立扩展；未启用时不创建其批次、索引、欠账或补齐计划，也不在每次“继续”时询问。第一册仍包含理解源码必需的机制说明、失败分析、测试证据及AI协作教学。

不要再把“第一册优先”命名为暂停模式。正常状态是`not_requested`，不是`paused`，更不是`incomplete`。

### 6.2 自然语言路由

| 用户表达 | 动作 |
|---|---|
| 学习这个项目／生成讲解／继续／下一批 | 默认或延续当前已确认范围；无显式扩展记录时只第一册 |
| 生成第二册／开始机制实验教材 | 开启第二册；默认从指定对象或当前机制开始，不自动补全所有旧批 |
| 生成第三册／系统做架构挑战 | 开启第三册；不顺带开启第二册 |
| 三册一起推进 | 明确设为1、2、3三册，并记录用户原话 |
| 只要第一册／关闭第二册 | 后续按新范围推进；保留原有文档 |
| 验证这段描述／解释架构位置／讲故障原理 | 默认视为第一册内部的核验或解释，不自动切册 |
| 给这段做一个实验 | 执行指定实验；不自动承诺长期生成第二册 |
| 帮项目修这个bug | 路由工程任务；工程日志与教材生成分开，避免为“讲解”执行生产代码改造 |

### 6.3 最小持久状态示意

```json
{
  "schema_version": 1,
  "books": [1],
  "extensions": {
    "book2": {"status": "not_requested", "request": null},
    "book3": {"status": "not_requested", "request": null}
  },
  "contract_profile": "book1-compatible",
  "source_revision": "16984b95454d3fc2a55b60ade1950fefeba339ec",
  "batch_id": "stage3-b44",
  "status": "validated",
  "next_batch": "stage3-b45"
}
```

`validated`只表示该批声明范围内的检查完成，不代表用户已经认可全部教材，也不代表所有代码路径经过运行验证。若使用用户验收状态，独立放`user_acceptance`字段，不要让每个普通批次都停下来等Accept。

### 6.4 老项目迁移规则

- 已有明确“只第一册”请求：转换为`books=[1]`，旧暂停记录转入历史，不再继续产生二、三册欠账。
- 仅因旧版默认产生的三册计划：新模式按`books=[1]`继续；保留已有文件，记录一次默认策略迁移。
- 已有明确“三册一起”授权且未撤销：保留其范围；全局新默认不覆盖用户已明确的项目选择。
- 范围无法辨别时：先完成不依赖二、三册的工作，只在需要执行扩展动作时确认。
- 二、三册尚未启用不影响第一册覆盖完成率和批次验收。真正缺失的第一册源码证据、故障解释仍应报缺口。

## 7. 新执行流水线：读一次、写一次、修局部

```text
读取模式与精简状态
    → 核对源码/规则/环境变更
    → 准备本批证据包与正确骨架
    → 写讲解分片和教学注释
    → 脚本注入源码、组装教材
    → 单次汇总检查
    → 必要时局部修复
    → 最终完整检查与语义审读
    → 更新状态、覆盖和索引
```

### 7.1 第一步：将入口变成执行导航

建议把`SKILL.md`精简到约150—220行作为工程目标，重点保留：触发范围、册选择、事实边界、当前工作流、按需阅读表、工具入口、失败处理。行数是维护目标，不是新的硬门槛。

将大块内容分离：

- `references/第一册执行协议.md`：开批、调查、写作、检查、续传的日常规则。
- `references/第一册质量卡.md`：由质量契约投影的本批适用检查清单。
- 现有事故档案继续保留，按失败规则ID按需阅读，不要求每批从头读。
- ⑫的长篇编写指导按需加载；二、三册模板仅在对应模式启用时加载。
- 黄金样例包只保留能示范结构与教学深度的短节，不需要每节再读取完整旧批次。

**权威次序只留一条：**用户本次明确范围 → 项目已确认范围 → 版本化质量契约 → 由契约生成的模板与检查器。样例证明“好内容长什么样”，不自行增加契约中没有的门槛。

### 7.2 第二步：准备本批证据包

证据包是轻量本地文件，不引入数据库、向量库或Agent平台。它包含：

- 项目/源码根路径、commit、工作树变化、规则版本。
- 本批范围：文件、主讲归属、关键方法、跨模块调用方、配置与测试关系。
- 每份源文件的内容hash；代码读取范围与尚未阅读的必要依赖。
- 关键主张及证据：源码位置、必要的外部版本信息、已有运行证据、未验证项。
- 初学者要理解的机制清单；正常/异常/边界/副作用的解释位置。
- 少量匹配的写法样例；不夹带旧批的源码、事实或结果作为新批证据。

源码精读不能省。效率来自一次批量读取相关原文、保留必要上下文，避免每写一个子节再从全仓搜索同一符号。调用搜索应一次处理本批多符号，同时覆盖生产和跨模块测试位置；基于正则得到的“提及”不得直接写成真实调用。

对于已在旧批讲过的依赖：当前批提供1—3句足以继续阅读的说明、它与本批的关系和回链。只有新增行为、版本变化或高风险机制才再次深入展开，不能只写“见上一批”。

### 7.3 第三步：组装型写作

复用现有`new_batch.py`与`inject_source.py`，不要再让LLM输出一遍完整源码：

1. 工具生成标题、章节、表格分隔线、稳定锚点与版本声明。
2. 模型写解释、因果、反例、回放和项目专用提示词。
3. 教学注释放在源码行号到解释的映射表中。
4. 脚本读取真实源码，注入行号与注释，组合成读者看到的完整Markdown。
5. 固定清单、索引、证据摘要由结构化内容渲染，避免模型手抄数字。

分片只按职责分为少数文件，例如`overview.md`、`items.md`、`mechanisms.md`、`engineering.md`。不把17节拆成17次独立模型对话，也不要求模型先写庞大JSON、再完整复述成Markdown。

工作目录建议：

```text
NOTES/.replicate-learning/
  state.json
  batches/<batch-id>/
    manifest.json
    parts/
    inject-plan.json
    gate-result.json
    metrics.json
NOTES/教学讲解/第一册-项目源码与工程实现/
  阶段N/批次M-主题.md
```

向读者交付的仍是一份自足的详细教材；内部工作文件用于续传、组装和核验。纯教学任务不默认创建五类Issue工程日志。

### 7.4 第四步：前置检查与汇总修复

不要等写完2000行才发现⑦标题层级、⑫提示词位置、逐行表分隔线不对。

- 初始化时校验骨架、范围、源码可访问性、工具存在性及规则版本。
- 写完一个较大的分片后运行便宜的结构检查；**同一命令汇总问题**，不每一行调用一次工具。
- 成稿后执行完整单批门禁，返回结构化问题：规则ID、文件、节ID、期望、实际、相关源码。
- 合并属于同一原因的错误，再让模型修一次；不要一条FAIL一轮完整对话。
- 问题修在分片或注释计划中，成品重新组装；避免下次注入覆盖刚修好的正文。

目标是首轮通过或一轮修复。超过两轮不强制放行，也不无限原地重试：检查是否规则冲突、检查器误报或证据不足；记录草稿状态与剩余缺陷，再决定局部升级模型或修工具。

### 7.5 第五步：验证后只提交一次状态增量

同一批只产生一份可信结果，再由工具更新覆盖矩阵、总索引和精简状态。长篇过程历史单独追加日志，不继续塞进“当前批次”一格。

ragent当前状态首页仍保留“本工作区未构建/已验证0”的旧口径，而后面的近期记录已有编译或测试执行。新状态应直接按证据计算，避免手写摘要彼此矛盾。

多文件写入不能仅靠逐个`os.replace`假装事务：先写版本化暂存产物，验证所有hash与链接，再更新一个当前批次指针；中途失败时续传根据manifest识别未提交产物，避免覆盖矩阵先标完成而教材没落盘。

## 8. 缓存与增量：只复用仍然有效的证据

### 8.1 缓存对象及失效条件

| 对象 | 缓存键的必要组成 | 失效触发 |
|---|---|---|
| 源码与符号索引 | 仓库规范路径、源文件hash、解析器版本 | 文件增删改名、内容变化、解析规则变化 |
| 调用点清单 | 相关源文件集合hash、符号、搜索根、工具版本 | 调用方变化，不能只看被调用类 |
| 第三方概念证据 | 依赖坐标/版本、证据来源与取得时间 | 锁文件/依赖版本变化，或关键行为需重新核验 |
| 教材门禁结果 | 正文hash、manifest、全部依赖源码hash、契约与检查器版本、适用模式 | 任意输入改变 |
| 运行/构建证据 | 源码及测试、构建依赖、命令、JDK/工具版本、必要配置 | 环境或相关代码变化；外部系统状态变化不得自动沿用 |

不能只用`HEAD`作为键：用户可能有未提交修改、未跟踪配置文件；跨项目同路径也不是同一个文件。时间戳只能用于快速筛选候选，最终依赖内容hash。

### 8.2 新批统一一个源码时点

新批以固定commit或显式工作树快照为依据。工作树有修改时，manifest记录每文件hash；不能注入工作树内容却声称全部来自旧commit。

源码生成中发生变化时，停止发布受影响内容，重读并更新对应说明。第一期不自动把当前源码与历史源码拼成一套“都能找到就算对”的解释。

历史批次仍按自己的快照验证；出现漂移就标历史事实与当前差异。为了生成下一批，不应默认重写全部旧教材。

### 8.3 检查结果复用不等于免检

- 当前批正文或注释变化，必须重新执行受影响的检查；终稿要有一次完整单批判定。
- 全库审计只重算依赖变化的批次，但保留结果清单，显示“本次执行”或“命中有效缓存”。
- 新规则发布、缓存schema变化、发现缓存错误、阶段收尾时安排全量复核。
- 某种语言/代码块没有检查器时返回`NOT_CHECKED`，不能因为检查对象计数为0而PASS。
- 快照读取失败时明确标证据不完整；如该批依赖历史源码，不能将退回当前树检查写成完整通过。

## 9. 第一册内容如何减少冗余而不变浅

### 9.1 第一阶段保持17节

保留①至⑯及索引，使新旧批次阅读习惯一致。暂不通过删章节来换速度。

| 部分 | 保留的教学功能 | 可减少的重复 |
|---|---|---|
| ①② | 系统位置与完整业务闭环 | 不反复复述六张全局图；本批只展开相关视图 |
| ③⑤ | 文件职责与覆盖计划 | 根据manifest渲染；不手写同一文件的多份状态 |
| ④ | 新概念及必要前置 | 旧概念给简短本地复习，不每批重新写定义长文 |
| ⑥ | 全源码、关键行解释、构造、用法、边界、接入 | 块内注释、要点表、讲解段分别负责动作、因果、权衡，不逐字重复 |
| ⑦ | 跨文件链路与状态变化 | 用真实节点数量，不为凑五跳加入空节点 |
| ⑧⑨ | 机制穿透与去框架示范 | 一个机制只保留一份主讲；示范不重复实现已充分解释的普通DTO |
| ⑩⑪⑬ | 失败、测试、证据 | 同一用例只完整解释一次，其他位置引用结果与适用范围 |
| ⑫ | AI工程协作能力 | 保留本项目决策与可复制提示词，减少通用八步方法的重复教学 |
| ⑭⑮⑯ | 理解自测、续传与质量结果 | 问答围绕真实误区；质量表由结果渲染，不再由模型重述全部数字 |

**对于⑤这类管理表：**第一阶段自动生成，保持既有版式；只有在对照评审确认不影响阅读后，第二阶段才考虑把详细计划移入工作目录、正文保留摘要。

### 9.2 注释与要点表不要机械复印

例如同一个`finally`：

- 块内注释：在正常退出和异常退出时都归还本次名额。
- 要点表：解释如果只在成功分支释放，中断会逐渐耗尽可用名额；指出释放使用哪个凭证。
- 回放：给出线程在何时被中断、取消句柄何时执行、名额何时归还的顺序。

三个位置都保留，但各自增加新的理解价值。相同事实与行号从证据包引用，文字不需要生成三次相同解释。

### 9.3 ⑫的质量升级

保留八类任务含义及多轮协作示例。调整“为满足格式必须制造事实”的部分：

- 有实际失败记录就展示真实失败，没有就提供**预设验证场景**，标明不是发生过的历史。
- 有两种值得比较的真实方案就比较两种；不必编造一个明显不合理的第三方案。
- 简单任务不硬拆六次实现；复杂任务保留自然的阶段与检查点。
- 提示词使用真实接口与依赖，保留输入、约束、验收和偏差信号；通用提示词方法放公共说明，当前批给本任务可直接使用的完整版本。
- “可复制”和“可编译”分开：缺少上下文的片段标适用位置与依赖；只有确实提供完整环境或已编译的示例才作相应承诺。
- 取消最低行数，改查：读者是否能提出任务、辨别错误产物、定位问题并决定下一步。

例如批次12约628—642行的“最小可编译实现”使用了`log`，片段内没有其定义。这至少需要明确前置条件或实际编译核验；不能因为有该标题就视为“可编译”。本次没有编译该片段，不将它写成已确认编译失败。

### 9.4 不把未知事实包装成教学深度

批次44的⑬明确记录只有`test-compile`、没有专属测试，但⑯约1648行仍出现“✅均指单测通过”的通用表述。这是值得由统一证据状态消除的具体矛盾。

新规则应自动约束：

- `compile_passed`不能渲染为`tests_passed`。
- 测试文件存在、测试设计存在、测试实际执行成功分别记录。
- 对“全部、唯一、零覆盖”等结论保留搜索范围与命令证据。
- 静态分析能确认项目调用什么，第三方实际行为仍需要版本匹配的来源；不因为符号存在就证明整句解释正确。

## 10. 门禁重构：减少机械返工，加强事实保障

### 10.1 三种判定职责

**第一类：事实与完整性硬门。**

源码来源、逐行匹配、反向覆盖、方法覆盖、行号、散文符号、引用、结构完整、占位符、快照适用性、证据等级。这些继续阻止发布不合格教材。新manifest要求已列入本批范围的完整源码100%由确定性注入覆盖；保留旧版99.5%判据用于旧批兼容，不用降低新批目标换速度。

**第二类：可自动构造的格式门。**

17节顺序、稳定节ID、合法围栏、表格、标签位置等由模板和渲染器构造，再检查构造结果。模型不负责记住“第几个井号”才能让正则命中。

**第三类：必须语义判断的教学质量。**

因果解释、关键失败与边界、调用顺序是否真实、示例能否照着用、前置概念是否足够、⑫是否可以指导开发。用带证据的审读，不用“字数越多越好”替代。

### 10.2 取消固定数量要求前的替代证据

| 原要求 | 新契约候选 | 回归保护 |
|---|---|---|
| ⑫至少180行 | 八类学习任务有具体内容，提示词可用，反馈与证据对应 | 同源码范围盲评+读者任务评分 |
| 至少三轮真实失败 | 实际失败按实记录；没有时给有价值的预设故障、定位和修复方案 | 注入伪造日志样本必须被拒 |
| 反例至少三对 | 覆盖该机制实际存在的主要不同失效类别 | 少一个关键异常/并发路径的样本必须被检出 |
| 调用链至少五跳 | 起点、终点、必要中间节点真实；异步/回调边有出处 | 无依据的填充节点必须被标记 |
| 任意类都写复杂扩展实现 | 有真实扩展点才讲扩展契约；其他说明正常使用与修改影响 | 不允许把接口实现、注册/装配步骤漏掉 |

不能只增加一个“语义评分”口号就删除旧门槛。先建立成对好坏样本，测新检查的误报和漏报，再发布新profile。

### 10.3 一份结构化检查结果

建议在现有检查器上抽取统一入口，而非继续扩张多个不同口径的扫描器：

```text
check_batch(document, manifest, source_view, contract)
    → checks[]
      {rule_id, status, checked_count, defect_locations, evidence}
    → summary
      {document_hash, source_digest, contract_version, applicable_books}
```

`PASS / FAIL / NOT_APPLICABLE / NOT_CHECKED`分开。适用但未查项不能参与“全通过”。输出JSON和终端摘要使用同一个结果，`sync_gate_result`消费结果而不再从展示文本猜数字。

保留最终复核：若同步质量表只影响受控的机器结果区，对正文hash和表格渲染单独校验；若改动了正文、规则版本、源码或块位置，则完整门禁重跑。不要仅凭“刚才跑过”复用失效结果。

### 10.4 旧判据与新判据隔离

采用显式的契约profile，例如：

- `book1-compatible`：保持现有17节和v2.30要求，先验证流程提速。
- `book1-optimized`：通过评估后的语义覆盖契约，取消部分固定数量要求。

实现时为新profile分配正式版本，升级SSOT、模板、检查器与自检，不直接把v2.30中的阈值改小。

旧教材继续按原版本核验，不追溯变红，不自动全库重写。修工具的正确性缺陷可以影响历史结果，但必须列出具体差异、原因和受影响项，不能为了维持原PASS而保留错误。

## 11. Flash模型与并行的合理用法

默认单主Agent即可先获得流程收益。机械操作全部交给脚本，Flash重点负责已准备好证据的讲解写作。

只有这些情形考虑更强推理或独立复核：

- 并发、事务、资源生命周期、跨文件控制流存在争议。
- 同一语义缺陷修复两轮仍未收敛。
- 源码与教材主张矛盾，需要查明，而不是继续补格式。

若运行平台支持并发，可在**同一冻结证据包**基础上并行处理：独立文件族的调查、测试证据检索、成稿语义审读。默认最多两个独立工作单元起步，先测端到端收益。

不建议同时让多个Agent写同一Markdown、覆盖矩阵或状态文件。最终组装只有一个写入者；每个并行结果都带来源、快照与未解决项。平台无并行能力时走同一串行流程。

并行不是免费加速：它增加重复上下文、结果冲突、限流排队和整合成本。不要把10个文件拆成10个Agent、每个都从头读6.9万字符入口。

本方案不绑定某个具体Flash型号，也不预设供应商缓存一定命中。是否启用并行、模型升级或API缓存，以可测量的平台能力与成本为准。

## 12. 需要修改哪些文件

以下路径均相对本仓库的`skills/replicate-learning/`，README等单独注明。

| 文件 | 具体变更 |
|---|---|
| `SKILL.md` | 默认只第一册；删旧暂停欠账规则；入口精简；统一阶段含义；明确生成教材与执行工程任务的边界 |
| `references/V2执行协议.md` | 变成兼容入口或更新为统一路由，避免与新协议并存两套强制流程 |
| `references/三本书与工程日志.md` | 三册独立启用；无请求不建索引/欠账；工程日志只服务实际工程任务 |
| `references/第一册执行协议.md`（新增） | 日常执行、增量读取、修复、验证及续传 |
| `references/第一册质量卡.md`（生成文件） | 从适用契约生成简短写作检查卡，不维护另一套人工规则 |
| `spec/00-质量契约.json` | 保留旧契约；增加版本/profile、规则适用范围与执行触发条件，取消数量门槛需显式新版本 |
| `spec/V2质量契约.json` | V2-02改为“启用册具有目录与索引”；三册能力存在不等于每次必须产出 |
| `spec/阶段工作流.md` | 一次性考古与逐批行为分开；第一册底层穿透不再与第二册运行阶段混名 |
| `spec/操作手册-闸门与工具.md` | 单批生成、局部修复、全库治理、skill发布四种命令路径分开 |
| `references/批次讲解全文模板.md` | 第一阶段保持17节；生成正确标签和表格；第二阶段按profile渲染语义契约 |
| `references/黄金样例-索引.md`及相关节选 | 固定版本的少量正例/反例；按语言、结构与风险选择，修掉重复或过期描述 |
| `references/能力账本模板.md`、两份`AGENTS.md-*模板.md` | 续传读取精简状态；二、三册未启用不记欠账；不用长历史替代当前事实 |
| `scripts/new_batch.py` | 开批就固定最终profile/版本，生成稳定章节ID与完整骨架 |
| `scripts/inject_source.py` | 复用现有保真逻辑；增稳定块ID、源hash、注释计划与manifest联动；支持可重复重组 |
| `scripts/callsite.py` | 支持多符号一次收集/复用索引；保持声明、使用、提及的区分 |
| `scripts/gate_lecture.py` | 抽出统一检查入口；增加结构化结果；源码读取缓存；明确未检查状态及快照失败 |
| `scripts/gate_all.py` | 调用同一检查入口；缓存按仓库+快照隔离；区分回归一致与内容通过，不仅返回一个模糊退出码 |
| `scripts/sync_gate_result.py` | 从带hash的JSON渲染结果，不二次解析展示文本；校验结果未过期 |
| `scripts/safe_edit.py` | 保留围栏/区间护栏；必要时补分片重组的原子替换验证，避免另起临时编辑器 |
| `scripts/skill_selfcheck.py`、`scripts/v2_selfcheck.py` | 新增模式行为、缓存失效、结果适用性、负向样本测试，不能只检查字符串和文件存在 |
| `scripts/prepare_batch.py`（新增候选） | 本批范围/源码hash/证据清单/样例选择；能复用现有函数就不重复实现 |
| `scripts/build_batch.py`（新增候选） | 薄编排入口，组装、注入、检查、局部续跑；不重写检查逻辑 |
| `scripts/publish_batch.py`（新增候选） | 更新覆盖、索引、状态及可恢复提交指针 |
| 仓库`README.md`、`docs/安装与执行边界.md`等实际说明 | 更正默认三册、旧版本徽章及工具依赖，说明新默认和旧项目兼容 |

`verify_blocks`、`scan_detail`等宿主工具：先盘点它们独有的保护能力，再将能力纳入统一检查器或随skill发布的适配器。不能只因包内缺失就把要求删掉，也不直接复制依赖某个项目路径的脚本充数。

新增脚本是最小职责划分的候选，不要求一开始全部实现。若现有脚本扩展即可满足，优先复用；不引入服务端、任务队列、数据库或复杂插件框架。

## 13. 实施顺序与每步验收

### 阶段0：冻结基线和采集耗时

1. 冻结skill版本、源码范围、现有优质批次；保存输入hash。
2. 记录一次完整“继续生成一批”的阶段耗时、读取量、输出量、模型调用和返工。
3. 选定同一份源码范围，防止通过少讲文件制造提速。

验收：能把总时长拆成读取、调查、写作、工具、修复和归档；无法取得供应商token时写未知，不伪造数字。

### 阶段1：模式与指令整理

默认只第一册；清除三册欠账逻辑；统一阶段命名；拆分长入口；按需样例；固定首轮判据。教材结构暂不改。

验收：普通“开始/继续”只产第一册；不问恢复二三册；第一册机制与测试内容仍存在；既有明确三册授权可延续。

### 阶段2：组装、局部修复与状态

打通本批manifest、分片、注释计划、源码注入、结构检查和状态生成。把宿主已有流水线中经过验证的能力收回包内。

验收：同输入重组结果稳定；正文不被重复生成；断点恢复不丢分片；最终门禁绑定终稿；所有源码与覆盖检查不弱于旧版。

### 阶段3：统一门禁与安全缓存

先解决结果口径，再缓存。用双仓库/双commit/工作树修改夹具证明缓存不会串用；将全量和增量检查结果作等价比较。

验收：相同输入判定一致；有修改必失效；适用未查不能PASS；`gate_all`和单批检查同口径。

### 阶段4：内容契约优化试点

对固定篇幅、固定失败轮次、重复说明做有限实验；保留17节和核心教学功能，逐项替换数量规则。

验收：质量盲评与读者任务不退步，事实错误不增加，同范围耗时确实下降。达不到就只发布前面几阶段，不勉强放宽契约。

### 阶段5：发布与旧项目迁移

更新skill文档、契约、模板、检查器、自检与安装说明。仅同步用户实际使用的安装位置；不沿用旧规则自动向四处仓库commit/push。

验收：权威目录与安装包文件清单/hash一致；旧教材不被改写；旧状态可读取；关闭新profile即可回退兼容行为。

## 14. 怎样验证“更快且同样好”

### 14.1 端到端对照设计

选择三类核心任务：

- 普通契约族：类似批次3，避免只在最复杂题上比较。
- 复杂流式/并发族：类似批次12或22，验证高风险机制不漏。
- 解析及外部依赖族：类似批次43或44，验证配置、模型与未实测边界。

再用Python/TypeScript等项目的小夹具覆盖注释前缀、源码块身份与多语言检查，防止只在Java上全绿。

至少对比三组：

| 组别 | 含义 | 用于回答 |
|---|---|---|
| A 原版skill | 固定旧版本、旧流程 | 真实基线是多少 |
| B 新流程+兼容契约 | 教材形态和要求尽量不变 | 去重复和减少返工能省多少 |
| C 新流程+优化契约 | 采用批准的语义覆盖规则 | 进一步压缩是否损伤教学 |

保持同模型/参数、源码快照、文件范围、权限和可用依赖。A组也明确要求只第一册，使它按旧版单册优先规则执行；不能拿A组三册与B组一册的耗时相除来算流程提速。默认模式变化另用行为用例验证。禁用参考成品偷渡：生成任务可用统一样例，但不能让某一组直接读被评分批次的现成答案。

先各跑一个任务做排错试验；正式评估每类至少3次，交错组别次序以减轻服务波动。分别记录冷启动和正常续传；小样本报告中位数、范围与每次原始结果，不把9次运行算成可靠P95。

### 14.2 必记的性能指标

```text
total_wall_ms
prepare_ms / investigate_ms / write_ms / validate_ms / repair_ms / publish_ms
model_calls / tool_calls / repair_rounds
input_tokens / output_tokens（平台能提供才记）
bytes_read / repeated_read_bytes / deterministic_inserted_bytes
cache_hits / cache_misses / invalidations
source_files_explained / critical_methods_explained
first_pass_gate / final_gate / semantic_defects
```

如果存在并行，阶段时间可能重叠，不能把各分支耗时直接相加当墙钟时长。用户等待审批的时间单独列出，同时报告总体验时长与主动执行时长。

### 14.3 质量评价

硬门先通过，再盲评。建议100分量表：

| 维度 | 分值 | 具体问题 |
|---|---:|---|
| 事实与证据 | 25 | 源码、版本、运行结果是否对应，是否有臆造事实 |
| 覆盖完整性 | 20 | 范围内文件与关键方法、异常路径是否遗漏 |
| 因果与机制 | 20 | 为什么这么写、失败时怎样、取舍是否说清 |
| 调用/扩展/复现 | 15 | 读者能否按例调用、定位并修改，示例前提是否明确 |
| 初学者可读性 | 10 | 前置概念、层次、自足性、回放是否足够 |
| AI工程协作 | 10 | ⑫能否指导真实任务、审查与反馈 |

放行条件建议：

- 源码保真、覆盖及必须执行的门禁全部符合适用契约；没有关键事实错误。
- 核心方法和关键失效路径覆盖不下降。
- 每个任务的事实、覆盖维度均不低于基线；总分允许的评审误差控制在2分内，明显可读性退化不能被总分掩盖。
- 高风险主张全部审读，其余按文件与章节分层抽查；评估阶段由不了解组别的独立评审加用户抽阅，不能让写作者只给自己打分。
- 用任务验证理解：定位一个故障、解释一次失败回放、补一个正确调用/扩展设计。评分重答案和依据，不以背出原文为标准。

可减少“语言华丽”分，不能用它抵消事实错误。当前用户满意的批次作为正向参照，但其中具体事实仍可被新证据纠正。

### 14.4 必须加入的负向回归

1. 教材源码改一行：保真门失败。
2. 删核心方法/整块源码：反向覆盖失败，不能0对象PASS。
3. 错行号、错版本、历史快照缺失：明确失败或不完整。
4. 围栏错配、表头缺分隔线、标题丢字：结构错误被抓住。
5. 正文引用不存在的方法：证据检查失败。
6. 有测试设计但没运行、只有编译却写单测通过：证据状态冲突。
7. 源码没变但调用方变了：调用点缓存失效。
8. 相同相对路径的两个仓库/两个commit：快照缓存不得串用。
9. HEAD不变但工作树修改/新增文件：缓存失效。
10. 关闭二三册后说“继续”：不创建扩展册，不产生欠账，不追问恢复。
11. 只开启第三册：不暗中开启第二册；指定一次实验不变成长期三册任务。
12. 用无关内容凑180行、用空话填八步：结构可以通过，但语义审读不通过。
13. 发布中途失败：教材、状态和覆盖可恢复，不出现假完成。
14. 多语言片段的注释标注改变语法：使用适配渲染或边表，不能为加注释破坏代码。

## 15. 性能目标、时间下限与投入取舍

可以用以下关系指导测量，而不是猜“Flash怎么还这么慢”：

```text
端到端时长 = 模型调用与服务等待的关键路径
           + 工具执行的关键路径
           + 修复与重新生成
           + 发布归档

必要写作时间下限 ≈ 必须新生成的token量 ÷ 实际输出速率
```

源码由脚本注入不会占同等数量的模型输出token，但模型仍需要读懂它；已经采用注入的批次，不能把注入收益再次算一遍。

建议目标分阶段设定，均属于**待测目标**：

- 新流程兼容版：同范围任务中位时长不超过原版60%，主要来自减少读取/编辑/修复重复。
- 优化契约版：在盲评不退步前提下，争取不超过原版40%。如果原基线为60分钟，分别对应36分钟与24分钟；不是承诺每批一定达到。
- 首轮机械门禁通过率目标80%以上；常规批修复不超过一轮。这是过程目标，不能用降低检查覆盖来达成。
- 规则和样例的重复读取量先争取下降一半；只统计输入重复，不声称源码调查也可减半。

如果实测显示大部分时间已经是必要长文的连续输出，而返工和重复读取很少，那么单靠流程优化不可能再快几倍。此时选择更高吞吐的运行方式、适度并行或经评估批准的去重；不能偷偷削减关键解释。

尤其不能把一个批次拆成四个更小批次，然后以“单批从60分钟变成15分钟”宣称4倍加速。应比较完成**同一组源码及同一知识范围**的总耗时、质量和成本。

## 16. 可直接用于后续实施的验收清单

- [ ] 默认只第一册，二三册必须显式开启，未启用不记欠账。
- [ ] 第一册仍包含机制、故障、测试证据与⑫工程教学。
- [ ] 同一条规则只有一个权威定义；模板、速查卡和检查器口径一致。
- [ ] 日常不再读取完整历史事故和多套完整样例。
- [ ] 新批首轮就是最终判据版本，不靠末尾升档才暴露缺项。
- [ ] 源码由工具注入；注释和解释保留；所有核心方法与失败路径覆盖。
- [ ] 分片、manifest和成品可重建；局部修复不重新写整篇。
- [ ] 普通批次不附带修旧批、全库清理或重复构建无关模块。
- [ ] 结果区分源码检查、编译、单测、集成、人工审读与用户验收。
- [ ] 缓存可解释且正确失效，跨仓库/跨SHA/脏工作树不串用。
- [ ] 所有强制工具随包提供或明确适配，缺工具不能伪报通过。
- [ ] 删除固定数量门槛经过新契约、负向回归与质量盲评。
- [ ] 端到端提速来自同范围比较，有原始计时证据，不用小批切分制造数字。
- [ ] 权威仓库、安装目录同步可核对；旧教材和源码保持完整。

## 附录A：重要证据定位

以下行号按本次调研版本记录，后续修改后请按节名或函数名定位。

| 证据 | 位置 |
|---|---|
| 三册默认及暂停欠账 | `SKILL.md:26、134、870`；`references/V2执行协议.md:25` |
| 默认目录和每批三册闭环 | `SKILL.md:32—60` |
| 必须读多份文件 | `SKILL.md:65—75` |
| Flash已有批量/复用策略 | `SKILL.md:140—149` |
| 阶段含义不一致 | `SKILL.md:208—216`与`references/V2执行协议.md:13—21` |
| 源码注入和注释叠加已经存在 | `SKILL.md:263—278`；`scripts/inject_source.py:13—35` |
| 逐行解释、要点表、回放 | `SKILL.md:288—315` |
| ⑫固定方案/步骤/失败轮数 | `SKILL.md:406—477` |
| 每节开批三读 | `SKILL.md:533—543` |
| 宿主工具、终检及现有流水线 | `SKILL.md:545—563` |
| 固定篇幅与排版门槛 | `SKILL.md:93—100、355—391` |
| 重复全源码读取 | `scripts/gate_lecture.py:484—560` |
| 快照加载及缓存键 | `scripts/gate_lecture.py:579—635`；`scripts/gate_all.py:47—52` |
| 全库报告退出码语义 | `scripts/gate_all.py:289—303` |
| sync再次运行检查并解析文本 | `scripts/sync_gate_result.py:56—75` |
| V2自检主要检查文件/字符串存在 | `scripts/v2_selfcheck.py:13—45` |
| 历史格式返工成本记载 | `spec/操作手册-闸门与工具.md:199—201`，其15—30分钟为历史记录，非本次测量 |

教材根路径：`D:\ragent-official\NOTES\教学讲解\第一册-项目源码与工程实现`。

- 阶段1批次3，约333—395行：逐行要点、边界、副作用、最小使用与接入的具体形态。
- 阶段2批次12，约3—162行、598—647行：首包与取消场景、跨模块测试、接口边界及教学合成示例。
- 阶段3批次38，约75—86行、795—845行：测试边界、ES实现的具体语义与调用方式。
- 阶段3批次44，约1230—1240行、1507—1538行、1648行：测试设计、编译证据、未实测项与自检措辞之间的核对点。
- `D:\ragent-official\NOTES\复刻状态-下一步.md`的当前状态与近期记录：已采用第一册优先、最近批次返工情况与状态摘要漂移。

## 附录B：建议给下一次实施任务的指令

> 按本方案实施replicate-learning保质提速。先完成默认第一册、规则按需加载、最终判据前置和组装/局部修复，保留现有17节及源码讲解深度。第二、三册须用户显式启用，未启用不记欠账。不要先删⑫、源码注释或关键方法解释，也不要为提速降低已有事实门禁。新增缓存必须覆盖跨仓库、跨commit及工作树修改的失效测试。每个阶段先跑对应回归，再用相同源码范围比较原版与新版的时长、返工和质量。固定篇幅/轮数等契约修改单独版本化，盲评通过才采用。只改本skill仓库与本次明确授权的安装目录，不修改ragent源码或重写现有教材，不自行向其他仓库同步或push。
