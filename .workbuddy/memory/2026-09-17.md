# 2026-09-17

## replicate-learning V2 覆盖发布到 GitHub zgozh/replicate-learning

- 把本地 V2 主线仓（16 commits）**强推覆盖** `git@github.com:zgozh/replicate-learning.git` 的 `master`：
  远端 33 文件（V1，HEAD `6abef75`，判据 2.14）→ 58 文件（V2，HEAD `ce34826`，判据 2.23）。两者**无共同祖先**，只能 force push。
- 安全兜底：推送前在远端建备份分支 **`v1-archive-20260917`**（= `6abef75`），旧版可随时回滚/对比。
  - 踩坑：Git Bash 下**分支名带 `/` 会静默创建失败**（`git branch archive/x origin/master` 退出 0 但 ref 不存在），
    改用无斜杠名 `v1-archive-20260917` 才成功。以后在这台机器上建分支尽量用平铺名。
- 内容范围（用户选择「排除聊天记录.txt」）：`聊天记录.txt` 从版本库移除并加入 `.gitignore`（本地文件保留）；
  `LICENSE`(Apache-2.0) 从旧仓取回补上；其余（README / 总方案.md / docs / gate_all.json / skills）全部发布。

## README 重写（15 节）

`README.md` 由 347 行旧稿重写成面向 GitHub 访客的完整文档：30 秒理解、与"源码讲解 Prompt"的差异对比表、
安装（三种 Agent 目录 + 自检命令）、快速开始、三本教材与独立工程日志、七阶段 A~G 表、十七节结构表、
闸门七项 + 四层治理（SSOT/血证/判据版本/自检）、12 个脚本表、仓库结构、证据等级、硬约束与不承诺、
**V1→V2 升级对照**、FAQ、文档索引。

## 修复：2.23 判据只落地了 gate，自检 FAIL

- 现象：`skill_selfcheck.py` 报 `SSOT 版本 2.22 ≠ gate GATE_VERSION 2.23`（提交 `47fe1d8` 只改了 gate）。
- 按 SKILL §6.6 三件套补齐：`spec/00-质量契约.json` version→2.23 并新增 **S23** 条目
  （注入源码里的 markdown 行不再被当成讲义结构：块内 H3 只对中文报警、件段边界只认正文 `## `，gate 锚点 `_prose_mask`）；
  `SKILL.md` §6.6 变更登记补 2.23 行（标注为补记、存量工单 0）。
- 复跑：`skill_selfcheck` **76 项 0 失败 PASS**、`v2_selfcheck` PASS。
- **教训：判据改动最容易只改 gate 就收工；发布/收尾前必须跑一次 `skill_selfcheck.py` 对账版本号。**

## 副本同步

源仓改动（SKILL.md + SSOT）已同步 ×3（`.workbuddy` / dsh-toolkit / `.agents`），`diff -rq` 均 0 差异。
dsh-toolkit 已 commit（`304765c`，顺带补上此前只拷文件未提交的 7 份漂移），**未 push**，待用户决定。
