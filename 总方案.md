# 新版项目深度学习与 AI 原生工程训练 Skill 总方案

版本：v0.1 方案稿
日期：2026-09-16
状态：方案已确认，V2 Skill 已完成首版骨架，进入参考案例试运行阶段

## 1. 文档目的

本文档为新的 Codex/Agent Skill 定义总体方向、目标能力、运行流程、产出物、用户参与边界、质量标准和分阶段建设计划。

本方案依据：

1. 当前目录的 `聊天记录.txt`；
2. 参考仓库 `D:\develop\workspace\replicate-learning`；
3. 参考仓库现有的 `replicate-learning` skill、模板、示例、质量契约和验证脚本。

本文档不是新版 `SKILL.md`，也不是单独执行引擎。它先回答三个问题：

- 新 skill 最终要解决什么问题；
- 使用它学习一个真实项目时，完整流程是什么；
- 这个 skill 完成后，用户最终能得到什么知识资产和工程能力。

方案确认后，已拆分为 `skills/replicate-learning/` 下的 `SKILL.md`、协议、模板、脚本、质量契约和样例。后续以真实案例试运行结果继续迭代，而不是继续扩展抽象方案。

## 2. 总体定位

### 2.1 一句话定位

> 给定一个陌生的真实代码库，AI 帮助学习者建立业务、架构、运行机制和全文件认知，并通过真实 Issue、AI 委派、代码审查、测试、调试和迁移训练，使学习者能够在企业级 Vibe Coding 环境中独立指挥 AI 完成开发任务。

### 2.2 它不是什么

新 skill 不应退化为以下任意一种：

- 只解释 API 的源码阅读 Prompt；
- 只生成漂亮 Markdown 的教材生产器；
- 只复制原项目代码的复刻器；
- 只训练提示词写法的 Prompt 教程；
- 只让 AI 自动完成、用户被动阅读的课程；
- 只统计“讲了多少文件”的进度工具；
- 只要求用户手写大量代码的编程训练营。

### 2.3 核心转变

旧版重点是：

```text
项目考古
  -> 业务建模
  -> 架构推导
  -> 全文件讲解
  -> 底层穿透
  -> 验证归档
```

新版升级为：

```text
项目考古
  -> 业务建模
  -> 架构与运行时建模
  -> 全文件源码掌握
  -> 预测
  -> 证据
  -> 实验
  -> 复刻
  -> 修改
  -> 故障定位
  -> 架构挑战
  -> 独立重构
  -> 新领域迁移
  -> AI 原生工程任务
  -> 能力评估与归档
```

关键原则：

> 每一个范围内的文件都要有覆盖记录；每一个核心机制都要有证据和验证；每一个重要设计都要经过质疑；每一个关键能力都要尝试复刻、修改或迁移；每一个开发任务都要训练学习者的判断，而不是只训练 AI 的执行。

## 3. 最终目标能力

完成一个项目的学习周期后，用户不应只得到“这个项目的知识”，还应具备以下可迁移能力。

### 3.1 陌生项目进入能力

- 快速识别项目入口、运行方式、模块边界和依赖关系；
- 从 README、配置、测试、Git 历史和源码中建立可信事实；
- 找到一个 Issue 可能影响的模块、类、方法、数据表和外部系统；
- 判断哪些信息必须给 Coding Agent，哪些信息会造成上下文噪声。

### 3.2 业务和架构判断能力

- 说清角色、场景、输入、状态、输出、异常和审计；
- 说清一条完整的端到端业务闭环；
- 解释架构选择解决了什么问题、付出了什么代价；
- 删除或替换一个模块后，系统和业务如何退化；
- 在不照抄原项目的情况下设计同类系统。

### 3.3 源码和运行机制能力

- 从入口追到关键出口；
- 解释控制流、数据流、状态变化和调用链；
- 解释框架、运行时、数据库、网络库或中间件替项目自动完成了什么；
- 通过源码、测试、日志、堆栈和最小实验确认结论；
- 区分源码事实、实测事实、历史事实、外部事实和推断。

### 3.4 Vibe Coding 工程能力

- 把模糊需求转成可执行 Issue；
- 先调查，再预测影响范围，再设计变更；
- 给 Coding Agent 提供准确、有限、可验证的上下文；
- 拆分任务并定义不变量、边界和验收标准；
- 审查 AI 的 diff、异常处理、并发、事务、安全、兼容性和测试；
- 识别 AI 的幻觉、过度重构、错误 API、假测试和伪实现；
- 根据反馈让 AI 迭代，而不是无条件接受第一次输出；
- 在陌生项目中完成一次真实交付。

### 3.5 独立迁移能力

- 不看原项目源码，设计同类机制；
- 替换存储、框架、协议或执行模型；
- 将项目中的抽象迁移到新的业务领域；
- 解释哪些设计是通用原则，哪些只是原项目上下文下的局部实现。

## 4. 产品形态：双轨制 + 四类产物

### 4.1 双轨制

```text
                         真实项目
                            |
              +-------------+-------------+
              |                           |
              v                           v
        知识掌握轨                 工程能力轨
              |                           |
    业务/架构/源码/机制          Issue/设计/委派/审查/调试
              |                           |
              +-------------+-------------+
                            |
                            v
                    独立 AI 工程能力
```

知识轨解决“项目怎么工作、为什么这样工作”。

工程轨解决“面对真实需求，如何让 AI 正确完成开发并由我验收”。

两条轨道必须在每个核心批次汇合，不能把 Vibe Coding 丢到最后写一章泛泛的提示词教程。

### 4.2 三本正式教材

#### 第一册：《项目源码与工程实现》

回答：

> 这个项目是什么、它解决什么业务、每个文件和关键代码如何工作？

内容包括：

- 项目全景；
- 业务闭环；
- 架构地图；
- 模块和文件覆盖矩阵；
- 文件、类、方法和配置的完整讲解；
- 调用链、数据流和状态变化；
- 框架与底层机制；
- 测试、部署、脚本和外围工程文件。

#### 第二册：《实验与机制验证》

回答：

> 为什么它这样工作？如果改变或删除它，会发生什么？

内容包括：

- Prediction Card；
- Evidence Card；
- Experiment Card；
- Benchmark；
- Fault Injection；
- 日志、堆栈和数据变化；
- 预测正确、错误或部分正确的复盘；
- 未实测边界和剩余风险。

#### 第三册：《架构挑战与独立重构》

回答：

> 如果不照抄原项目，我会怎样设计、替换和迁移这个系统？

内容包括：

- Architecture Challenge；
- 等价实现；
- Replacement；
- Modification；
- Reconstruction；
- Trade-off；
- Business Counterfactual；
- 新领域 Transfer；
- 原项目设计与用户设计的差异。

### 4.3 一个隐形但必须存在的工程档案

#### 《AI 原生软件工程实战日志》

这不是传统教材，而是用户的训练和求职资产，记录：

- 真实或模拟 Issue；
- 用户的初始判断；
- AI 的调查结果；
- 用户提供的上下文；
- AI 生成的变更；
- 用户发现的问题；
- 测试和调试过程；
- 最终验收结论；
- AI 失误案例；
- 能力等级变化。

三本书是项目知识资产，工程日志是用户能力证据。

## 5. 学习闭环

每个核心主题或批次采用如下闭环：

```text
业务问题
  -> 架构位置
  -> 源码证据
  -> 学习者预测
  -> AI 质疑
  -> 实验验证
  -> 机制解释
  -> 等价复刻
  -> 真实修改
  -> 故障注入
  -> Debug
  -> 架构挑战
  -> 独立重构
  -> 新领域迁移
  -> 能力记录
```

### 5.1 Prediction -> Evidence -> Experiment -> Correction

这是新版最重要的主动学习机制。

一个完整示例：

```text
问题：
为什么长任务不会让上下文无限增长？

用户预测：
可能由摘要中间件在每轮调用前压缩上下文。

AI 调查：
定位到 middleware、state、runtime 和测试文件。

源码证据：
确认摘要发生在工具调用结果写入后的特定阶段。

实验：
构造连续工具调用，观察消息数量、摘要字段和 token 变化。

结果：
预测正确一部分，但恢复流程另有独立的 checkpoint 机制。

修正：
上下文控制由摘要和 checkpoint 两个机制共同完成。
```

预测不是考试，不通过不代表失败。它的作用是暴露学习者的认知模型，避免“看答案后觉得自己懂了”。

### 5.2 L0-L5 能力层

旧版 L0-L4 保留并扩展：

| 层级 | 含义 |
|---|---|
| L0 | 能找到真实源码、依赖、配置、测试或实验依据 |
| L1 | 能解释机制具体如何运行 |
| L2 | 能写出不依赖当前框架的可读等价实现 |
| L3 | 能解释设计取舍、边界和具体失败时间线 |
| L4 | 能通过真实运行、故障注入、日志或堆栈验证 |
| L5 | 能脱离原项目进行设计、替换、重构和跨领域迁移 |

L0-L5 是能力深度，不是文件优先级，也不是允许少讲某类文件的降档规则。

## 6. 全文件覆盖与认知权重

### 6.1 全文件覆盖必须保留

范围内所有文件必须进入覆盖矩阵：

- 业务源码；
- 领域对象、DTO、VO、枚举、异常和常量；
- 配置、环境变量和注解；
- 测试、夹具和测试工具；
- 构建文件、锁文件、插件和脚本；
- SQL、迁移、容器、CI/CD 和部署文件；
- 前端页面、组件、状态、请求封装、样式和资源；
- Agent、MCP、命令行、初始化和运维工具；
- 自动生成文件、第三方目录和二进制资源。

自动生成文件、第三方目录或二进制资源可以不逐行讲，但必须记录用途、来源、引用者和排除理由。

### 6.2 覆盖不等于时间平均分配

增加“认知权重”字段，但不以此降低解释质量：

| 字段 | 作用 |
|---|---|
| Coverage | 是否进入范围，以及是否有教程或排除理由 |
| Cognitive Weight | S/A/B/C/D，表示对理解系统和迁移能力的影响 |
| Evidence Depth | 当前达到 L0-L5 哪一层 |
| Learning Time | 建议用户投入的时间 |
| Engineering Relevance | 对真实开发任务的相关性 |

认知权重只影响批次安排、复习频率、实验资源和挑战密度，不允许把低权重文件解释成一句空话。

### 6.3 完成度的新定义

项目完成度不得只写：

```text
已讲文件数 / 总文件数
```

必须同时记录：

```text
业务理解
架构理解
运行时理解
全文件覆盖
核心机制验证
AI 委派
Diff 审查
测试与 Debug
独立设计
跨领域迁移
```

文件覆盖是知识资产完成度；能力账本是训练完成度。两者不能互相替代。

## 7. Vibe Coding 训练主线

### 7.1 训练目标

Vibe Coding 在本方案中不是“让 AI 写代码更快”，而是：

> 人负责问题定义、上下文、边界、设计、判断和验收；AI 负责搜索、实现、测试劳动、重构劳动和反馈执行。

### 7.2 每个工程任务的八步协议

```text
1. Understand    理解需求
2. Investigate   调查代码和影响面
3. Predict       预测变更范围与风险
4. Plan          设计方案、约束和验收
5. Delegate      将任务交给 Coding Agent
6. Review        审查 diff 和实现逻辑
7. Test          执行分层验证
8. Accept        人做最终接受或拒绝
```

#### 1. Understand

用户必须能说清：

- 谁遇到什么问题；
- 当前行为是什么；
- 期望行为是什么；
- 什么不在范围内；
- 什么结果才算完成。

#### 2. Investigate

AI 先读代码和文档，不得直接修改。调查结果必须包含：

- 证据路径；
- 影响模块；
- 入口和出口；
- 相关测试；
- 可能遗漏的依赖；
- 未确认的推断。

#### 3. Predict

用户先预测：

- 哪些文件会改；
- 哪些文件不应改；
- 哪些行为可能回归；
- 哪些测试必须增加；
- 最大风险是什么。

#### 4. Plan

形成小而可验证的实施计划：

- 变更目标；
- 文件范围；
- 不变量；
- 兼容性约束；
- 异常和边界；
- 验收命令；
- 不做什么。

#### 5. Delegate

交给 AI 的任务必须包含：

- Issue 原文；
- 相关文档；
- 相关源码和测试；
- 业务约束；
- 技术约束；
- 变更范围；
- 验收标准；
- 禁止事项。

#### 6. Review

用户必须直接审查 diff，至少检查：

- 是否改多或改少；
- 是否破坏公共接口；
- 是否改变调用顺序；
- 是否遗漏状态、缓存、事务或权限；
- 是否新增不可重试的 retry；
- 是否引入并发、资源释放或数据一致性问题；
- 测试是否验证行为而非只验证实现表面。

#### 7. Test

按任务风险选择：

- happy path；
- 空值和边界；
- 重复请求；
- 并发；
- 超时；
- 依赖失败；
- 权限失败；
- LLM 或工具失败；
- 恢复与重试；
- 回归测试；
- 静态检查和构建。

#### 8. Accept

AI 不得自己宣布任务完成。最终状态由用户确认：

- Accept；
- Accept with known risk；
- Reject and revise；
- Blocked with evidence。

### 7.3 Context Engineering

新增“上下文预算挑战”，训练用户选择最小充分上下文。

典型任务：

```text
你只有有限上下文预算，需要从以下材料中选择：

Issue
架构文档
业务闭环
A.py
B.py
C.py
测试
数据库结构
README
Git 历史
```

系统评估：

- 是否覆盖真实依赖；
- 是否包含约束；
- 是否排除了噪声；
- 是否给了足够验收标准；
- 是否把未经验证的推断当成事实。

### 7.4 任务类型

Skill 应从真实项目中生成或抽取不同类型的训练任务：

- Feature；
- Bug Fix；
- Refactor；
- Performance；
- Security；
- Test；
- Migration；
- Integration；
- Incident/Debug；
- Documentation/Contract。

### 7.5 AI 失误训练

不把 AI 永远设定成正确执行者。工程日志维护 AI Failure Corpus：

| 案例 | 训练目标 |
|---|---|
| 只改一个文件，遗漏同步依赖 | 影响面分析 |
| 对非幂等操作盲目加 retry | 重试语义 |
| 添加缓存造成权限数据泄露 | 安全边界 |
| 测试只覆盖 happy path | 测试判断 |
| 调用不存在或签名错误的 API | 源码保真 |
| 过度重构无关模块 | 变更范围控制 |
| 用 mock 掩盖真实集成失败 | 证据质量 |

每个失误记录：

- AI 的错误输出；
- 为什么看起来合理；
- 实际错误；
- 源码或运行证据；
- 哪个测试能抓住；
- 用户以后如何提前识别。

### 7.6 Vibe Coding 能力等级

| 等级 | 能力 |
|---|---|
| V0 | 会向 AI 提出简单编码请求 |
| V1 | 能提供需求和基本上下文 |
| V2 | 能调查影响面、拆任务、定义验收 |
| V3 | 能审查 AI 代码并定位错误 |
| V4 | 能设计架构并指挥 AI 实现 |
| V5 | 能在陌生企业项目中完成端到端 AI 原生交付 |

面向实习目标，第一阶段至少以 V3 为目标，核心项目完成后争取达到 V4。

## 8. 用户与 AI 的分工

总体目标：AI 自动完成约 80% 至 90% 的检索、整理、写作、脚本执行和重复劳动；用户保留关键工程判断。

### 8.1 AI 自动完成

- 项目扫描；
- 文件分类；
- 入口、依赖和调用点定位；
- 初版业务与架构地图；
- 文件讲解和教材落盘；
- 代码快照和证据链接；
- 实验脚本草稿；
- 测试、静态检查和验证命令执行；
- 工程任务的影响面调查；
- AI 变更后的报告、索引和状态同步；
- 能力账本的客观证据汇总。

### 8.2 用户必须参与

用户参与压缩为五类，不要求每个文件都手写：

1. **认知确认**：确认或纠正 AI 对业务、架构和机制的关键判断；
2. **Prediction**：在关键机制和任务前先做预测；
3. **Architecture Challenge**：对核心抽象尝试自己的设计；
4. **核心 Coding/Review**：核心机制至少亲自完成一次小型实现或直接审查 AI 实现；
5. **最终验收**：用户决定 Accept、Reject、Known Risk 或 Blocked。

普通 DTO、简单配置、重复样板和低风险搬运工作不强制用户手写。

### 8.3 交互原则

- 交互是训练判断的工具，不是阻塞式闯关；
- 用户可以回答“暂时不知道”，AI 必须记录并继续调查；
- 用户没有时间回答时，AI 可以按默认策略推进，但必须标记为“未确认”；
- 只在路径、权限、写入位置、安全边界或不可逆操作无法推断时强制提问；
- 每次交互都必须产生可落盘的结论、预测、设计或验收记录。

## 9. 新 Skill 的阶段工作流

新版可以保留旧版 A-G 的严谨基础，但重新组织为四个阶段组。

### 阶段组 A：建立可信项目模型

#### A1 项目考古

扫描全仓库，建立：

- 系统全景地图；
- 全文件覆盖矩阵；
- 概念词典；
- 阶段对照与完整性地图；
- 教材总索引；
- 初始状态快照。

#### A2 业务建模

建立角色、场景、业务规则、状态、异常、审计、运营和反事实模型。

#### A3 架构与运行时建模

建立模块依赖图、请求链路图、异步链路图、数据模型图、部署拓扑和关键机制清单。

### 阶段组 B：形成项目知识教材

#### B1 全文件教材化

按业务闭环和技术机制分批，不按目录随机讲。

#### B2 底层穿透

使用 L0-L4 解释源码、框架行为、等价实现、失败时间线和实证。

#### B3 三本书同步落盘

一个批次至少同步更新：

- 第一册的源码/工程讲解；
- 第二册的预测/实验/验证；
- 第三册的挑战/重构/迁移素材。

不是等全部源码讲完后才开始训练。

### 阶段组 C：形成 AI 工程能力

#### C1 Issue 生成与接单

从真实项目历史、测试缺口、配置问题、故障场景和业务反事实中生成任务。

#### C2 Context Engineering

用户选择或编写任务上下文，AI 评估上下文质量并补充证据。

#### C3 AI 委派与变更

先调查和计划，再委派实现；禁止“直接让 AI 改代码”作为默认流程。

#### C4 Diff Review、测试与 Debug

用户审查变更，AI 执行测试和反馈修复，双方记录决策与证据。

#### C5 交付复盘

生成工程日志、AI 失误案例、规则沉淀和能力账本更新。

### 阶段组 D：脱离原项目验证迁移

#### D1 Architecture Challenge

不看源码设计同类机制，再和原项目对照。

#### D2 Replacement/Reconstruction

替换实现、删除原模块或从零重构一个最小但真实的版本。

#### D3 Transfer

迁移到新的业务领域，检查用户是否掌握了抽象而不是记住原项目。

#### D4 最终能力验收

模拟进入陌生企业项目，给出一个 Issue，要求用户完成：

```text
理解
调查
预测
方案
上下文
委派
审查
测试
调试
验收
复盘
```

## 10. 输出目录设计

建议新项目中的学习产物如下：

```text
NOTES/
  00-系统全景地图.md
  项目文件覆盖矩阵.md
  概念词典.md
  阶段对照-完整性地图.md
  能力账本.md
  AI失误样本库.md
  工程任务索引.md
  教学讲解/
    00-总索引.md
    第一册-项目源码与工程实现/
      阶段N-主题/
        00-阶段总览.md
        批次M-主题.md
    第二册-实验与机制验证/
      预测卡/
      实验卡/
      故障注入/
    第三册-架构挑战与独立重构/
      架构挑战/
      重构任务/
      迁移任务/
  AI原生工程实战日志/
    00-总索引.md
    Issue/
    Context/
    Review/
    Debug/
    Delivery/
```

### 10.1 状态文件

状态必须写入目标项目的 `AGENTS.md` 或项目约定的状态文件，至少包含：

- 当前阶段；
- 当前批次；
- 三本书已完成内容；
- 已讲文件数/总文件数；
- 已验证/未验证数量；
- 能力等级；
- 未完成任务；
- 最近验证命令和结果；
- 已知风险；
- 下一步入口。

聊天记录只作为临时交互，不是进度真源。

## 11. 质量与验收标准

### 11.1 知识覆盖验收

- 范围内文件全部有教材链接或排除理由；
- 每个核心业务闭环有端到端链路；
- 每个核心模块有架构位置、依赖方向和数据流；
- 关键源码讲解可回链到真实路径、类、方法和行；
- 事实、实测、历史、外部事实和推断分级清楚；
- 未实测内容不得写成已验证。

### 11.2 主动学习验收

- 核心机制有 Prediction Card；
- 关键结论有 Evidence Card；
- 可运行时有 Experiment Card；
- 预测结果有正确/错误/部分正确复盘；
- 核心机制至少有一个 L0-L3 完整穿透；
- 关键机制尽量达到 L4；
- 至少一个核心抽象达到 L5。

### 11.3 Vibe Coding 验收

- 至少包含真实需求或真实风格 Issue；
- 有调查证据和影响面；
- 用户先预测变更范围；
- 有明确任务上下文、约束和验收标准；
- AI 有实际变更或明确记录为何未变更；
- 用户审查 diff；
- 有测试、静态检查或运行反馈；
- 至少经历一次反馈迭代；
- 有最终 Accept/Reject 结论；
- 记录至少一个 AI 失误或主动反例。

### 11.4 能力验收

最终不能只输出“已讲完多少文件”，还必须回答：

- 用户能否解释核心系统；
- 用户能否定位一个新 Issue；
- 用户能否定义给 AI 的上下文；
- 用户能否识别 AI 的错误；
- 用户能否独立设计核心机制；
- 用户能否将机制迁移到新业务；
- 用户当前 Vibe Coding 等级是多少；
- 还缺哪些能力，下一步如何训练。

## 12. 参考仓库的继承与改造策略

### 12.1 直接继承

参考仓库中以下机制应尽量保留：

- 证据等级；
- 全文件覆盖矩阵；
- 磁盘状态续传；
- A-G 阶段工作流；
- 批次化教材；
- 逐文件和逐行源码保真；
- L0-L4 底层穿透；
- 闸门脚本和自检；
- 失败也落盘、不得虚报；
- 模板、示例和黄金样例。

### 12.2 需要改造

旧版中需要重点改造的部分：

1. **F Vibecoding**：从批次中的一个章节升级为贯穿整个流程的工程主线；
2. **完成度定义**：从文件覆盖扩展到能力账本；
3. **复刻定义**：从复制原实现扩展到复刻、修改、替换、重构和迁移；
4. **交互协议**：增加 Prediction、Architecture Challenge、Issue 接单和最终验收；
5. **输出结构**：从单一教材目录升级为三本书 + 工程日志；
6. **验证系统**：增加任务流程、上下文质量、diff 审查和能力证据；
7. **质量契约**：将新增机制纳入 SSOT、模板、闸门和回归用例。

### 12.3 不应直接照搬

- 旧版所有章节编号；
- 固定的教材节数；
- 固定的批次数量；
- 只适用于某一语言或项目的示例；
- 把“提示词写得完整”当作 Vibe Coding 能力的全部；
- 让 AI 自己判断任务是否完成。

## 13. 分阶段建设路线

### Phase 0：方案确认

产物：

- 本总方案；
- 名称和定位；
- 目标用户；
- 三本书和工程日志定义；
- 人机分工；
- 质量边界。

退出条件：用户确认方案主方向。

### Phase 1：V2 Skill 执行协议

产物：

- 新版 `SKILL.md`；
- 入口与意图路由；
- 阶段组和批次协议；
- 人机交互协议；
- 三本书输出协议；
- 工程日志协议。

重点：先让 AI 知道什么时候扫描、什么时候讲解、什么时候提问、什么时候要求预测、什么时候生成 Issue、什么时候验收。

### Phase 2：模板与规范

产物：

- Prediction Card；
- Evidence Card；
- Experiment Card；
- Architecture Challenge 模板；
- Issue 模板；
- Context Pack 模板；
- Diff Review 模板；
- Debug 记录模板；
- Capability Ledger；
- AI Failure Corpus；
- 三本书索引模板。

### Phase 3：脚本和自动化闸门

产物：

- 继承并改造旧版保真检查；
- 覆盖矩阵检查；
- 教材链接检查；
- 三本书交叉索引检查；
- Prediction/Experiment/Challenge 存在性检查；
- Vibe Coding 任务八步记录检查；
- 状态文件同步检查；
- 能力账本证据检查。

### Phase 4：黄金样例和反例集

产物：

- 一个完整核心机制样例；
- 一个完整 Issue 实战样例；
- 一个 AI 错误样例；
- 一个 Architecture Challenge 样例；
- 一个跨领域迁移样例；
- 小模型执行和上下文压缩样例。

### Phase 5：真实项目试运行

选择参考项目或新项目，完成至少一个完整批次：

```text
源码教材
  + 实验验证
  + 架构挑战
  + Vibe Coding Issue
  + Review/Test/Debug
  + 能力账本
```

根据试运行暴露的问题，再修改规范和脚本。

### Phase 6：求职导向训练模式

新增“陌生企业项目模拟”：

- 只给项目和 Issue，不提前给答案；
- 限制上下文预算；
- 要求用户先给调查和方案；
- 让 AI 执行并故意保留部分风险；
- 用户完成 diff review、测试和验收；
- 输出可展示的工程日志摘要。

目标是从“学会一个项目”转向“能进入新团队工作”。

## 14. 首个版本建议的最小范围

不建议第一版同时实现所有自动化能力。首个可用版本只需要保证以下闭环：

1. 扫描项目并建立四份基础地图；
2. 按批次生成第一册教材；
3. 对核心机制生成 Prediction Card 和 Experiment Card；
4. 生成一个真实风格 Issue；
5. 完成 Understand 到 Accept 八步流程；
6. 维护能力账本和工程日志；
7. 保留旧版源码保真和验证闸门；
8. 支持跨会话从磁盘续传。

以下内容可以延后：

- 自动评分用户回答；
- 多 Agent 并行协作；
- 自动生成复杂 benchmark；
- 全自动故障注入；
- 面向简历的图形化报告；
- 多项目横向能力比较。

## 15. 关键设计决策

### 决策一：保留全文件覆盖

原因：用户的核心诉求是吃透完整项目，外围配置、测试、脚本和部署文件不能被默认忽略。

### 决策二：增加认知权重，但不做深度降档

原因：全覆盖不能意味着所有文件占用相同时间，但低权重不能成为空话讲解的理由。

### 决策三：三本书保留，Vibe Coding 贯穿

原因：三本书分别承载“怎么做、为什么、我如何自己做”；工程能力不能被压缩到最后一章。

### 决策四：用户只参与关键判断

原因：用户要训练工程判断，而不是被迫手写所有样板代码或重复抄录。

### 决策五：最终完成度以能力为核心

原因：文件读完不等于可以接 Issue、审查 AI 和独立设计。

### 决策六：证据优先，未验证就明确标注

原因：源码学习和 AI Coding 中最危险的错误是把合理猜测写成事实。

## 16. 待评审问题

以下问题在进入实现前需要确认，但不影响本方案先作为总方向使用：

以上问题已在方案评审中确认：

1. Skill 继续沿用 `replicate-learning` 名称；
2. 三本书作为正式目录和正式产物；
3. 工程日志独立于被学习项目存放；
4. 不要求用户亲自手写核心生产代码，用户负责关键判断、审查和验收；
5. 首个试运行继续使用参考仓库中的既有案例；
6. 能力等级第一版作为证据账本记录，不在首版加入复杂自动评分。

## 17. 方案结论

新版 skill 的最终产品不是“更详细的源码讲解”，而是一套可持续运行的 AI 原生软件工程训练系统：

```text
陌生项目
  -> 建立可信模型
  -> 生成三本书
  -> 用预测和实验验证理解
  -> 用复刻和重构获得实现能力
  -> 用 Issue 训练 Vibe Coding
  -> 用 Review/Test/Debug 训练工程判断
  -> 用迁移确认抽象能力
  -> 用能力账本记录是否真的会
```

完成后，用户得到的不是一句“我看过 DeerFlow/Ragent”，而是：

> 我能进入一个陌生企业代码库，快速理解它；能把真实 Issue 拆成可验证任务；能给 Coding Agent 准确上下文；能审查它的实现并定位错误；能通过测试和调试完成交付；还能把学到的机制迁移到新的业务系统。

这应当作为新 skill 的总验收标准。
