# 血证档案（每条规则背后的真实事故）

> 用途：SKILL 正文只留规则本身，长事故叙事集中放这里。规则里出现 `（血证 Hn）` 时就查这张表。
> 这些不是故事，是**判据的来历**——改判据前先读对应条目，避免把用事故换来的约束当场放宽。

| 编号 | 事故（时间 / 项目） | 现象 | 代价 | 换来的规则 |
|---|---|---|---|---|
| **H1** | 2026-09-13 · agentic-rag 阶段8批次4 | 闸门报 **43 行不符**，逐行核对 `git show` 后确认 43 行**全部来自该批快照、0 行编造**（`ChatQueueLimiter` 被后续阶段从 141 行重写成 277 行） | 若无快照机制，会按"编造"去重写，**把对的代码改错**、毁掉分批次叙事 | F1 快照分类（演进不计 FAIL）、F2 批头声明、F3 历史版本块按快照核验；A1 补注格式同理（注释文本混进代码行会造成假不符） |
| **H2** | 2026-09-15 · 全量回扫 | A 组结构项**没人跑**：某批自指 17 处、某批围栏奇数 33 个存活；注释密度存在"只贴裸行号"形态 | 结构缺陷长期潜伏；"看起来有注释"≠有讲解 | S4~S8（结构内置进闸门）、D1~D3（密度与签名覆盖） |
| **H3** | 2026-09-15 · 全量回扫 | **21 个批次 / 465 处** `// :Lnn` 指错行，其中 **10 个批次保真度还是 100%** | 靠肉眼永远发现不了；读者按行号回查源码会找不到 | N1 行号一致性；行号一律由注入脚本生成，禁止手打 |
| **H4** | 2026-09-16 · 全量复查 | 61 份讲解 / 515 个逐件小节 / 1088 个 java 块里，**"调用现场 / 实现注册"内容为 0**——读者原话："类讲得非常详细，但我看了还是有点懵，不知道怎么使用" | 逐行注释回答的是"这行为什么这么写"，不回答"我要用它/扩展它该怎么做" | F4/U2/U3/U4/U5（⑤ 用法与接入四项 + 免检边界 + A6 五层结构） |
| **H5** | 2026-09-13 · 阶段13批次3 | A 组全绿，但 ⑥ 节**整章漏掉 571 行的★核心类**；原因是用了"取该类 `:L` 最大值"的粗糙检查 | 漏报"整节缺失"；只要类名在别处被引用过就显示"覆盖充足" | R1（逐行比对、并集口径、≥99.5%） |
| **H6** | 2026-09-13 · 阶段7批次2 | 同批**反向覆盖仅 12.4%、正向保真度仅 55.3%** | 只跑单向必漏一半问题 | 正反向必须成对跑 |
| **H7** | 2026-09-14 · 修批实战 | C 组只核对**代码块**，但批次其他节常引用旧实现：某批 ⑥ 修到 100%，节外仍写着旧方法名/旧字段 | 闸门全绿而正文自相矛盾 | 「⑥ 节之外的残留引用检查」必做清单（②⑦⑧⑩⑪⑯ 逐节人工过） |
| **H8** | 2026-09-14 · 样例文件 | 参照物自己带着**未闭合围栏**（9 个 = 奇数）与 45 处作废的 `// :N` 格式，存活两周 | 参照物不绿，抄它的人跟着错 | S2/S3；"新环境先对样例跑一次闸门" |
| **H9** | 2026-09-16 · 规则自查 | SKILL §6 还写着旧的"每批 1~12 项"清单（既无 17 节、也无用法接入）；某批次 ⑯ 自检脚本仍在用已作废的 `// :[0-9]` 格式 | **规则文档自己没人管**：两套节数并存，自检脚本查的是假口径 | S1 与 `skill_selfcheck.py`（A 档止血） |
| **H10** | 2026-09-16 | §6.5 的"可机械复核指标"被当成达标线，而它只是**节选样例自身**的规模（围栏 72、行号 340 都是那一份的量） | 批次大小本应浮动，被误当门槛 | §6.5 加⚠️标注；唯一达标线 = C 组七项判定值 |
| **H11** | 2026-09-15 / 09-16 · 两次判据变更 | 加快照机制后 9 批 77 行由"编造 FAIL"转为"源码演进"；加 ⑤ 后原全绿的阶段8批次4 转为 FAIL（21 件待补） | 没有版本号 → 查不出"这批按哪版判的"；没有工单 → 批次突然变红没人知道为什么 | V1 判据版本与变更规程（§6.6） |
| **H12** | 2026-09-16 · 技能同步核查 | 源仓库 5 个提交**只 commit 未 push**；dsh-toolkit 快照库的 `scripts/` **从未被跟踪**（闸门脚本一直不在快照里）；workbuddy 无 git 只能拷贝 | 副本与实际实现不一致，别人拿到的是缺脚本的技能 | V2 四处同步与 DoD；AGENTS.md 写明完成定义 |
| **H13** | 2026-09-16 · 阶段7批次3a 修复 | 批次3a 用的是**已作废的 `// :N` 格式**（143 处），而免检护栏 `L_ANNO` 只认 `// :Lnn`——意味着"在 `**【怎么用】**` 之下标旧格式行号"可以**既不核验、又假装是源码原文**；全库扫描发现旧格式存量 **36 个文件 / 7315 处**，其中"位于用法标记之下且只用旧格式"的块当时为 **0 个** | 当时无人踩到，但这是**结构性后门**：任何人把编造代码标成 `// :N` 就能免检 | U6（护栏认两种格式，2.5）；配套工具 `fix_lineno.py`（纠偏 + 格式升级一次完成） |
| **H14** | 2026-09-16 · 阶段7批次3a 修复（自我事故） | 用脚本把"围栏+内容"整段替换成纯内容时，**开闭栅栏被删掉/互换**：4.2 缺开栅栏、4.2 适配器块缺闭栅栏、4.3 整块无栅栏 → **④ 的 4.2~4.6 五节正文全被吞进一个代码块**。因为删的是"一对里的一个"、或把开闭互换，**围栏总数仍是偶数**，⓪ 只查奇偶 → 照样 PASS；被吞段落 lang 为空，①②③④ 也整体跳过 → **六组全绿却是坏的** | 读者一眼看出"第 4 节怎么在代码块里"；若交付，整节讲解在渲染后不可读 | S9 围栏配对体检（2.6，A/D/H3 三类，实测全库 0 处）；教训：**批量替换"围栏+内容"必须把围栏一并写回**，改完用 `fence_scan` 复检 |
| **H15** | 2026-09-16 · 三批结构对照（承接 H14） | 用户追问"为什么批次3a 没对齐批次1/2 的质量"。对照后发现三批的 **⑯ 自检脚本都只写 `grep -c '^```'` 数围栏个数**——**样板书自己教的防破坏方法，比闸门弱**（数个数判不出配对错位）。同时 SKILL §6.4 的"⑥ 五层结构"只规定**有哪五层、没规定顺序**，于是"⑤ 放在 `---` 前还是后"无规可依：批次1 没有 `---` 所以没暴露，批次3a 每件都有 `---`，9 个件的 ⑤ 全落到了分隔线后面，读起来像下一节 | 参照物有洞 → 抄它的人跟着有洞；规则缺一维 → 同一件事在不同文件里长出不同形态 | U7（⑤ 位置判据，2.7）+ A6 五层**定序**（顺序即结构）+ 样例 ⑯ 脚本改为"跑闸门一条命令"、应急 grep 版标注"只判奇偶、不够用"；`spec/操作手册` 的"修一批"新增两条工具纪律 |
| **H16** | 2026-09-16 · 用户追问"skill 到底怎么保证质量" | 盘点发现闸门 ⓪~⑤ **只管代码块**：批次1 的 ⑫ 整段描述了从未实现过的类与方法（`ListItemBlock` / `MarkdownVisitor` / `hasExplicitBullet` 在源码里各 **0 处**），六组却全绿——那 14 处是靠**人工过盲区清单**才揪出来的。另在阶段2 总览实测到一处真错：正文写 `` `UserContext.getLoginUser()` ``，而真方法是 `UserContext.get()` | 正文层面的"描述不存在的东西"**完全没有机械判据**；这类错误比代码贴错更隐蔽（读者不会去 grep 一个类名） | P1/P2（⑥ 散文符号真实性，2.8）：正文里的 `Xxx.java:NN` 引用 / 件标题声明的 `.java` / `本仓类.method(` 三类必须真实存在；**判据取舍有实测依据**——naive 版（所有反引号 CamelCase 符号）实测 371 处里约 95% 是合法提及（第三方库类型、`Tiny*/Mini*/Immutable*` 教学示例类、"尚未实现"的规划名），因此降级为**待确认清单** + `spec/散文符号白名单.txt` |
| **H17** | 2026-09-16 · 用户问"这些 py 文件是 skill 里配套的吗" | 一查发现：**执行规程的工具几乎都不在技能里**。① `spec/操作手册` §5 直接指向宿主项目的 `.tmp_usage/callsite.py`——该目录被 `.gitignore` 忽略、不入 git、随时可删；② §6.6 铁律 2「回归集判定不变」由 `.tmp_audit/gate_all.py` 执行，而它同样是临时脚本；③ `fix_lineno.py` / `gate_all.py` 分别摸 `gate_lecture` 内部 **19 / 16 个符号**（含私有 `_snap_file_cache`、`main()` 内才 `global` 的 `ROOT`），却完全不在 `skill_selfcheck.py` 的 21 项检查范围内；④ 同一件事已长出 6 组高仿版本（`callsite.py` / `callsite_batch.py` / `digest_callsite.py`、`blocks.py` / `blocks_61.py` / `apply_blocks.py`、`audit2~5.py` …），两个临时目录合计约 60 个 py | **规程写着"必须证明回归不变"，执行它的工具却可以被删 → 这一步退化成口头声明**；重写的脚本启发式各不相同 → 同一判据在不同批次被不同的尺子量；判据一改，那个**会写回 NOTES 行号**的工具可静默失准（写错就是真损失） | V3 工具随技能发布（不改判据、不动 `GATE_VERSION`）：`scripts/safe_edit.py`（H14 护栏：insert-only + 围栏事件序列比对，改讲解只用它）/ `callsite.py`（⑤ 调用现场证据来源）/ `fix_lineno.py`（④ 纠偏 + `// :N` 升级）/ `gate_all.py`（全库记分卡 + `--baseline` 回归比对）；各工具声明 `GATE_API` 清单，`skill_selfcheck.py` 新增"工具不留孤儿 / 依赖符号存在性 / safe_edit 护栏负向自测"三项 |
| **H18** | 2026-09-16 · 新脚手架首跑（承接 H17） | 写完 `scripts/new_batch.py`（从模板生成 17 节骨架）后拿它生成的**空骨架**去跑闸门，结果 **总判定 PASS ✅** ——因为 ⓪ 的占位判据只认 `TODO/FIXME/CONT-/<<<SRC:`，而模板用的是 `__一句话说清…__` 这种中文占位；①②③④ 在"零代码块"时天然通过，⑤ 又因模板自带 1 个示例件而 1/1。**"还没写"与"已写好"在闸门眼里没有区别**——这正是 H8/H14 那一类"看起来没坏"。同时暴露模板自身的两个结构缺陷：`### 索引节` 不降级导致节数只有 16；⑫ 的指导语含"本批/教材第/阶段N"抄进文件就让自指检查 FAIL（实测 6 处） | 骨架/草稿可以一路全绿地冒充达标批；模板缺陷会随每一份新批次复制下去 | S10（占位判据扩到**含中文的 `__占位__`**，2.9）：**先测误报率再收判据**——78 份讲解 + 2 份样例实测 **0 处命中**（仅模板自身 53 处），回归逐字段零变化；V4（新批次一律从骨架起写）；`new_batch.py` 顺带把模板的指导语剥离、把自指清零并**断言** ⑫ 段再无自指（模板将来新增自指词会让工具当场报错） |
| **H19** | 2026-09-16 · 整节重写「⑫ Vibecoding 视角」（承接 H14） | 要把 ⑫ 整节换成含更多代码块的新版本，`safe_edit --replace` **必然拒绝**——它的护栏要求"被替换区间"与"替换体"的围栏事件序列**逐一相等**（区间 :3866-4037 是 4 个事件，新版本是 30 个）。于是"改整节"在工具里**无路可走**，只剩"手写区间替换"一条路——而那正是 H14 的复发路径 | **护栏与它要保护的操作不匹配时，人不会停止改，只会绕过护栏**——过严的护栏等于没有护栏，且比没有护栏更危险：它让人以为"工具不让做 = 这事不该做"，然后悄悄用脚本做掉 | `safe_replace_section()` + CLI `--replace-section`（S1 区间两端必须在块外 / S2 区间自身围栏自洽 / S3 替换体与拼接后全文自洽且无 A/D/H3 三类问题）；`skill_selfcheck.py` ⑧ 组增 3 项自测（1 正向 2 负向），检查项 34 → **37**；操作手册 §3 补"怎么选 `--replace` / `--replace-section`" |
| **H20** | 2026-09-16 · 用户问"是不是全部讲解的这一节都没讲好"（承接 H16 / H17） | 把全库 **57 份有 ⑫ 的讲解**按 **18 项可判定实质**逐份体检：**只有 8 份 18 项全过**，另有 12 份只差 1~4 项，而 **21 份缺 13~18 项**。最普遍的系统性缺口：`沉淀≥5 条` 23%、`勘察翻车场景≥3` 33%、`三轮四段迭代` 37%、`方案落选论证` 40%、`勘察清单≥5 项` 42%、`不可触碰边界` 47%；**阶段14 四批的 ⑫ 只有 33 行时间线**（八条=0、八段=0、无审查清单）。度量本身也踩了坑：第一版量尺有 4 类误报（把 `- [ ]` 勾选清单判成"0 项审查"、把中式标签提示词判成"无骨架"、把对话式方案比较判成"无对比表"、把"改哪一行/你会看到什么"式迭代判成"无四段"），收窄后误报 0 | ⑫ 是**唯一能跨项目复用**的一节（其余各节都是本项目的知识），也是**唯一产生过"内容编造"级血证的一节（H16）**，可闸门对它长期只有容器检查——**"最重要的一节"与"最松的判据"并存**；度量工具本身必须先验误报率，否则会得出"全库都不合格"这种结论并据此改造 56 份文件 | S12（⑫ 内容深度，L2）：A 八类语义 / B 提示词骨架 / C 设计要点 / D ≥6 项审查清单。**实测 FAIL 7/57、逐份人工验伪误报 0**；且这 7 份**本来就因自指/八条/回链 ⓪ FAIL** → **判定变化 0**（唯一变化：阶段10批次3 因去掉 `八段≠8` 误报从 FAIL 转 PASS）；`八段=8` 降级为报告项；`vib_depth()` 的 13 个报告档维度进记分卡作存量工单 |
| **H21** | 2026-09-16 · ⑫ 改造中刷 ⑯ 判据版本（自我事故，承接 H14 / H19） | 用一个"整行替换"脚本把 ⑯ 里的 `判据 v2.9` 字样改成 v2.11：脚本**只按子串匹配就整行覆盖**，结果把 `## ⑯ 教材质量自检（七项闸门，逐条实测 · 判据 v2.9）` 这**一整行标题**换成了注释文字 → **节数 17 → 16** | 这是 H14 的同族错误（**用"区间/整行替换"做"改几个字"的事**）。所幸闸门 ⓪ 的"节数=17"当场拦下——**说明既有判据在这一类上是有效的，缺的是"动手前的提醒"** | `safe_edit.warn_section_heading()`：被替换区间含 `## ` 一级节标题时**先打警告**（不 ABORT——替换节标题可能是正当操作，但必须让人在动手前分清"改一行"与"换一节"）；教训固化：**改若干字优先用"只替换那一行"或 `insert_before`，不要用区间替换**（操作手册 §3 已写同类纪律） |
| **H22** | 2026-09-16 · 阶段8批次3 收口（③ 注释密度修不动） | 给 `StreamChatEventHandler` 的 6.7.F 块标注时发现：该块**只有 4 个关键行**，而判据要求 **5 条注释**（`need = max(5, key/12⌉)` 对 ★ 块无条件取 5），注释只能落在关键行上 → **怎么改都红**。全库量测：245 个 ③ 不达标块里 **27 个（11%）**属这种"数学上不可满足"，涉及 7 份文件（阶段11批次1 有 9 个、阶段11批次2 有 7 个…） | 判据本身不可满足时，人会以为"再多写点注释就好了"，实际是在**追一个永远追不上的目标**；这类缺陷还会污染存量统计——"③ 有 50 份不达标"这个数字里有一部分是判据的锅，不是文档的锅 | D2 修正（判据 2.12）：`need = min(key_n, max(5, ⌈key_n/12⌉))`——**以关键行数为上限**。全库 ③ 不达标块 245 → **223**，且只放宽不收紧（无块转红）；SSOT D2 / SKILL §6.1.1 同步改写 |
