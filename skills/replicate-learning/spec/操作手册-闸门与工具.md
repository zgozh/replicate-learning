# 操作手册：闸门与工具（从 SKILL §6.4「每批合规性自查」搬出的操作性内容）

> 规则条文在 SKILL §6.4 / 契约条目在 `spec/00-质量契约.json`；本文件只放**怎么敲命令、怎么修、怎么核**。
> 判定口径见 SKILL §6.4 的判定表；版本与变更规程见 SKILL §6.6。

## 1. 每批落盘后：一条命令

```bash
# 权威判定（六项一次跑完，退出码 0 才算过；快照自动读批头「源码依据：commit <sha>」）
python "<skill>/scripts/gate_lecture.py" "<批次.md>" --src <源码根目录> [--snapshot <commit>] [--json out.json]

#   ⓪ 结构：节数=17 / 围栏偶数**且配对**（块内不得再出现带语言标签的围栏、`### ` 标题不得落入块内）/ 占位=0 / ⑫ 八条=8、八段=8、自指=0、回链≥1、无薄条
#   ① 正向保真：每个 java 块逐行须在「当前树或本批快照」找到（只在快照命中 = 源码演进，不计 FAIL）
#   ② 反向完整：★类源文件每个有效行都要出现在讲解里（★ 在父标题同样计入；多块按并集）
#   ③ 注释密度：连段 <8 / 条数 ≥ 关键行÷12 / ★类方法签名有注释
#   ④ 行号一致性：`// :Lnn` 必须就是该内容真正所在的行
#   ⑤ 用法与接入：每件【怎么用】【上下游】；抽象件【怎么接】；批级 ⑦.5（≥3 步）；**位置**——【怎么用】必须排在「逐行要点表」之后、件内 `---` 之前
```

`--json` 输出可直接用于**判据回归对比**：改判据前存一份，改后逐字段比，"不该变的判定必须一字不变"。

## 2. 无 python 环境时的应急版（只查结构项）

```bash
F="批次文件.md"
echo "节数: $(grep -c '^## ' "$F")"                    # 17
echo "围栏数: $(grep -c '^```' "$F")"                  # 只判奇偶 —— **不够**，配对错位照样是偶数（血证 H14）
echo "⑫自指: $(awk '/^## ⑫ /,/^## ⑬ /' "$F" | grep -c '本批\|本阶段\|教材第\|答案卷\|阶段[0-9]\|批次[0-9]\|批[0-9]')"   # 0
echo "⑫八条: $(awk '/^## ⑫ /,/^## ⑬ /' "$F" | grep -c '^### [0-9]\.')"    # 8
echo "⑫八段: $(awk '/^## ⑫ /,/^## ⑬ /' "$F" | grep -c '^【任务】\|^【依赖】\|^【要新增的类】\|^【核心约束】\|^【注释要求】\|^【验收标准】\|^【禁止】\|^【输出格式】')"   # 8
echo "⑫回链: $(awk '/^## ⑫ /,/^## ⑬ /' "$F" | grep -c '^- 「')"            # ≥1
```

> **禁止用零散 grep 代替脚本**（血证 H5：用"取 :L 最大值"的粗糙写法漏报了整节缺失）。
> 应急版**只能证明"看起来没坏"**：上面的"围栏数"只数个数，**把一对围栏的开闭互换后它照样是偶数**（血证 H14 的真实事故）。
> 凡动过代码块的文件，**必须**在有 python 的环境跑一次正式闸门；没环境就人工确认"每个 ```` ```xxx ```` 都被一个裸 ```` ``` ```` 收掉"。

## 3. 修一批（而不是写一批）时的安全做法

1. **先分类再动手**（血证 H1）：闸门报的"不符"有四种，处置完全不同——
   | 分类 | 判据 | 处置 |
   |---|---|---|
   | 源码演进 | 行只在本批快照命中 | 标【历史版本示例】+ 补当前实现块；**禁止重写历史块** |
   | 教材注释格式 | 代码命中，只因行尾写了中文或旧补注 | 改成 `// :Lnn  ←教材：…`，**不是代码错** |
   | 缩写/改写/压行 | 多行压一行、去花括号、省略号、字段名被顺改 | 用注入工具**整块重注入** |
   | 编造 | 快照与当前树都没有 | 按源码重写，并回查同批其它块与节外引用 |
2. **只替换目标节**，写 `tmpnew` → 校验"节外内容逐字节不变" → `os.replace`。**禁止 `open(target,'w')` 写长内容**（历史事故：中断写出全 NUL）。
3. **改代码块只能"插入"，不能"整段替换"**（血证 H14）：用 `rep_range` 之类的区间替换时，若区间**跨了围栏**（开头是 ```` ```java ````、结尾是 ```` ``` ````），替换体**必须把围栏一并写回**；更稳的做法是改成"在锚点前插入"，完全不碰旧行。
4. **改完立即复检围栏配对**（不要等闸门跑完才看）：
   ```bash
   python -c "import sys;sys.path.insert(0,r'<skill>/scripts');import gate_lecture as G;L=open('<批次.md>',encoding='utf-8').read().split(chr(10));fb,op=G.fence_scan(L);print('配对问题',len(fb),'未闭合' if op else 'OK');[print('  ',x) for x in fb[:6]]"
   ```
5. 修完按血证 H7 逐节人工过一遍"⑥ 节之外的残留引用"（②⑦⑧⑩⑪⑯）。

## 4. 源码块注入

`python "<项目>/NOTES/工具-源码块注入.py" <讲解.md> <plan.json>`：从 `src` 指定文件逐行读真实代码、自动加 `// :L<真实行号>`，并自校验每行确实来自源文件（plan.json 的 `anchor/src/start/end/anno` 写法见脚本头注释）。

## 5. 调用点提取（补【怎么用】/【怎么接】时用）

```bash
python .tmp_usage/callsite.py <类型名>          # 单符号：声明 / 调用点（含所在方法）/ 仅提及 三类清单
python .tmp_usage/callsite_batch.py             # 批量（改 SYMS 列表）
```
**调用点一律以工具输出为准，不凭印象**；注册/装配机制回源码读那个装配类的构造器；查不到的写「本仓库尚未实现」，不许编。
