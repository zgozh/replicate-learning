# 黄金样例索引（canonical 样例的定义 + 各参照物形态状态）

> **为什么需要本文档**：样板会腐烂。血证 H26 的成因就是"模板里的一句话被逐批照抄"——旧形态的样例不标注状态，就会被当成"达标实物"抄进新批次。本文档把"哪份**可照抄**、哪份只是**深度基准**、哪份是**旧形态存档**"写死，并给出**机械判定规则**。
> 判据版本：v2.26（2026-09-18）。被 `SKILL.md` §6.5 / §10 引用；`skill_selfcheck.py` 的"技能内文件引用全部存在"会机械校验本文件存在。

## 1. canonical 样例：是**规则**，不是写死某一份文件

**定义**：canonical 样例 = **在 FAIL 档下通过闸门（退出码 0）的真实批次文件**，且同时满足三条：

1. 第 16 节（教材质量自检）写明「判据版本：v<当前 GATE_VERSION>」——写明即进 FAIL 档，**享受不了存量豁免**（判据 2.19 的适用范围规则）；
2. ⓪b 三项齐备：第 1 节架构图（围栏内 ≥5 行连接符/箭头 + ≥3 个能在仓库里找到的真实标识符）、第 7.1 小节编号调用链（≥5 跳、每跳带行号）、第 8 节穿透卡（L0–L3 四级标签 + 「省略了什么生产边界」）；**判据 v2.25 起另须 ⓪d 形态四项齐**：② 流水线图、⑤ 双清单、⑥ 每件问题开场+要点表锚+★回放、⑭ 完整答案率 100%；**判据 v2.26 起另须 ⓪e 结构密度五项齐**（`STYLE_FAIL_SINCE=2.26`）：⑦ 五小节齐全、⑫ ≥180 行、②④ 单段 ≤300 字、①/②/⑦ 围栏必标语言、⑩ ❌≥3 且代码块 ≥3 对（E6/E7 为报告档）；
3. 批头写明真实源码依据（`源码依据：commit <sha>`），代码块由 `inject_source.py` 注入（行号不可手打）。

**当前实例**（随批次滚动，见 §5 维护规则 1）：

| 项 | 值 |
|---|---|
| 文件 | `D:\ragent-official\NOTES\教学讲解\第一册-项目源码与工程实现\阶段1-启动装配与配置契约\批次1-启动装配与配置契约（RagentApplication★+主配置+pom+DDL+logback）.md` |
| 规模 | 1802 行 / 175,918 字节 / 17 节 / 76 个围栏 / 59 处 `// :Lnn` 行号标注 |
**v2.25 形态 canonical（2026-09-18 增补）**：ragent 项目批次38（关键词族）——**v2.25 FAIL 档 PASS**：⓪b 三项齐 + **⓪d 形态四项齐**（② 流水线图 18 箭头行、⑤ 双清单、⑥ 4 件问题开场+要点表+★回放、⑭ 12 题/12 完整答）+ 保真 100% + 行号漂移 0 + ⑯ 声明 v2.25。文件：`D:\ragent-official\NOTES\教学讲解\第一册-项目源码与工程实现\阶段3-问答业务域\批次38-关键词族（KeywordIndexService+KeywordRetrieverService+EsKeywordIndexService★+EsKeywordRetrieverService）.md`。

| 闸门实测 | `gate_lecture.py <该文件> --src D:\ragent-official` → **PASS（v2.21，FAIL 档）**：正向保真 100%（候选不符 0 行）｜★类 `RagentApplication` 覆盖 100%（有效行 14、缺失 0）｜注释密度 17 个代码块全过｜行号漂移 0 处｜逐件 5/5（【怎么用】【上下游】齐备）｜散文符号三类引用 0 处不成立 |
| 为什么它是 canonical | 它是**真实产物**（不是为写样例而写）：配置与 DDL 的三处契约、两道启动校验的职责边界、7 条反例、2 张 L0–L4 穿透卡，每个结论都指得到真实文件行号；且它**自己声明了判据版本**，受最严档约束 |

**canonical 按维度指定（v2.26 起 · 用户钦定与实测共同确定）**：

| 维度 | 参照批次 | 为什么 |
|---|---|---|
| **结构密度与排版**（⑦ 五小节 / ⑩ ❌✅ 对照块 / ③ 教学片段 / ④ 深潜 / ⑫ 厚度 / ②④ 段长 / 围栏标注） | **批次1/2/3**（阶段1 首批） | 用户钦定高水位；⓪e 全部判据的正向提取源。实物已收进 `references/结构密度样例.md` |
| **⑥ 九子块三段式**（问题开场 → 白话开场 → 构造方式与手法 → 整文件代码 → 逐行要点表 → 边界与副作用 → 【讲解】 → 【怎么用】 → 【上下游】 → 【怎么接】；★ 件全开、非★ 件去【讲解】【怎么接】） | **批次3**（阶段1 统一响应与异常族，1,850 行） | 用户 2026-09-18 钦定：⑥“先讲一些东西 → 代码讲解 → 再讲一堆东西”；闸门 ⓪c 双零分（0/39 缺白话开场、0/5 缺构造手法）。实物收进 `references/六节九子块样例（ragent批次3节选）.md`；机械核对用 scan_detail.py（宿主 deer-flow NOTES/_tools/） |
| **形态锚**（② 流水线图 / ⑤ 双清单 / ⑥ 问题开场+要点表+★回放 / ⑭ 完整答案） | **批次21/30/38** | 这些批在 ⓪d 维度成熟（批次38 为 v2.25 FAIL 档 PASS 样板） |
| **两者交叉的完整样板** | 批次30（⑦ 五小节 + ⑩ 12 块 + ③ 17 处 + ⑫ 235 行 + 形态四项均达标） | 阶段3 唯一结构维度保持的批次（反证"退化非必然"） |
| **v2.26 修复达标样板**（结构维度可照抄写法） | **批次37**（2026-09-18 修复后：⑦ 五小节齐 + ⑩ 四条 ❌✅ 五段式对照块 + ③ 3 处教学片段 + ④ 深潜 4.1/4.2/4.3 + ⑫ 185 行含三轮迭代 + ②④ 零超长段 + 喂栏全标 `text`） | 它是**从最差形态修到达标**的实物（修复前 ⑫ 114 行、⑩ 0 块、⑦ 只剩两小节、②④ 密排、裸围栏）——写新批时可对照"⑦.2/⑦.3/⑦.4 具体怎么长、⑩ 五段式怎么写" |
| **v2.26 修复达标样板**（结构维度可照抄写法） | **批次37**（2026-09-18 修复后：⑦ 五小节齐 + ⑩ 四条 ❌✅ 五段式对照块 + ③ 3 处教学片段 + ④ 深潜 4.1/4.2/4.3 + ⑫ 185 行含三轮迭代 + ②④ 零超长段 + 喂栏全标 `text`） | 它是**从最差形态修到达标**的实物（修复前 ⑫ 114 行、⑩ 0 块、⑦ 只剩两小节、②④ 密排、裸围栏）——写新批时可对照"⑦.2/⑦.3/⑦.4 具体怎么长、⑩ 五段式怎么写" |

**py 项目专条（v2.26 新增）**：结构密度与排版维度对照 **deer-flow 批次1/2/3**，实物参照 = `references/十七节黄金样例-py（deer-flow批次1-3节选）.md`（不含 Java 事实，可整节照抄形态）；Java 项目仍以批次1/2/3（ragent）+ `references/结构密度样例.md` 为准。

**怎么用**：写新批次对照**上表对应维度的批次**——结构密度维度读批次1（第 6 节步骤顺序、⑦ 五小节、⑩❌✅ 对、⑫ 单条厚度、排版六条），形态维度读批次38/节选文件；⚠️ **批次38 自己只有 ⑦.1+⑦.5、⑩ 0 块、⑫ 117 行**——所以**不能拿它当结构维度参照**（v2.26 前只指 21/38 是批次31/33/34 三处结构流失的直接原因之一）。

## 2. 参照物状态表（每份样例的形态 + **不可照抄清单**）

| 文件 | 定位 | 形态状态 | 可照抄 | **不可照抄** |
|---|---|---|---|---|
| `references/六节九子块样例（ragent批次3节选）.md`（**2026-09-18 新增**） | **⑥ 九子块三段式 + ②③④⑤ 排版实物** | 写 ⑥（尤其 ★ 件的【讲解】/【怎么接】）与 ②③④⑤ 内部表之前 | 代码与行号（属批次3 的 Java 源码事实）；是**节选**，不能当全批达标线（血证 H10） |
| `references/结构密度样例.md`（**v2.26 新增**） | **结构密度维度的实物参照**（⑦ 五小节 / ④ 深潜 / ⑫ 单条厚度 / ⑩ ❌✅ 对照块 / ③ 教学片段 / 排版六条） | **v2.26 形态**（取自批次1，2026-09-18 脚本化提取、未手抄；268 行） | ⑧ 第五节以上的**结构与排版**（⑦ 五小节的分工与层级、⑦.2 产出/消费表、⑦.3 状态机图+迁移表、⑦.4 不变式编号、⑩ 的五段式、⑫ 的开发叙事视角） | **内容与行号**（`RagentApplication`/`application.yaml`/`schema_pg.sql` 属批次1 的源码事实）；不能直接当"形态锚"用（②⑤⑥⑭ 看节选文件与批次38） |
| `references/十七节黄金样例-py（deer-flow批次1-3节选）.md`（**v2.26 新增 · py 版**） | **py 项目的结构/形态双维度实物参照**（② 分支表 / ③ 清单表 / ④ 三列宽表+深潜四段式 / ⑤ 六列穿透+九列骨架 / ⑥ 注释归属声明+件头四问 / ⑦ 五子节含状态机图与载体表 / ⑧ L0-L4 卡 / ⑨ TinyXxx 骨架 / ⑩ 现象→定位→根因→修复 / ⑫ 八条带（开发任务）（AI 协作过程）标注+诚实声明 / ⑭ 答：+钩回 / ⑯ 自检） | **v2.26 形态**（取自 deer-flow 批次1/2/3——该 py 项目黄金批：④深潜 2~3 / ⑦ 186~212 行 / ⑫ 305~323 行含 12 处标注 / 分节符 27~32 / 正文均行宽 46~47，`scan_form.py` 全量实测） | 同上各节的**形态与排版**（一行即一段、节间 `---`、代码块前引导句、围栏必标语言）；两版样例（Java/py）**互补速查表**在文件末尾 | **内容与行号**（批次1 的 `Makefile`/`serve.sh` 属 deer-flow 源码事实）；**不能替代 Java 样例**（Java 项目仍以 `十七节黄金样例-节选.md` + `结构密度样例.md` 为主参照） |
| `references/十七节黄金样例-节选.md` | ★类逐行讲法的**深度基准** + **形态锚**（②⑤⑥⑭） | **v2.25 形态**（节选，实测 13 节——②⑤⑭ 实物已于 2026-09-18 补齐，取自 ragent 批次21） | 第 6 节的逐行要点表写法、`←教材：` 的标注写法、【讲解】的论证颗粒度、②⑤⑭ 的形态 | **第 12 节（Vibecoding）的叙事视角**——它写的是"回顾性重构 + 本批…"，自 v2.16 起判 FAIL（回顾语 + 教材自指，实测该段自指 17 处、回顾语 1 处）；节标题措辞；**不能当"结构密度"依据**（⑦/⑩/③/⑫ 看 `结构密度样例.md`） |
| `examples/黄金样例.md` | 六段深度基准（机制类与零件类都满深度） | **旧六段形态**（实测 7 节，非 17 节） | 深度定性（"零件类也按满深度讲，只减份量不降结构"）、反例对比的写法 | **节序**（新批次必须 17 节）、第 12 节的写法、"六段对应"这套用途说明 |
| `examples/黄金样例-python.md` | 非 Java 对照（语言无关性） | 旧六段形态，**且无 java 代码块** | 语言无关性（同一套阈值换语言） | 节序与第 12 节写法；另注意它会被闸门当"非教材批"整体跳过（见 §3 盲区） |
| `examples/archive/V2工程任务样例.md`（已归档） | 工程任务（Issue / Diff 审查）结构样例 | 与批次形态无关 | 结构 | 任何事实（文首已声明"不代表任意项目的真实事实"） |

## 3. 盲区与已封堵项（随判据版本更新）

- **无 java 代码块就跳过结构判定** —— **已封堵（2.20）**：结构判定改看「是否教材类文件」（`is_lecture`：声明位豁免 / ≥5 行 java 块 / **教材标志投票 ≥3 项**）；合成无-java 样本已由「跳过」变为 **FAIL**。三份旧样例因首屏声明了「形态状态」而走**显式豁免**（不再是静默通过）。
- **记录类产物（整理批 / 进行中记录 / 草稿）既不按 17 节判、也不能没人管** —— **已立判据（2.21）**：改走记录类形态三项（R1 状态或日期 / R2 未完成与下一步清单 >=2 条 / R3 不冒充成品）；识别只认「文件名含整理批/进行中/草稿/WIP/记录/续传/日志」或「声明位写明记录类」（**不认裸的「进行中」**，否则阶段总览这类状态文档会被误判）；自声明判据版本 >= 2.21 才判 FAIL。
- **结构密度与排版没有任何判据**（⑦ 五小节 / ⑩ ❌✅ 对照块 / ③ 教学片段 / ⑫ 厚度 / ②④ 段长 / 围栏语言标注） —— **已立判据（2.26）**：`check_structure_density` 七项（E1-E5 FAIL 档 + E6/E7 报告档，`STYLE_FAIL_SINCE=2.26`）。实测断层：⑩/③ 在批次31、⑦ 在批次33、⑫ 在批次34、②④/围栏在 33-38；**批次30 为反证**（同阶段同注入管线，结构维度全保持）；另实测阶段2 批次11-15 亦缺 ⑦.4 与 ⑩ 对照块 → 存量工单扩展。**同族**：H23（② 静默跳过）/ H25（带圈数字丢失致整节检查静默失效）——**判据只在能识别出对象时才生效**。
- **节选类参照物的第 0 组与第 5 组本就 FAIL**（血证 H10）：对样例跑闸门的意义是「确认闸门能跑、且不误伤参照物」，**不是**拿节选当全批达标线。

## 4. 机械动作（动笔前 / 收尾后）

```bash
# 动笔前：用骨架起步（骨架自带 17 节 + 第 7.5 小节 + 第 12 节八条与八段 + 判据版本落笔）
python scripts/new_batch.py --stage <N> --batch <M> --title "…" --classes "…" --sha <sha> --out <路径> --src <仓库根>
# 收尾：一条命令定生死（本批已声明判据版本 → 按 ⓪b/⓪d/⓪e 三档 FAIL 判）
python scripts/gate_lecture.py <本批.md> --src <仓库根>
# 结构密度维度动笔前的对照物（⑦/⑩/③/④/⑫/排版）：
#   references/结构密度样例.md（批次1 实物）+ SKILL §6.1.3 六条排版标准
# 参照物体检（新环境首次使用、或判据升级后各跑一次）
python scripts/gate_lecture.py references/十七节黄金样例-节选.md --src <任一真实仓库根>
```

## 5. 维护规则（防样板腐烂）

1. **canonical 样例随批次滚动**：新批一旦在 FAIL 档下 PASS 且规模/覆盖面更完整，就把 §1 的实例替换为它，旧实例降为"历史 canonical"（保留路径与闸门数字）。
2. **判据每升一版，参照物必须复检**：新增或收紧判据后，把 §2 的三份参照物各跑一次闸门，把"形态状态"字段更新为新版本号；**一旦发现参照物在教旧写法，立刻补"不可照抄"条目**——这正是 H26 的教训。
3. **样例不得作为达标线的替代**：唯一达标线始终是闸门 C 组七项判定值（血证 H10）；样例只提供"深度与形态"的参照。
4. **黄金批按维度指定，不得泛指"最高质量批次"**（2026-09-18 血证）：结构密度维度 → 批次1/2/3，形态锚维度 → 批次21/30/38。**任何单一批次都可能在某一维度上缺项**（批次38 形态齐但结构缺），拿它当唯一参照会把缺项当水位抄下去——这正是批次31/33/34 三处结构流失的成因链。新增参照物时必须写明它覆盖哪个维度、不覆盖哪个维度。