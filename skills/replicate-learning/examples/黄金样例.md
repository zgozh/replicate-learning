# 讲解达标样例（黄金样例）——每类讲解的达标形态参照

> **用法**：开始任何一类的六段讲解之前，先读本文档，用样例深度校准自己的输出——而不是对着六段字段清单打勾。内容换到任何机制（幂等/SSE/雪花/熔断…）都一样：**只换内容，格式与深度保持**。
> **六段对应**：①业务场景 → 小节一；②数据流/调用链 → 小节二；③实现（代码块逐行）→ 小节三；④用法 → 小节四；架构概念与实现四问的知识点分布在逐行注释与反例对比中。
> **深度定性（重要）**：本文档是**满深度的达标基准**。从这一版起，**每一类（不管机制还是 DTO/配置/常量这类零件）都按满深度讲**——六段完整、逐行注释、反例对比、触发回放，差别只在**份量**（机制行多、零件行少），**不降深度、不缩结构**。所以本文档的深度对**所有类都适用**，不只是"重要机制类"。节奏靠**按「相关类聚合」分批**（数量不设死限但禁止离谱大批，详见 `../SKILL.md`「分批」）控制，不靠降深度省时间。
> **语言无关（重要）**：本例用 Java/Spring（`GlobalExceptionHandler`）演示，教的是**深度/格式/阈值**，**不是内容**。讲到别的项目时，先读该项目源码判断其语言与框架，再按这里的**结构与阈值**（六段、≥8跳调用链带真实类名、整方法逐行+每手法反例、用法带触发回放、结尾读法提示）产出**该语言/框架**的内容——不要照搬 Java 术语（`注解`/`切面`/`TypeHandler` 等在 Python/TS 里对应装饰器/中间件/序列化器）。
> **非 Java 对照样例**：同套六段 + 同阈值的 Python/LangGraph（`NodeRrf`·RRF）演示，见 **`黄金样例-python.md`**（节二/三的判定表与自检表与本文件一致，只换语言不换尺度）。

---

## 一、达标样例：GlobalExceptionHandler（异常翻译官）——完整示范

### 业务场景（具体到人、事、时）

小明的公司用这套系统管制度文档。今天他在「上传」页选了 **200MB** 的 PDF 点上传（yaml 配置单文件上限 50MB）。他期望系统给他一句人话，而不是浏览器里出现一堆英文报错。

### 数据流 · 完整调用链实例（从一次真实点击一路到前端收到结果）

```text
① 小明网页点上传（200MB 文件）
② 浏览器发出 POST /api/storage/upload（POST=HTTP 动词"提交数据"；Content-Type: multipart/form-data 标明请求体是多段表单，④ 步会解析）
③ Tomcat（真正监听端口、收发 HTTP 报文的服务器，Java 世界叫它"Servlet 容器"）把 HTTP 报文交给 Spring 的 DispatcherServlet（Spring MVC 的总入口，所有请求都先到它）
④ DispatcherServlet 解析参数时调用 MultipartResolver 解析 multipart（多段表单）请求
⑤ 解析过程中 Tomcat 底层检查：单文件 200MB > spring.servlet.multipart.max-file-size(50MB)
   → Tomcat 抛 FileSizeLimitExceededException（"单个文件超限"的原生异常，在 org.apache.tomcat 包）
⑥ MultipartResolver 把它包装成 Spring 统一形态：MaxUploadSizeExceededException（spring-web 包）
   [包装链现形：MaxUploadSizeExceededException → cause IllegalStateException → cause FileSizeLimitExceededException]
⑦ 控制器方法还没开始执行异常已发生 → 异常抛回 DispatcherServlet
⑧ DispatcherServlet 把异常按顺序交给 HandlerExceptionResolver 链
   → 命中 ExceptionHandlerExceptionResolver（它专门收集所有 @RestControllerAdvice 类里的 @ExceptionHandler 方法；
     @RestControllerAdvice 读作"全局异常处理类"标签：贴在某个类上，声明"这个类里的方法负责翻译全项目的异常"）
⑨ 按异常类型匹配：MaxUploadSizeExceededException.class 精确命中 GHE.maxUploadSizeExceededException
   （GHE = GlobalExceptionHandler 的简称，下同）
⑩ 方法内判 cause 链（见下方代码逐行）→ 判定"单文件超限"分支
⑪ 拼出人话 message = "上传文件大小超过限制，单个文件最大允许 50MB"
⑫ return Results.failure(BaseErrorCode.CLIENT_ERROR.code(), message)
   → 一个 Result{code:"A000001", message:"...", data:null, requestId:null}（A=用户端问题）
⑬ DispatcherServlet 把返回值交给 Jackson 序列化 → HTTP 200，响应体就是那段 JSON
   （Jackson = Spring 自带的 JSON 库，负责 Java 对象 ↔ JSON 文本互转；"序列化"= 把内存里的对象转成能在线路上传的文本）
⑭ 小明前端拿到 {"code":"A000001","message":"单个文件最大允许 50MB",...}，
   按 code 首字母 A 判断"是我操作的问题"，把 message 原样弹窗给他看
```

**读法**：断在 ⑤~⑨ 中间任意一跳 = 你找到了还没懂的点（比如"MultipartResolver 是什么、包装链哪来的"），当场问。

### 实现 · 每个手法配代码逐行讲

**手法① cause 分叉（有根因记全栈 / 无根因截 5 帧），abstractException 方法完整代码逐行**：

```java
@ExceptionHandler(value = {AbstractException.class})             // 标签：这个异常类型交给本方法翻译
public Result<Void> abstractException(HttpServletRequest request, AbstractException ex) {
    // HttpServletRequest = 框架递进本方法的"这次 HTTP 请求"对象：里面装着请求方法、URL 等信息（下面打日志要用）
    if (ex.getCause() != null) {                                 // 【有根因】业务异常底下还包着真正的原始异常？
        log.error("[{}] {} [ex] {}", request.getMethod(),        //   是 → 日志打完整堆栈（含 ex 和它的 cause）。
                request.getRequestURL().toString(), ex, ex.getCause());   //   日志串里的 {} 是填空槽：日志框架把后面的参数按顺序填进各槽（免手工拼字符串）
        return Results.failure(ex);                              //   返回业务异常对应的 Result（码/消息在异常里带着）
    }
    StringBuilder stackTraceBuilder = new StringBuilder();       // 【无根因】业务直接 new 出来的（没有底层根因）
    stackTraceBuilder.append(ex.getClass().getName())            //   手工拼日志文本：先写"类名: 消息"
            .append(": ").append(ex.getErrorMessage()).append("\n");
    StackTraceElement[] stackTrace = ex.getStackTrace();         //   拿到堆栈帧数组（Java 每次异常都带）
    for (int i = 0; i < Math.min(5, stackTrace.length); i++) {   //   只取前 5 帧——
        //   为什么是 5：Business new 出来的异常 50 帧里，只有前几帧是"谁调用它"的真实链路，
        //   剩下的都是创建它的框架代码，全打 = 日志噪音（每个异常 50 行）
        stackTraceBuilder.append("\tat ").append(stackTrace[i]).append("\n");  // 按 Java 栈写法 \tat 逐帧拼
    }
    log.error("[{}] {} [ex] {} \n\n{}", request.getMethod(), request.getRequestURL().toString(), ex, stackTraceBuilder);
    return Results.failure(ex);                                  //   两种分支返回同一个 Result（外层表现不变）
}
```

**手法② cause 链判上传类型，maxUploadSizeExceededException 逐行**：

```java
@ExceptionHandler(value = MaxUploadSizeExceededException.class)  // 拦截 Spring 统一的上传超限异常
public Result<Void> maxUploadSizeExceededException(HttpServletRequest request, MaxUploadSizeExceededException ex) {
    log.warn("[{}] {} [upload] 文件上传大小超限: {}", request.getMethod(), getUrl(request), ex.getMessage());  // warn 级日志+[upload]标记；{} 填空槽同手法①
    String message;
    if (ex.getCause() instanceof IllegalStateException           // 第一判：第二层是不是 IllegalStateException？
            && ex.getCause().getCause() instanceof FileSizeLimitExceededException) {  // 第二判：第三层是不是 Tomcat 的"单文件超限"异常？
        message = "上传文件大小超过限制，单个文件最大允许 " + maxFileSize;    // 两层都中 → 单文件超限 → 提示配置的单文件限额
    } else {
        message = "上传请求大小超过限制，单次请求最大允许 " + maxRequestSize;  // 否则 → 整请求超限（多个小文件合计超 100MB）
    }
    // maxFileSize / maxRequestSize 是配置类字段，由 @Value 注入：启动时 Spring 自动把 application.yaml 里配的数值填进来
    // （"注入"= 框架替你把值塞进字段，不用自己读配置文件）——所以提示语永远和 yaml 配置同源，改配置即改提示
    return Results.failure(BaseErrorCode.CLIENT_ERROR.code(), message);  // A 类错误（用户端）+ 人话提示
}
```

### 用法 · 可照写的最小完整示例

业务侧（假设 rag 模块有个提问控制器，你照抄改改就能跑）：

```java
@RestController                                             // 标签：这个类负责接 HTTP 请求、返回 JSON 数据（REST 接口类）
@RequestMapping("/api/ragent/chat")                         // 标签：本类所有接口的 URL 前缀
public class ChatController {

    @PostMapping("/send")                                   // 标签：POST /api/ragent/chat/send 这个请求落到这个方法
    public Result<Void> send(@RequestParam @ChatQuestion String question) {   // @RequestParam=把请求参数 question 的值绑到这个形参上
        // 注意：@ChatQuestion 闸门已在参数解析时挡掉空/超长问题，到这儿的都过了校验
        if ("某某违禁词".equals(question)) {
            throw new ClientException("包含违禁词，请修改后重试");   // ★ 业务侧唯一动作：抛！ClientException=业务异常子类（GHE 会把它翻译成 A 类错误信封），别的都不用管
        }
        return Results.success();                                    // 成功侧：调工厂拿空信封
    }
}
```

小明在前端问了一句违禁词，发生了什么：

```text
ChatController.send → 判中违禁词 → throw new ClientException("包含违禁词，请修改后重试")
→ 异常传出控制器 → DispatcherServlet → HandlerExceptionResolver → GHE.abstractException（无 cause 分支：截 5 帧日志）
→ Results.failure(ex) → JSON: {"code":"A000001","message":"包含违禁词，请修改后重试","data":null,"requestId":null}
→ 前端 popup(message)
```

看完这个示例你会注意到一件事：**业务代码里从头到尾看不到 Result 的失败拼装——抛异常就完事，翻译 100% 是 GHE 的活。** 这比十句"业务只抛异常"都直观，这就是实例化标准的用意。

---

## 一·续、注释深度对照（真实踩例：用词悬空 vs 解释到底）

> 出处：一次真实实测——拦截器讲解里写了 `// ② 预检放行`，初学者读完仍不知道 OPTIONS 是什么、"预检"是啥流程。**只讲"为什么"不讲"这是什么" = 没注释；用术语解释术语 = 悬空。** 下面用同一处代码做正反对照。

**例①：OPTIONS 放行（跨域预检）**

```java
// ❌ 悬空注释（只答了"为什么"，没答"这是什么"——"预检""跨域""OPTIONS"三个词全是没讲过的词）
if ("OPTIONS".equalsIgnoreCase(request.getMethod())) {
    return; // 预检请求直接放行：浏览器跨域前先发 OPTIONS，不该被登录检查判死
}

// ✅ 解释到底（先讲这是什么、再讲为什么；每个词都落在已知地基）
if ("OPTIONS".equalsIgnoreCase(request.getMethod())) {
    // getMethod() = 读这次请求用的 HTTP 方法（HTTP 方法 = 请求的"动词"：GET=要数据、POST=交数据；
    // OPTIONS = 另一个动词，意思是"先问问你允不允许我发请求"，探测用、不带业务数据）
    return; // 浏览器在"跨域"场景（网页在 a 站、接口在 b 站）真正发请求前，会先自动发一个 OPTIONS 试探；
            // 这趟试探不带登录凭证，若不跳过登录检查，试探会被当成"未登录"拦掉 → 跨域直接失败
}
```

**例②：ASYNC 分发跳过**

```java
// ❌ 悬空注释（"异步派发"本身就是需要解释的词，等于用生词解释生词）
if (request.getDispatcherType() == DispatcherType.ASYNC) {
    return; // 异步派发放行
}

// ✅ 解释到底
// DispatcherType = Servlet 容器（Tomcat 这类"真正接请求的服务器"）给每次流进过滤器的请求打的"来路标签"：
// REQUEST=浏览器原始请求；FORWARD=服务器内部转发；ERROR=进错误页；ASYNC=一次异步任务的开始/收尾被容器重新路由进来
if (request.getDispatcherType() == DispatcherType.ASYNC) {
    return; // SSE 长连接结束时，容器会以 ASYNC 方式把请求"再送进来"走一遍收尾；那时登录上下文已丢，
            // 不跳过就会把正常收尾误判成"未登录"拦掉，流式响应没法正常结束
}
```

**判定规则**：解释里每个词，要么是生活语言/语言基础，要么前文已解释过；有一个悬空 = 没解释，继续拆。底层机制首次出现拆到底，同批内再出现可简写「见先修概念」。

---

## 一·续二、通俗度对照（真实踩例：批次3 滑向文档体）

> 出处：批次2（课堂上当场讲）通俗清晰、不失专业；批次3（节奏失控后"对着源码补写"）读起来死板、不够初学者友好。根因**不是没讲全**（知识点一个不少），而是**掉了"教学姿态"**——把"给新手当面讲"写成了"整理档案"。对照如下。

**例①：业务场景 抽象主体 vs 具象镜头**

❌ 抽象（没有具体的人、没有数值，读起来像需求文档）：
"审计行要标这条变更发生在哪个业务域，比如是改了知识库还是改了示例问题。"

✅ 具象（有身份的人 + 具体数值 + 镜头感）：
"运营小李在后台把一条示例问题的答案从'退货流程是 A→B'改成'A→B→C'，点保存。事后他想知道'上个月这条是谁改的、改前长什么样'——审计写侧就是替这一刻自动留痕。"

**例②：先修概念 纯定义 vs 生活化类比**

❌ 纯定义（读完还是抽象、记不住）：
"AOP（切面）= 不侵入业务代码，在方法前后自动插一段逻辑的技术。"

✅ 类比（有记忆钩子、一眼懂）：
"AOP（切面）= 给每个业务方法门口挂一道'门帘'：请求进出前后，门帘自动做登记（记审计），业务方法本身一行不用改。"

**判定规则**：先修概念的每个词、业务场景的每个类，都要落到"具体的人 / 具体的事 / 生活里的物"；只要还在"术语↔术语"之间打转，就是文档体，回炉改成"给新手当面讲"的口吻。

---

## 一·续三、底层穿透对照（真实踩例：只讲 API = 培养 API 调用工程师）

> 出处：阶段4 讲完，用户反馈「这些东西本身就是框架做的、工具是外部的、全部层层包装，实际就是一堆 API 调来调去；**讲解如果也是一堆 API 讲来讲去，那只是 API 调用工程师，算不上真正的开发工程师**」。
> 根因：**讲解停在了 L0 用法层**（"这行调 X 的 Y 方法做 Z"）。这类项目的代码 80% 是框架/SDK 调用，只在 L0 讲，读者学到的是**调用姿势**，换个框架全部作废；**机制 + 手写等价 + 取舍**才是能迁移的工程能力。同一个知识点，两种讲法对照如下。

**例①：MyBatis-Plus 的条件构造器 `.eq(condition, 列, 值)`**

❌ 只讲用法（L0）：
```java
// ❌ 这行调 eq 拼等值条件；第一个参数是布尔开关，true 才拼
.eq(StringUtils.hasText(requestParam.getBizType()), BizChangeLogDO::getBizType, requestParam.getBizType())
```

✅ 穿透三层（L1 机制 → L2 手写 → L3 取舍）：
```java
// ✅ L1 机制：LambdaQueryWrapper 内部维护一个「SQL 片段列表」，每调一个条件方法，
//    它就把「列名 + 运算符 + 占位符」做成一小段存进去（列名是从方法引用 BizChangeLogDO::getBizType
//    反解出来的——框架读这个方法名，转成字段名 biz_type）；布尔开关为 false 时，它**整个方法直接原样返回、
//    不往列表里加东西**。最后执行时把列表用 AND 串起来，参数单独放进一个数组交给 JDBC 填占位符。
//    L2 手写：不用它，等价实现是——
//      List<String> parts = new ArrayList<>(); List<Object> args = new ArrayList<>();
//      if (StringUtils.hasText(bizType)) { parts.add("biz_type = ?"); args.add(bizType); }
//      String sql = "SELECT * FROM t_biz_change_log" + (parts.isEmpty() ? "" : " WHERE " + String.join(" AND ", parts));
//      // ↑ 你自己写，本质上就是「攒片段 + 攒参数」两件事，框架替你攒、还替你防注入
//    L3 取舍：为什么用方法引用而不是字符串列名？——因为字段改名时，字符串列名编译能过、运行才炸，
//      方法引用编译期就报错。什么时候不该用它？——多表 JOIN / 复杂子查询时，构造器写出来比 XML 还绕，该换 XML。
.eq(StringUtils.hasText(requestParam.getBizType()), BizChangeLogDO::getBizType, requestParam.getBizType())
```

**例②：`@LogRecord` 注解「加一行就记审计」**

❌ 只讲用法（L0）："在方法上贴 `@LogRecord(...)`，SDK 就会自动帮你把变更写进审计表。"

✅ 穿透三层：
> **L1 机制（它靠什么做到的）**：SDK 在 Spring 里注册了一个**环绕切面**（就是给贴了注解的方法门口挂道门帘）。方法执行前，切面先解析注解里的 SpEL 表达式（模板里那些 `{{#key}}` 填空）把要记的话拼出来；方法执行后，再把结果交给**你实现的一个回调接口**（`ILogRecordService`）——**"记什么"你用注解声明，"什么时候记"是切面的活，"记到哪"是你的回调**。业务方法全程一行没改。
> **为什么还需要 `LogRecordContext`（ThreadLocal 寄存柜）**：切面的方法签名是框架定死的，它**拿不到你方法里的局部变量**。所以 SDK 开了个"寄存柜"（ThreadLocal，同一线程内随存随取），你把要记的变量存进去，切面从柜子里取。这也是为什么它必须放在 `try/finally` 里清干净——线程是复用的，不清，下一个请求会取到上一个人存的东西（串号）。
> **L2 手写（不用 SDK，你自己怎么做）**：自定义一个注解 + 一个 `@Around` 切面（`ProceedingJoinPoint.proceed()` 前后各取一次快照、比较、insert），约 30 行就能跑。
> **L3 反例推演（不用它会怎样 / 它替你省了什么）**：不用它，每个业务方法里都得手写"取改前值 → 取改后值 → 比较 → 拼描述 → insert 审计表"五件事，10 个方法就是 10 份重复代码；某天审计表要加一列，**要改 10 处、漏一处就是静默丢日志**——这才是这套包装真正替你扛的代价。

**例③：分页插件「一个 selectPage 就分页了」**

❌ 只讲用法（L0）："调 `selectPage(page, wrapper)`，返回的分页对象里有 total 和当页数据。"

✅ 穿透三层：
> **L1 机制**：插件本质是个 **SQL 拦截器**——它把你写的那条查询 SQL 拦下来，**先自动跑一条 `SELECT COUNT(*)` 拿到总数，再在原 SQL 尾巴上拼 `LIMIT ? OFFSET ?` 跑第二次**，把两次结果塞进同一个 Page 对象。所以你看到"调了一次方法"，底下其实是**两条 SQL**。
> **L2 手写**：自己写 `countSql` + `limitSql` 两条、执行两次、组装——20 行。
> **L3 取舍（什么时候不该用它）**：它解决的是**重复劳动，不是性能**。深翻页（`OFFSET 100000`）照样要扫掉前十万行，插件救不了你——这时候该换"游标/上次最大 id"方案。**能说出"它不解决什么"，才算真的懂这个工具。**

**例④：穿透卡的 L1 本身（机制词 悬空 vs 拆到底）**

> 出处：第一次写穿透卡，L1 写「用 @Around 环绕」「ThreadLocal 是线程私有仓库」，用户反馈"还是浅"——因为「环绕」「私有」本身还是没讲过的词，穿透卡又犯了术语悬空。下面用同一机制做对照。

❌ 停在机制名词（L1 悬空）：
"切面用 `@Around` 环绕所有贴注解的方法；`LogRecordContext` 本质是 `ThreadLocal<Map>`，每个线程一张私有小仓库。"

✅ 拆到底（每个词落到已知地基）：
"切面 = Spring 给你的 Bean 换了个「替身」（代理对象）：JDK 动态代理（实现同接口的新类）或 CGLIB（继承你的子类），方法调用先打到替身的 `invoke()`，invoke 里执行「前置 → `proceed()` 调真方法 → 后置/异常」——**环绕通知就是这三件合成一个 proceed()**。
`ThreadLocal` 的数据不是存"池子"里，而是**存在每个 Thread 对象自己的 `threadLocals` 字段（一张自定义哈希表）**里：`set(v)` = 拿当前线程的表、以这个 ThreadLocal 实例为 key 存 v。所以"同线程可见、跨线程隔离"的真相是"表挂在 Thread 上、每个线程各一张"；线程池复用线程不清理就串号。"

**判定**：L1 里出现的每个机制词（环绕/私有/解析/代理/切面…），要么是语言基础，要么继续拆到落地层（代理对象、Thread 字段、AST 求值）。有一个悬空 = 穿透还停在"高级先修概念"，回炉。

**例⑤：@TableLogic（MyBatis-Plus 逻辑删除）悬空 vs 拆到底**

> 出处：批次5 首次写 @TableLogic 穿透卡，L1 写「MP 启动扫描实体发现有 @TableLogic，就记住逻辑删除字段，此后改写 SQL」——用户指出还是没拆到底，因为「扫描」「改写」本身就是悬空动词：**谁**扫描？**谁**改写？**改成什么**？停在动词上等于没讲机制。

❌ 停在悬空动词（L1 悬空）：
"MyBatis-Plus 启动扫描实体时发现有 @TableLogic，就记住'逻辑删除字段 = deleted'，此后把 deleteById 改写为 UPDATE、select 自动追加 deleted=0。"

✅ 拆到底（指到真实类名 + 三个环节）：
"①**记住**发生在启动初始化——`TableInfoHelper.initTableInfo()` 解析每个实体，把 `@TableLogic` 记进 `TableFieldInfo`（存逻辑删除字段 + 未删值 0 + 已删值 1），放进 `TableInfo` 元信息表；
②**删变改**发生在 SQL 注入——BaseMapper 方法本身没写死 SQL，是 `DefaultSqlInjector`（实现 `ISqlInjector`）启动时逐个生成的，`DeleteById` 里判断 `tableInfo.isWithLogicDelete()`：有逻辑删除字段就用 `SqlMethod.LOGIC_DELETE_BY_ID` 模板（生成 UPDATE），没有才用 `DELETE_BY_ID`（生成 DELETE）；
③**查自动过滤**发生在 WHERE 拼装——select 的 where 由 `TableFieldInfo` 自动 append `AND deleted=0`。"

**判定**：L1 里出现「扫描/改写/记住/注册/接管」这类**悬空动词**，必须追到「谁（类名）在哪个环节（初始化/注入/拼装）干了什么（模板名）」。追不到 = 还在悬空层，回炉。

**例⑥：@ConfigurationProperties（Spring Boot 绑定器）——L2 手写「一句话」vs「打满三层」**

> 出处：阶段5 复盘时，L2 只写了一句"换手写就得 `Environment.getProperty(...)` 逐个手工取"，L3 只写"易在边界出问题"。用户指出「讲解要深入底层、重开发能力而非 API 调用工程师」，于是补到打满三层。

❌ L2 一句话、L3 抽象形容词（不合格）：
"换手写就得 `Environment.getProperty("ai.providers.ollama.url")` 逐个手工取，绑定器把松散绑定全包了。不用它配置读不进，代码难维护。"

✅ 打满三层（L2 逐条点出边界，L3 落到"人-时-错"）：
> **L2 手写（够真，把绑定器替你扛的边界逐条摊开）**：
> ①**松散绑定**：yaml 写 `open-duration-ms`，字段要是 `openDurationMs`（驼峰）——手写得自己把 kebab-case 转 camelCase，漏了就键对不上、字段 null；
> ②**递归嵌套**：`ai.providers.ollama.url` 要一步步走到 `providers` 里的 `ollama` 对象再调 `setUrl`——手写没法"自动把嵌套层级解出来"，要自己遍历；
> ③**List/Map**：yaml 的 `- x` 列表自动塞成 `List<String>`、`fast:` / `standard:` 键名自动成 `Map` 的 key——手写得自己遍历键空间逐个塞；
> ④**占位符解析**：yaml 写 `${rag.default.dimension}` 会先解析再绑定——手写得自己扫 `${...}` 再查 Environment，嵌套占位符、循环引用都是坑；
> ⑤**缺省默认值**：yaml 没写就为 null——手写要逐个判、补齐默认值，常漏。
> **L3 具体故障（人-时-错）**：业务 01.250 提问 → `ModelSelector` 读 `ai.chat` → 配置类全 null（因类上没标 `@ConfigurationProperties`，绑定器不认它、`ai.*` 键读不进）→ 选不出模型 → NPE；**且因配置类没被绑定器处理，配置缺失的启动时校验也触发不了——是运行时才炸、不是启动时炸**，排查要翻半天。又若 yaml 写了 `open_duration_ms`（下划线）也绑得上（过度宽容），而字段写成 `openDurationMS`（S 大写）就会大小写失配 → 静默 null。

**判定规则（底层穿透三问 + 一句「拆到底」）**：讲完任何一个框架/工具调用，自问——
① **它怎么做到的**？（能说出内部几个步骤 / 指到关键类名吗？**每个机制词拆到底了吗**——"环绕"拆到代理对象、"线程私有"拆到 Thread 字段，还是停在悬空名词？）
② **不用它我怎么写**？（能给 20~40 行最小手写版吗？**够真吗**——是写出清理/失败改写/模板求值/解耦边界的真版，还是 try/catch+save 玩具骨架？）
③ **不用它会出什么具体故障 / 它解决不了什么**？（是"第 3 个并发请求读到脏数据"这种具体故障，还是"难维护"这种形容词？**最好落到分钟级时序**）
任一答不上 = 还停在 L0，回炉。**★ 类三问全答，普通调用至少答①。**

---

## 二、每段详细度基准（对照自查，任一项不达标 = 该类未讲完）

| 段 | 达标判定（明文标准） | 不达标表现 |
|---|---|---|
| ①业务场景 | 有**具体的人 + 具体数值/配置值 + 期望结果** | 出现"用户/请求"但无名字无数值 = 抽象句 |
| ②调用链 | **≥8 跳**（上方示范 14 跳），每跳带真实类名/方法名/具体异常名与消息形态；结尾有**读法提示** | ≤5 跳 或 抽象链（"请求→Spring→前端"） |
| ③代码块 | **整方法**贴出 + **逐行中文注释**（每处答三问：这是什么/为什么/不用会怎样；解释无悬空词）；**这份注释就是真实源码文件里的注释**（写码时已写进文件，讲解照贴）；每个手法有**反例对比**（不这样做会怎样） | 贴片段 / **标注"节选"** / "见下方落码"指向 / 只讲手法名 / 注释只写"为什么"四个字带过 / 用术语解释术语 / 注释只存在于讲解消息、源码文件是裸的 |
| ④用法 | **完整可跑片段** + **触发回放**（输入 → JSON/返回 → 前端反应）+ **收尾洞察句** | 只有注释式一句 |
| **底层穿透**（防「API 调用工程师」） | 凡框架/工具/中间件/SDK 调用，至少剥开一层讲**机制**（L1：它内部怎么做到的、替你干了哪几件事，**机制词拆到底无悬空**）；**★ 类与核心依赖**再补 **No-Framework 最小手写版**（L2：不用它你自己怎么写，20~40 行，**够真非玩具骨架**）+ **具体故障推演**（L3：不用它会出什么具体错 / 它解决不了什么，最好分钟级时序） | 通篇「这行调 X 的 Y 方法做 Z」只停在用法层（L0）；L1 停在「环绕/线程私有」这种悬空名词；手写版是 try/catch+save 玩具骨架；「不用它会怎样」写成「难维护/易出错」这类抽象形容词 |
| 实现四问 = 开发四问 | ①思路（为什么这么拆）②设计决策（为什么选这个不选别的、备选方案与代价）③手法（是什么→L1内部怎么做→L2换我怎么做→L3不用会怎样）④数据流与边界（异常/并发/降级在哪）。四问缺一问即不合格；每问**完整教学句**，穿透融进手法问 | 三问或两问带过 / 缺"设计决策"只陈述现状无方案对比 / **电报体名词串**（"手法：A+B"一行完事） |

## 三、讲解产出自查（每类讲完自问，任一 No 回炉）

1. 场景里有没有"具体的人 + 数值"？
2. 调用链 ≥8 跳、全带类名？有没有给读法提示？
3. 代码贴整方法（**没标"节选"**）+ 逐行中文注释（注释在**真实源码文件内**，讲解照贴，不是只在消息里）？每处注释三问齐吗？**解释链有没有悬空词**？每个手法有"反例（不这样做会怎样）"？
4. 用法能复制改改就跑？有没有触发回放？
5. 本批的框架/工具/中间件/SDK 依赖，剥开一层讲**机制**了吗（L1：它内部怎么做到的）？★ 类与核心依赖给了 **No-Framework 最小手写版**（L2）+ **具体故障推演**（L3）吗？
6. 「不用它会怎样」是**具体故障**（哪个请求/哪条数据/什么时序下出什么错），还是「难维护/易出错」这类抽象形容词？