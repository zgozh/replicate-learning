# 判据变更史（SKILL §6.6 登记表的历史部分 · 2026-09-18 整合移档）

> **为什么有这份文件**：判据每次变更都要登记（版本 / 日期 / 改了什么 / 影响面 / 存量工单），但 28 条累积在 SKILL 里会挤占正文。**2.1-2.25 共 25 条**移档到本文件；**最近 3 条（2.26/2.27/2.28）留在 SKILL §6.6**，新增变更仍写 SKILL（满了再往下移）。
> **移档不改一字**：下表为原表原样搬移。

| 版本 | 日期 | 改了什么 | 影响面 | 存量工单 |
|---|---|---|---|---|
| 2.1 | 2026-09-13 | 正向保真（双向核对）+ ★反向 + 注释密度 + 行号一致性 | 全量首次引入；多处批次转红 | 逐批修 |
| 2.2 | 2026-09-15 | 快照机制（区分"源码演进/编造"）+ ★ 父标题计入 + ③c 签名覆盖 | 9 批 77 行由 FAIL 转"演进" | 补批头快照 |
| 2.3 | 2026-09-15 | 内置 ⓪ 结构检查（节数/围栏/占位/⑫ 五项） | 50 批结构项转红 | 逐批修 ⑫ 与回链 |
| 2.4 | 2026-09-16 | 新增 ⑤ 用法与接入 + 用法片段免检边界（带 `// :Lnn` 不豁免） | 57 份批次 ⑤ 转红（503 件缺【怎么用】、62 件缺【怎么接】、57 份无 ⑦.5） | 按批次补（样板：阶段7批次2） |
| 2.5 | 2026-09-16 | 免检护栏认两种行号格式：**旧格式 `// :N` 不豁免**（此前只认 `// :Lnn`，标行号可混进免检）；另修 ③ 组箭头漏计 ★类签名（显示口径） | 实测 **0 个块命中** → 78 份讲解判定值全部不变；堵住的是未来把编造代码标成 `// :N` 的后门 | 存量 36 个文件/7315 处旧格式待统一升级（`scripts/fix_lineno.py` 可一次完成纠偏+升级） |
| 2.6 | 2026-09-16 | ⓪ 增**围栏配对**体检（A 块内再出现带标签围栏 / D 结束时未闭合 / H3 标题被吞）；原判据只查"数量为偶数" | 实测 **A/D/H3 全库 0 处** → 78 份判定值不变；暴露的是"删掉半对围栏或把开闭互换，数量仍为偶数"这一盲区（真实事故见血证 H14） | 无需回扫；改文件时闸门会当场拦下 |
| 2.7 | 2026-09-16 | ⑤ 增**位置**判据（【怎么用】须在「逐行要点表」之后、件内 `---` 之前）；A6 五层改为**定序**（顺序即结构）；样例 ⑯ 脚本改为"跑闸门"，应急 grep 版标注"只判奇偶、不够用" | 实测 **37 个已写⑤的件里命中 0 件** → 判定值不变；堵住的是"⑤ 落在件内 `---` 之后、被读成下一节"（真实事故见血证 H15） | 无需回扫 |
| 2.8 | 2026-09-16 | 新增 **⑥ 散文符号真实性**：正文里的 `Xxx.java:NN` 引用 / 件标题声明的 `.java` / `本仓类.method(` 三类必须真实存在；反引号其他符号走 `spec/散文符号白名单.txt` + 待确认清单 | 实测三类误报率≈0（R1 0 处 / R4 0 处 / R2 2 处，其中 1 处为真错）；**naive 版 371 处约 95% 是合法提及，故只取三条** | 存量 1 处真错已修（`UserContext.getLoginUser()` → `UserContext.get()`） |
| 2.9 | 2026-09-16 | ⓪ 占位判据扩到**含中文的 `__占位__`**（原只认 TODO/FIXME/CONT-/<<<SRC:）；配套发布 `scripts/new_batch.py`（骨架与模板同源）与 `scripts/inject_source.py`（源码块注入） | 实测 78 份讲解 + 2 份样例 **0 处命中** → 判定值全部不变；堵住的是"填空白骨架一路全绿"（血证 H18，新工具首跑即暴露） | 无需回扫；**新批次一律从 `new_batch.py` 的骨架起写**（契约 V4） |
| 2.10 | 2026-09-16 | 新增 **⑯ 判据版本标注**（`ver_marker`）：⑯ 段必须写明本批按哪版判据跑的。**报告项，不计 FAIL** | 实测只有 **6/78** 份写过版本；直接判 FAIL 会让 51 份转红、"绿"清零 → **降级为报告**，并在闸门与记分卡里点名；回归实测**判定变化 0**（报告项不进 rows） | 存量 73 份未标版本，一键补齐 `scripts/sync_gate_result.py`；配套新增 `scripts/annotate_gaps.py`（③ 最小标注点定位器） |
| 2.11 | 2026-09-16 | 新增 **⑫ 内容深度**（`vib_depth`）：A 八个条目必须命中八类语义（真实需求/现状勘察/方案比较/增量实现/提示词/产出后审查/验证反馈循环/最终沉淀）｜ B 第 5 条必须有可识别的提示词骨架（`【任务】` 与 `任务：` 两种写法都认）｜ C 第 5 条必须附「设计要点」｜ D 第 6 条审查清单 ≥6 项（表/编号/`- [ ]` 三种形态都认）。**同时把 `八段提示词 = 8` 降级为报告项** | ⑫ 是唯一跨项目复用、且唯一产生过"内容编造"级血证（H16）的一节，而闸门对它只有容器检查（数条数/数标签/数行数）——旧八条与现行八条同样全绿；`八段=8` 只数 `^【…】` 行总数，实测两类误报：合法写**两份**八段提示词的节（阶段10批次2 = 16）与用**中式标签**写成的成型提示词（阶段13批次2 = 0） | **先测误报率**：全库 57 份逐项度量（A 红 5 / B 红 4 / C 红 7 / D 红 5，合计 **FAIL 7/57**），7 份**逐份人工验伪、误报 0**；**回归判定变化 0**——这 7 份本来就因自指/八条/回链而 ⓪ FAIL，唯一变化是阶段10批次3 去掉误报后 FAIL→PASS。第一版量尺本身有 4 类误报（`- [ ]` 清单/中式标签/对话式方案比较/无"现象"二字的迭代），收窄后才定稿 | 存量工单 7 份（阶段14 四批 + 阶段11批次3·4 + 阶段13批次2）；报告档 13 维度进 `gate_all.py` 记分卡 |
| 2.12 | 2026-09-16 | ③ 注释密度的最低条数 `need` 一律以该块的**关键行数为上限**（`min(key_n, max(5, ⌈key_n/12⌉))`） | 原式对 ★ 块**无条件取 5**，而注释只能落在关键行上 → 「关键行 < 5 的 ★ 块」要求 5 条却最多只有 key_n 条，**数学上不可满足、怎么改都红**。实测全库 245 个 ③ 不达标块里 **27 个（11%）**属此类（涉及 7 份文件） | 修正后 **245 → 223**，**只放宽不收紧**；逐字段回归实测判定变化 0（血证 H22）；SSOT D2 / SKILL §6.1.1 / 血证档案三处同步。（**补记**：本条当时只登记在宿主状态页/SSOT/血证，漏了本表——2.13 一并补录） |
| 2.13 | 2026-09-16 | ② 反向完整度的**类名归属加「★ 标题链兜底」**（`star_class_fallback`）+ 同一 ★ 类只出一条记录（同类去重）。块内类声明优先，其次倒序取最近一个带 ★ 的祖先标题里的类名；**类名长度按 ≥3 字符**取（`[A-Z][A-Za-z0-9_]{2,}`） | 原实现只认「块自身含类声明」，而 ⑥ 明确要求 ★ 类**按职责段拆讲**（段1/段2…）——拆分后没有任何单块含类声明 → 本项**静默跳过**、打印「0 个★类」并 PASS。**这是一次真空通过，不是核验过**：全库 60 份批次里 **6 份**命中（阶段9 四批 + 阶段10批次1 + 阶段6批次1），其中 4 份一个类都没查过。自检当场又抓到第二层缺陷：照抄别处的 `{3,}`（≥4 字符）会让 `Dao`/`Rrf` 这类**短类名**解析不出来、仍真空通过 | ② 不达标 **6 → 16 份**；逐份人工验伪：缺失行**都是真缺口**（`@Slf4j` / 类声明 / 依赖字段 / 私有方法确实没在讲解里出现过），**误报 0**。逐字段回归实测：**只有 `rev_bad` 变（10 份）、其余 14 个字段零变化、判定层面新增 FAIL 0 份**（16 份里 6 份本来就已因别的组 FAIL）。存量工单 **16 份**；`annotate_gaps.py` 不管 ②，用 `show_reverse_miss` 式探针列缺行；自检 ⑫ 组新增 3 项（兜底生效 / 不误拒整文件 / 同类去重），检查项 42 → 45 |
| 2.14 | 2026-09-16 | ⑤ 的**件段边界**：件段（判断 iface / use / io / wire / 位置 的区间）**不得越过一级节标题** `## `（原实现让最后一个 6.x 件的段一直延伸到 EOF） | ⑥ 之后的小节（⑧ 穿透卡 / ⑨ No-Framework / ⑩ 反例）里常出现手写 `interface Xxx {` 骨架 —— 原实现把它们算进**末件**。实测全库：**10 件 iface 误判为真**（末件凭空多出「抽象件」身份，会被要求补一个语义上不该有的【怎么接】小节）、**6 件 wire 误判为真**——两者是同一次误判的两个面；`use`/`io` 实测 0 变化 | 逐字段回归 + **判定级复核**：只有 `usage` 字段的计数变（11 份），其余字段零变化；而 **⑤ 判定变化 0 份**——受影响项大多是「iface 与 wire 一起来自后文」，一起消失后该件回归**非抽象件**，判定不变。所以这一条**清掉的是「虚假工单」而不是把绿变红**：不修的话，接下来每批收口都会被要求给末件补一个语义上不该有的【怎么接】小节（阶段9批次2 的 6.12 已被点名过一次，差点照做）。**教训**：登记里的数字必须复核到「判定」那一层，不能拿探针的中间量外推（本版第一稿就写错成"6 件转红"）。自检 ⑬ 组新增 2 项（越界不许认 / 件内自己的必须认出），检查项 45 → 47 |
| 2.15 | 2026-09-17 | ⓪ 新增**17 个一级节标题规范形态硬检查**（`sec_missing`/`sec_deformed`）：`## ①`…`## ⑯` + `## 索引` 必须按规范形态命中（`##` 后恰好一个空格 + 带圈数字）；带圈数字丢失或多余空格 → **FAIL 并打印实际标题原文**。配套：`new_batch.py` 生成后自断言 17 节标题正则全命中 | 带圈数字（⑨⑩⑫⑮⑯ 等）在部分落盘链路会**静默丢失**——标题变形后 `^## ⑫` 这类定位正则静默失效，**那一节的全部检查被跳过却照样打印 PASS**（血证 H25：本技能自己的 `skill_selfcheck.py`/`gate_lecture.py` 就丢过 ⑨/⑫ 共三处，存活数周无人发现；本次修复并钉成判据）。全库实测（60 份批次文件，含 official-cold）：命中 **2 份**——`挑战C1`（12 节、全部标题无带圈数字）与 `批次4-事件桥`（索引节写成 `## ⑰ 索引与导航`，非规范形态）——两份**本来就红**（节数≠17 / 八条0 等），**判定翻转 0 份**，只是让失败原因精确到"哪一节缺了/哪一节变形"；58 份规范批次 0 误伤 | 无需回扫；两份命中文件的 ⓪ 失败原因更精确化。自检 ⑭ 组新增 4 项（规范全齐 / 丢 ⑫ / 多空格 / 缺索引），检查项 47 → 51 |
| 2.16 | 2026-09-17 | ⓪ 新增 **⑫ 回顾语 FAIL 档**（`VIB_RETRO` 14 词：回顾性重构/回顾性重建/本批已实现/本批已经实现/本批的代码/本批代码/本批实现/回看这一批/当时我们/已经写完/回述/事后重建/复刻出/那一批）——⑫ 必须以"能力尚不存在、从零构建"为起点（§6.2 写作视角铁律重构）；**报告档 13 → 15 维**（新增：既有资产清单带路径条目 ≥3、真实需求含企业级约束词 ≥1）。配套：SKILL §6.2/§6.2.1/§6.2.3 重构、模板 ⑫ 声明换代（"回顾性重构声明"作废，保留"逆向重建"诚实标注）、new_batch 自断言扩展 | 词表按全库实测给全（60 份批次 ⑫ 段逐一过目）：回顾性重构 33 处/26 文件、回顾性重建 7 处——**全部是旧框架的声明/标注形态，否定语境 0 处 → 误报 0**；其余 12 词实测 0 命中（防回潮）。**特意不收**的词：回看（实测 3 处全是"回看 SQL/正则"的调试叙事，误报）、这批代码（6 处里有 1 处是否定式声明"不是'这批代码怎么来的'"，误报）、当时的（合规声明"不是当时的真实对话"必含）、逆向重建（§6.2.3 合规声明用词） | 命中 = 真报（这些批次确实在用旧框架）：约 26 份批次的 ⑫ 由绿转红，**存量工单**：按 §6.2 重写⑫ 为从零构建叙事（声明换代 + 视角改写）。**报告项不进 FAIL**：既有资产/企业级约束两维只进报告档。自检 ⑮ 组新增 3 项（合规声明不误伤 / 旧声明必命中 / 典型回顾表述全命中），检查项 51 → 54 |
| 2.17 | 2026-09-17 | ⓪b 新增**每批必含内容**硬检查（`check_core_sections`，§6.4 ①⑦⑧ 从"通常会有"升为"必含可机械判定"）：**①** 架构图 = 围栏块内连接符/箭头 ≥5 行 且图中 ≥3 个标识符能在仓库中找到（类名/模块名/中间件名，通用词 STOP 表防呆）；**⑦.1** = ≥5 跳编号链且每跳带行号（`⑦.1`/`7.1` 标题都认，`:L41`/`Xxx.java:41` 都认）；**⑧** = L0~L3 四层级标签齐全 + 含"省略了什么生产边界"或等价表述（9 词短语表，另扫过"忽略/未处理"等近义形态无漏网）。配套：SKILL §6.4 ①⑦⑧ 必含项写明机械口径、模板三节加判定提示 | 全库实测（60 份批次 / 88 份 md）：① 命中 10 份（1 无 ① 节 + 8 无图 + 1 图内标识符不足）、⑦.1 命中 44 份（5 缺小节 + 39 散文链，16 份编号链批次全过）、⑧ 命中 32 份（1 无 ⑧ 节 + 1 缺层级标签 + 30 缺省略段）——抽查均为真缺，**误报 0**。命中率高的原因不是判据宽，是存量确实普遍没写（⑦.1 散文链是旧主流写法） | 命中 = 真报：命中批次由绿转红为**存量工单**（① 补图、⑦.1 散文链改编号跳链、⑧ 补省略清单）。自检 ⑯ 组新增 8 项（合规全过 / 无图 / 空盒子通用词 / 散文链 / 无行号 / 缺 L2 / 缺省略段 / `7.1` 替代标题不误报），检查项 54 → 62 |
| 2.18 | 2026-09-17 | ⓪c 新增**报告档**（不计 FAIL，用户要求 2/4）：A 代码块白话开场（围栏块上方 4 行内有含中文的开场行或说明表格）B ⑥ 件构造方式（谁 new/注入/生命周期）与实现手法（模式/数据结构/算法+权衡）点名统计。配套：SKILL §2 增"教材必须自足"、§6.1 增白话开场与构造方式/手法两条硬标准、模板 ⑥ 增指导语与填空行 | 报告项不进 FAIL 不需要误报率门槛，但仍实测：全库 60 份批次统计见 gate_all 输出（供存量工单排序）；口径防呆：表格行紧邻代码块不算缺开场（表格本身就是说明） | 无判定变化（纯报告档，不进 FAIL、不进 rows——基线比对一字不变）。自检 ⑰ 组新增 4 项（有/无开场、表格防呆、构造/手法计数、件段越节保护），检查项 62 → 66 |
| 2.20 | 2026-09-17 | 新增**教材类文件判定**（`gate_lecture.is_lecture`，两个入口同步：`gate_lecture` 与 `gate_all`）：结构判定不再只看「有没有 java 代码块」——含 >=5 行 java 块，**或教材标志投票 >=3 项**（带圈一级节标题 >=3 / 批头声明 / 行号标注 >=5 / 6.x 小节 / 7.5 或索引节）即判教材；声明位（前 8 行的 `>` 或 `# ` 行）写明「形态状态 / 节选 / 已存档」的参照物走**报告档** | 用户指令「按建议继续」（封堵盲区）。实测：`examples/黄金样例-python.md`（无 java 块）曾被整体跳过结构判定并打印 PASS（真空通过）；这与 H23（② 静默跳过）、H25（  定位失效）同族：**判据只在能识别出对象时才生效，于是改变对象的语言或形态就能绕过它** | 回归实测（79 份第一册批次：`gate_all.py --root agentic-rag/NOTES/教学讲解 --src agentic-rag`）逐字段零变化（均含 java 块，老口径不变）；行为自测 4+1 项（`skill_selfcheck` ⑧ 组：真实批次 / 参照物豁免 / **合成无-java 样本必须 FAIL** / 普通文档不误伤 / 批头「【历史版本示例】」不误判）；自检检查项 66 → 71 |
| 2.21 | 2026-09-17 | 新增**记录类形态判据**（`gate_lecture.is_record` + `check_record_shape`，`gate_all` 同口径报告）：整理批 / 进行中记录 / 草稿类文件**既不按 17 节判，也不能没人管**——三项：R1 首屏状态或日期 / R2 未完成与下一步清单 >=2 条 / R3 带成品批迹时状态行必须写未完成或草稿；识别：文件名含整理批/进行中/草稿/WIP/记录/续传/日志，或声明位含记录类；档位：自声明判据版本 >= 2.21 才判 FAIL，否则报告档（不追溯存量，同 2.19 机制） | 用户「A 能更好完善质量就继续」。实测：79 份语料里 2 份记录类（批次0-整理批 / 批次1a-进行中记录属旧存量，无版本声明 → 报告档）；它们此前落在「教材标志 <3 → 普通文档」的灰区，**静默逃过一切形态约束**（H23/H25 同族） | 回归实测（79 份）：逐字段零变化；自测 5 项（`skill_selfcheck` ⑲ 组：识别 / 合规 0 违规 / R1+R2 命中 / R3 伪装命中 / 版本门控）；自检检查项 71 → 76；同时修掉 `GATE_VERSION` 被重复赋值两行的缺陷 |
| 2.19 | 2026-09-17 | 给 2.17 引入的 ⓪b 三项（① 架构图 / ⑦.1 编号链 / ⑧ 省略段）加**适用档位**（`gate_lecture.core_scope` + `CORE_FAIL_SINCE = "2.18"`）：批次在 ⑯ 声明「判据版本 >= 2.18」→ 三项判 FAIL；未声明或声明更早 → 降**报告档**（`[报告]` 前缀 + 数字照打；`gate_all` 聚合打印、**不进 rows**）；`new_batch.py` 生成骨架时即写入当前判据版本，**新批一律从严** | 用户 2026-09-17 指令「按建议改一下那个 FAIL 档」。2.17 把三项从「通常会有」直接升为 FAIL，而 79 份回归集里 **62 份从没写过判据版本**——拿新判据追溯旧产物，实测 ①图缺 9 / ⑦.1链缺 43 / ⑧穿透缺 31，多份由绿转红；这与 2.10（⑯ 版本标注 6/78）与 2.11（⑫ 内容深度）两次的处置原则（**先报告档、实测误报率、清完存量再议升级**）冲突 | 回归实测（`gate_all.py --root agentic-rag/NOTES/教学讲解 --src agentic-rag`，79 份）：**变化只落在 ⓪ 的 struct 字段**，`lost / unattr / rev_bad / den_bad / sig_bad / ln_bad / usage` 逐字段零变化；新批（阶段1批次1）⑯ 已声明 v2.18 → 仍按 FAIL 档核验并保持 PASS |
| 2.22 | 2026-09-16 | **注释标记按语言取前缀**（`ANNO`/`L_ANNO` 认 `//` `#` `--`）+ **节数与节标题形态只在正文里数**（`_prose_lines()` 先剔除代码围栏内部的行）。配套：`spec/00-质量契约.json` 的 A1 改写 + 新增 S22；`spec/血证档案.md` 新增 H27 | 用户「继续」（在第一个非 Java 项目 deer-flow 上首跑）。三道判据以**两种相反的方式**失效：① `ANNO` 只认 `//` → YAML 里写对的 `# :Lnn  ←教材：…` **一条都认不出来**（221 条教材注释被 ③ 判 FAIL）；② `check_structure` 数 `^## ` 不跳围栏 → 注入的 `Makefile:64` 分节注释 `## Setup & Diagnosis` 让节数 17 → 18，并「切断」件段、**连带使 ⑤ 误报**；③ `inject_source.py` 前缀写死 `//` → 在 shell 上是**非法语法** | 回归实测（10 份 Java 批次，`ragent-official`）：`总判定` + `[FAIL]` 行归一（抹平版本号）后**逐份一字不变**，10/10 仍 PASS；`skill_selfcheck.py` 76 项 **0 失败**；`v2_selfcheck.py` PASS。存量工单 **0**（Java 批次判定不变）；但对**非 Java 首次落盘**的批次是**解锁**（此前只能靠项目内替代件绕） |
| 2.23 | 2026-09-17 | 修两处「注入源码里的 markdown 行被当成讲义结构」的**误判**（H27 同族第二轮）：(a) 块内 `### ` **只在含中文时报警**（讲义节标题一律中文，注入源码里的英文 `### xxx` 是代码不是标题）；(b) **件段边界只认正文** `## `——新增 `_prose_mask`（与 2.22 的 `_prose_lines` 同口径），`check_usage` 不再用原始行找件段边界，围栏内的源码行（实测 FastAPI `description` 里的 `## DeerFlow API Gateway`）不再被当成一级节标题把件段提前切断 | 只收误判、**不新增 FAIL 项** → 存量批次只会由红转绿、不会由绿转红；受益面 = 首跑含 markdown 风格源码注释的项目（Python / Go / Shell / Makefile 等）；回归口径：判定层「总判定 + `[FAIL]` 行」归一后逐份一字不变 | 存量工单 **0**（不追溯）；本行为**补记**：gate 先落地而登记滞后一天，后续判据变更仍须按本节三件套走完（递增 → 登记 → 回归） |
| 2.24 | 2026-09-17 | gate 对 Python 等注释型语言生效（**非 Java 语言实检**，H27 家族收尾）：(a) 非 Java 块的 ① 内容保真 / ④ 行号一致性 / ② ★文件覆盖不再真空通过——有同名 `.blocks.json`（位置契约）时按**位置级语义**判 FAIL，无 manifest 时按标题反引号归属做内容级核对但**只报告不判红**（多源拼块/同名歧义下归属会选错，ragent 批次1 多 profile yaml 实测误报）；(b) ③ 密度对 py/sh 为**报告档**（`is_key_lang` 首版近似，语料校准后单独升档）；(c) `prose_index` 扩展名白名单跨语言（.py/.ts/.go/.sh 等）——① 图标识符只可能 FAIL→PASS；(d) `safe_edit` 的块内 H3 收窄为含中文才报（与 2.23a 对齐，解锁批次11 机器补章）；(e) Java 路径零改动（双索引隔离：java 走原 by_class/rev，py 走新索引） | 新增实检 + 只收误判；py 从"真空 PASS"变实检，存量 Java 判定层归一后逐份一字不变（23 份语料验证）；py 侧唯一可能由绿转红的路径 = manifest 位置级核对，而 manifest 由注入器生成、注入即保真 | 存量工单 0（不追溯）；回归：ragent-official 23 份 Java 判定层 diff=0 + deer-flow 17 批 PASS（④p 位置核对 725 处/0 漂移）+ skill_selfcheck 0 失败 |
| 2.25 | 2026-09-18 | 新增 **⓪d 形态质量 FAIL 档**（`check_form` + `FORM_FAIL_SINCE=2.25` 版本门）：判据全部从高质量批（批次21/32）正向提取——**②** 每个图围栏内箭头/连接线 ≥3 行；**⑤** 穿透表表头含「穿透+证据」+ 骨架表含「业务镜头」；**⑥** 每件（历史版本小节免检）必有「要解决的一个问题」开场 + 逐行要点表锚 + ★件「逐步回放」；**⑭** 完整答案数（`**答**：`/`答：`/`A：` 都认）== 题数且题数 ≥10。**动机 = Goodhart 效应实录**：用户点名批次33+ 讲解质量回退，逐节扫描证实——闸门机械判定覆盖的形态全程零退化（⑫ 八条八段始终 115+ 行），**未覆盖的 §6.4 必含形态整体丢失**（② 流程图 33-38 全无 / ⑤ 双清单→单清单 / ⑥ 问题开场与逐步回放 33+ 归零 / ⑭ 完整答案 34+ 退化成钩回指针）；根因三条链：闸门盲区 + 黄金样例节选本身缺 ②⑤⑭（参照物退化）+ §6.4.0「可选校准」致链式漂移。配套三道锁：SKILL §6.4 判定表 ⓪d 行 + §6.4.0 **开批三读**（读模板→读黄金样例对应节→读黄金批对应节，⑯ 记录「已对照」）+ 黄金样例节选补 ②⑤⑭ 实物、索引 canonical 条件加 ⓪d | **19 批校准（批次20-38）与退化曲线精确吻合**：21/22/23/27/28 全绿；20/24/25/26/29/30/31/32 报告档部分缺口（⑤ 表头措辞旧/单★回放/⑭ 6题等，均为真实历史形态、不过度调参）；33-38 全红 | **存量工单**：批次38 已全项回修为 **v2.25 FAIL 档 PASS 样板**（② 箭头 18 行 / ⑤ 双表 / ⑥ 4件问题开场+要点表+★回放 / ⑭ 12题完整答案，canonical 实例已登记索引）；**批次33-37 待三痛项回修**（②图+⑤穿透清单+⑭完整答案；⑥ 三项可选，补齐才升 v2.25 否则保持 v2.24 报告档）；未补齐批次按版本门走报告档不判红 |
